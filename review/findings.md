# Findings ledger

Source of truth for the thermo-nuclear review. One row per finding. `state` is `Fixed` or `Deferred (reason)` once Phase 3 is done; `verdict` comes from Phase 2.

| id | file:line | rule | severity | evidence | verdict | state |
|---|---|---|---|---|---|---|
| F001 | app/src/androidTest/java/dev/stan/yotsuba/CatalogFlowTest.kt:40 | bug | critical | openCatalog() starts with composeRule.waitForText(TestSeed.BOARD_TITLE) but never navigates off the start destination. AppNavHost.kt:160 sets startDestination = Route.Home; HomeScreen.kt:78-122 renders boards from settings.favouriteBoards, and Settings.kt:70 defaults favouriteBoards = emptySet(), so Home renders the EmptyState whose strings are "No favourite boards yet" / "Star a board and it show | Confirmed | Open |
| F002 | app/src/androidTest/java/dev/stan/yotsuba/HiddenThreadFlowTest.kt:31 | bug | critical | Same defect as CatalogFlowTest: the test opens with composeRule.waitForText(TestSeed.BOARD_TITLE) directly on the Home start destination, which shows only the "No favourite boards yet" empty state (HomeScreen.kt:118-124, strings_home.xml:4-6) because Settings.kt:70 defaults favouriteBoards to emptySet(). The Boards tab is never tapped, so "Technology" never appears and the single test in this clas | Confirmed | Open |
| F003 | app/src/main/AndroidManifest.xml:23 | bug | critical | `android:allowBackup="true"` is set with no `android:dataExtractionRules` and no `android:fullBackupContent`; `app/src/main/res/xml/` contains only `file_paths.xml` and `watched_threads_widget_info.xml`. With targetSdk 37 that means Android Auto Backup ships the whole `databases/` and `datastore/` tree — bookmarks, reading history, hidden threads, claimed posts, settings — to the user's Google Dri | Confirmed | Open |
| F004 | app/src/main/java/dev/stan/yotsuba/core/database/dao/BookmarkDao.kt:33 | bug | critical | `markSeen` is `UPDATE bookmarks SET readUpTo = MAX(COALESCE(readUpTo, 0), :postNo) WHERE board = :board AND threadNo = :threadNo` — the WHERE clause has no "only when it rises" condition, so the row is written on every call even when the value is unchanged. `ThreadViewModel.raiseReadMark` (:532-539) calls it for every post crossed, fed by `bottomVisiblePostNo...collect(::raiseReadMark)` at :187 wi | Confirmed | Open |
| F005 | app/src/main/java/dev/stan/yotsuba/core/designsystem/component/TabScaffoldSlots.kt:50 | bug | critical | `SideEffect { if (slots.owner !== token) { slots.owner = token; ... } }` runs after EVERY successful recomposition of `TabChrome`, not once per mount. The KDoc at line 33-34 claims "the newest caller wins", but nothing here encodes recency — it is last-writer-wins on each recomposition. During a tab switch both destinations are composed at once (AppNavHost.kt:151-160 cross-fades them). Sequence: B | Confirmed | Open |
| F006 | app/src/main/java/dev/stan/yotsuba/core/text/PostHtmlParser.kt:150 | bug | critical | decodeEntity does `entity.drop(2).toIntOrNull(16)?.let { String(Character.toChars(it)) }` (line 150) and `entity.drop(1).toIntOrNull()?.let { String(Character.toChars(it)) }` (line 152). `Character.toChars` throws IllegalArgumentException for any int that is not a valid code point (negative, or > 0x10FFFF). The `semi - i <= 10` guard at line 100 permits entities up to 9 chars, so `&#x1FFFFF;` (209 | Confirmed | Open |
| F007 | app/src/main/java/dev/stan/yotsuba/data/repository/MediaVaultRepositoryImpl.kt:564 | bug | critical | `rescan()` rebuilds every row via `savedMediaEntity(meta, f, file)` (SavedMediaMappers.kt:42-60) and then calls `savedMediaDao.replaceAll(rebuilt)`, which is `clearAll()` + `insertAll()` (SavedMediaDao.kt:57-62). The sidecar-based mapper sets no `md5`, `phash` or `pixelSize`, and those columns default to null (Entities.kt:84-90). The sidecar itself has no md5/phash field (VaultMeta.kt:42-65), so t | Confirmed | Open |
| F008 | app/src/main/java/dev/stan/yotsuba/feature/thread/ThreadScreen.kt:295 | 2 spaghetti growth | critical | The compose compiler report (app/build/compose_compiler/app-composables.txt:663-672) shows `restartable skippable ... fun PostCard(... stable actions: PostCardActions ...)`. Because `PostCardActions` is reported stable (app-classes.txt:1671), Compose skips PostCard only when the instance compares `equals`. But `postCard` (ThreadScreen.kt:294-306) calls `actionsFor(post, inPreview)` (:233-282) per  | Confirmed | Open |
| F009 | app/src/main/java/dev/stan/yotsuba/feature/thread/ThreadScreen.kt:354 | bug | critical | The LazyColumn key lambda contains escaped dollar signs that never interpolate: ``` is ThreadRow.MoreReplies -> "more-${'$'}{row.parentNo}" is ThreadRow.Filtered -> "filtered-${'$'}{row.postNo}" ``` `${'$'}` is a template expression yielding the literal character `$`; the following `{row.parentNo}` is plain text. Verified against the raw bytes (`cat -A` shows `"more-${'$'}{row.parentNo}"`), and `g | Confirmed | Open |
| F010 | app/src/main/java/dev/stan/yotsuba/feature/thread/ThreadScreen.kt:418 | bug | critical | Same escaping artifact in the share intent: `putExtra(Intent.EXTRA_TEXT, "${'$'}{Urls.threadWebUrl(board, threadNo)}#p${'$'}{post.no}")`. The shared text is the literal string `${Urls.threadWebUrl(board, threadNo)}#p${post.no}` — not a URL. "Share post link" from PostActionSheet (PostActionSheet.kt:51) therefore sends garbage to every share target. Contrast ThreadTopBar.kt:126-136, which shares `w | Confirmed | Open |
| F011 | app/src/main/java/dev/stan/yotsuba/feature/thread/ThreadViewModel.kt:520 | 6 canonical layer | critical | `onVisiblePostsChanged` rebuilds the ENTIRE row list from scratch on every visible-index change: `val rows = rows(details, session.value, filterVerdicts(details.posts, matcher.value))`. It is driven from ThreadScreen.kt:171-175, a `snapshotFlow { listState.firstVisibleItemIndex to ...visibleItemsInfo.lastOrNull()?.index }` collected in a `LaunchedEffect` — i.e. on the Main dispatcher, once per dis | Confirmed | Open |
| F012 | app/src/main/java/dev/stan/yotsuba/feature/vault/VaultStatsSheet.kt:69 | bug | critical | `items(stats.biggestThreads, key = { it.location })` passes a `VaultLocation` — a plain `data class` (domain/model/VaultEntry.kt:13), neither Parcelable nor Serializable — as a LazyColumn item key. Compose routes every item key through `SaveableStateHolderImpl.SaveableStateProvider`, which does `require(parentSaveableStateRegistry?.canBeSaved(key) ?: true)`. Verified in the actual artifact on the  | Confirmed | Open |
| F013 | app/src/test/java/dev/stan/yotsuba/database/RoomTest.kt:28 | bug | critical | `YotsubaDatabase` is at `version = 10` and `core/di/Modules.kt:127` wires nine hand-written migrations (`MIGRATION_1_2` … `MIGRATION_9_10` in `core/database/Migrations.kt`), and schemas are exported to `app/schemas/dev.stan.yotsuba.core.database.YotsubaDatabase`. The only Room test in the repo is this one, and it opens `Room.inMemoryDatabaseBuilder(...)` which creates the schema at version 10 dire | Rejected (Not a defect at line 28 — that line just builds an in-memory DB for DAO tests, which is exactly what it should do; the claim is a test-coverage gap, and the thermo-nuclear rules cover structure/abstra) | Deferred (rejected in verification) |
| F014 | app/src/androidTest/java/dev/stan/yotsuba/BoardsFavouriteFlowTest.kt:39 | 5 type/boundary | high | `val fake = settingsRepository as FakeSettingsRepository` — an unchecked downcast repeated in four files: here, CatalogFlowTest.kt:64, RevealSpoilersFlowTest.kt:36, SettingsFlowTest.kt:36. Each also declares `@Inject lateinit var settingsRepository: SettingsRepository` purely to cast it away. The cast is unnecessary: FakeSettingsRepository (TestRepositoryModule.kt:323-331) is `@Singleton` with an  | Confirmed | Open |
| F015 | app/src/androidTest/java/dev/stan/yotsuba/FlowTestHelpers.kt:10 | 2 spaghetti growth | high | All 12 instrumented test classes copy the identical 15-line preamble: `@get:Rule(order = 0) val hiltRule = HiltAndroidRule(this)`, `@get:Rule(order = 1) val composeRule = createAndroidComposeRule<MainActivity>()`, and `@Before fun setUp() { hiltRule.inject() }` — BoardsFlowTest.kt:25-34, BoardsFavouriteFlowTest.kt:23-35, BookmarksFlowTest.kt:16-25, CatalogFlowTest.kt:25-37, HiddenThreadFlowTest.kt | Confirmed | Open |
| F016 | app/src/main/java/dev/stan/yotsuba/core/datastore/SettingsDataStore.kt:57 | bug | high | `migrateLegacy()` does `dataStore.edit { p -> p.clear(); p[blobKey] = ... }` on the single app-wide `user_preferences` DataStore (core/di/Modules.kt:68, :143). That store is shared: MediaVaultRepositoryImpl.kt:55 defines `VAULT_MIGRATED = booleanPreferencesKey("vault_legacy_migrated_v1")` and writes it at :598. The guard at :54 is `prefs[blobKey] == null && prefs.asMap().isNotEmpty()` — and `blobK | Confirmed | Open |
| F017 | app/src/main/java/dev/stan/yotsuba/core/designsystem/component/SettingsRows.kt:42 | ui/ux | high | `SwitchRow` puts `Modifier.clickable { onToggle(!checked) }` on the Row (line 42) and also passes `onCheckedChange = onToggle` to the `Switch` (line 51). To TalkBack that is two separate focusable nodes: the Row announces "Reduce motion. Reduce animations… double tap to activate" with no on/off state, and the Switch announces "on/off switch" with no label. Every switch in Appearance, Reading, Medi | Confirmed | Open |
| F018 | app/src/main/java/dev/stan/yotsuba/core/designsystem/ReducedMotion.kt:29 | 6 canonical layer | high | Three separate problems converge on this one 33-line file. (a) `rememberReducedMotion()` does `remember(context) { isReducedMotion(context) }` — a `Settings.Global.getFloat` ContentResolver lookup — at every call site instance. Call sites include `MotionSpecs.kt:38` (`animatedListItem`, invoked per LazyColumn row), `MotionSpecs.kt:50` (`animatedGridItem`, per grid cell), `MotionSpecs.kt:81` (`reme | Confirmed | Open |
| F019 | app/src/main/java/dev/stan/yotsuba/core/update/Updater.kt:205 | bug | high | statusReceiver's `else` branch retries whenever `allowSilent` is true, but `allowSilent` only records how the session was *created*, not whether the user was already shown the installer. Flow on any Android 12+ device where the silent update is declined: commit with USER_ACTION_NOT_REQUIRED -> STATUS_PENDING_USER_ACTION (line 186) -> we launch the system installer (line 191) -> user taps Cancel -> | Confirmed | Open |
| F020 | app/src/main/java/dev/stan/yotsuba/core/util/UiState.kt:32 | 7 orchestration/atomicity | high | `load()` does `if (showLoading) result.value = null; return scope.launch { result.value = fetch(forceRefresh) }` and keeps no reference to the previous job, so overlapping loads all run and the *last to complete* wins. CatalogViewModel calls `load()` in `init` (CatalogViewModel.kt:62) and exposes `load(forceRefresh = true)` for pull-to-refresh (line 67) plus `retry()` (line 75) — all against the s | Confirmed | Open |
| F021 | app/src/main/java/dev/stan/yotsuba/core/widget/WidgetDeepLink.kt:13 | 0 structural simplification | high | There are two independent mechanisms for the same concept — "an intent asked us to open something". `ShellViewModel.pendingLink` (a StateFlow of `Urls.InternalLink`, survives rotation by living in a ViewModel) and `WidgetDeepLink` (a process-wide `object` with its own MutableStateFlow and a bespoke `Target(board, threadNo)` type that is structurally `InternalLink.Thread` minus postNo). MainActivit | Confirmed | Open |
| F022 | app/src/main/java/dev/stan/yotsuba/core/work/BookmarkRefreshScheduler.kt:24 | 0 structural simplification | high | WorkManagerBookmarkRefreshScheduler (BookmarkRefreshScheduler.kt:24-41) and WorkManagerVaultSyncScheduler (VaultSyncWorker.kt:84-99) are the same class twice: same @Singleton @Inject context, same `if (!WorkManager.isInitialized()) return` guard with the same comment ("Same guard as the bookmark scheduler"), same PeriodicWorkRequestBuilder + Constraints(requiredNetworkType = NetworkType.CONNECTED) | Confirmed | Open |
| F023 | app/src/main/java/dev/stan/yotsuba/core/work/BookmarkRefreshScheduler.kt:33 | bug | high | b4a6c33 added `Settings.bookmarkRefreshMinutes` (Settings.kt:63) and a user-facing chip row for it (ReadingSection.kt:44-50, options 15/30/60/180). Nothing reads it: `grep -rn bookmarkRefreshMinutes app/src/main` returns only the Settings field and the ReadingSection row. The scheduler built one commit earlier (47d9dcb) hardcodes `PeriodicWorkRequestBuilder<BookmarkRefreshWorker>(BookmarkRefreshWo | Confirmed | Open |
| F024 | app/src/main/java/dev/stan/yotsuba/core/work/BookmarkRefreshWorker.kt:44 | bug | high | b4a6c33 added `Settings.bookmarkNotifications` (Settings.kt:65) with a switch in ReadingSection.kt:51-56 titled "Notify on new replies". The only consumer is ShellViewModel.kt:32, which uses it to decide whether to *ask for the POST_NOTIFICATIONS permission*. The worker that actually posts the notification does `if (summary.newUnread > 0) notify(summary.newUnread, summary.threadsWithNew)` with no  | Confirmed | Open |
| F025 | app/src/main/java/dev/stan/yotsuba/data/repository/MediaVaultRepositoryImpl.kt:58 | 0 structural simplification | high | One class holds eleven unrelated concerns across 625 lines: storage-access state (69-85), save (95-144), snapshot read (146-156), delete (158-174), an in-memory trash state machine with its own `Trashed` type and unsynchronised `mutableMapOf` (176-231), MediaStore gallery export (233-285), local-file import with its own synthetic-post mapper and stream copier (287-373), sync/snapshot/rate-limited  | Confirmed | Open |
| F026 | app/src/main/java/dev/stan/yotsuba/data/repository/MediaVaultRepositoryImpl.kt:100 | 6 canonical layer | high | The thread directory is assembled by hand in four places: MediaVaultRepositoryImpl.save L100-105 (`File(File(store.root, sanitizeSegment(board)), threadDirName(...)).apply { mkdirs() }`), importLocalThread L299-302, VaultLegacyMigration.migrateMatched L106-109, VaultStore.snapshot L116-119 (and threadDir L71 builds the board dir a fifth time). meta.json is read with the same `File(dir, META_FILE_N | Confirmed | Open |
| F027 | app/src/main/java/dev/stan/yotsuba/data/repository/MediaVaultRepositoryImpl.kt:531 | 4 magic/wrappers | high | `val fromMeta = store.updateMeta(from) { it }` uses a read-modify-write with an identity transform purely to *read* meta.json — it re-encodes and rewrites the file (VaultStore.kt:56-63 always calls `writeSidecar`) as a side effect of a read. The reason is that `VaultStore` exposes no read: the decode snippet `File(dir, VaultPaths.META_FILE_NAME).takeIf { it.isFile }?.let { VaultMetaCodec.decode(it | Confirmed | Open |
| F028 | app/src/main/java/dev/stan/yotsuba/data/repository/MediaVaultRepositoryImpl.kt:541 | 7 orchestration/atomicity | high | Inside the per-file loop of `mergeThreads`, lines 541-542 call `store.updateMeta(into) { it.upsert(...) }` and `store.updateMeta(from) { it.remove(...) }` once per file. Each `updateMeta` is a full read-decode-transform-encode-write of a meta.json whose `files` list grows with N (VaultStore.kt:56-63, `upsert` at VaultMeta.kt:33-36 rebuilds the list). Merging N files therefore does 4N file operatio | Confirmed | Open |
| F029 | app/src/main/java/dev/stan/yotsuba/domain/model/Bookmark.kt:25 | 0 structural simplification | high | Three @Deprecated fields on the domain model are dead in practice. `lastSeenPostNo`: nothing ever writes a non-null value — BookmarkDao.markSeen (BookmarkDao.kt:33-37) only touches readUpTo, ThreadViewModel.kt:340 and BackupFile.kt:130 pass null, Mappers.kt:209/227 just round-trip the column. `newReplies`/`unreadCount`: Mappers never copy them (always default 0); `rg -n -w 'newReplies\|unreadCount | Confirmed | Open |
| F030 | app/src/main/java/dev/stan/yotsuba/domain/repository/BoardRepository.kt:3 | 3 design cleanliness | high | Eight of the twelve repository interfaces carry a byte-identical 15-line import header (DataResult, Board, Bookmark, CatalogThread, HiddenThread, HistoryEntry, MediaItem, ImportSource, Settings, ThreadDetails, VaultSyncSummary, VaultEntry, VaultError, VaultSaveContext, Flow) that was clearly copy-pasted when a god-file was split. Counted unused imports per file: BoardRepository.kt 13 of 15 (uses o | Confirmed | Open |
| F031 | app/src/main/java/dev/stan/yotsuba/domain/repository/BookmarkRepository.kt:48 | 5 type/boundary | high | Interface carries three dead/vestigial members: `markSeen(..., replyCount: Int)` whose doc says "[replyCount] is ignored; it stays for source compatibility" (impl line 223 drops it); `@Deprecated updateUnread` which has zero production callers (grep over main finds only the override at BookmarkRepositoryImpl.kt:227 returning Unit); `refreshOne` whose only callers are BookmarkRepositoryImplTest.kt: | Confirmed | Open |
| F032 | app/src/main/java/dev/stan/yotsuba/domain/repository/BookmarkRepository.kt:52 | 5 type/boundary | high | 71a3003 replaced the two unread counters with `readUpTo` but kept the whole old API alive as dead weight. `updateUnread` is a mandatory interface member whose only implementation is `= Unit` (BookmarkRepositoryImpl.kt:159) and which has zero production callers — every other occurrence is a no-op override in a fake (ThreadViewModelTest.kt:109, SettingsViewModelTest.kt:84, BookmarksViewModelTest.kt: | Confirmed | Open |
| F033 | app/src/main/java/dev/stan/yotsuba/domain/repository/MediaVaultRepository.kt:14 | 5 type/boundary | high | MediaVaultRepository declares 20 members and 11 of them ship a default body: `storageAccess` (l.23), `refreshStorageAccess` (l.25), `trash` (l.47), `restoreTrashed` (l.50), `purgeTrash` (l.53), `exportToGallery` (l.59), `syncSavedThreads(onProgress, skip)` (l.91), `snapshotThread` (l.99), `snapshotThreads` (l.108), `renameThread` (l.114), `mergeThreads` (l.122). Five of them return `VaultError.Io( | Confirmed | Open |
| F034 | app/src/main/java/dev/stan/yotsuba/domain/repository/MediaVaultRepository.kt:88 | 5 type/boundary | high | Two overloads of the same operation differ only by a `skip: Set<VaultLocation>` parameter, and the two-arg default silently drops it: `suspend fun syncSavedThreads(onProgress: (done: Int, total: Int) -> Unit, skip: Set<VaultLocation>): VaultSyncSummary = syncSavedThreads(onProgress)` (l.88-91). The KDoc at l.85-87 promises the skipped threads will not be fetched twice; the default body guarantees  | Confirmed | Open |
| F035 | app/src/main/java/dev/stan/yotsuba/feature/boards/BoardsViewModel.kt:88 | 5 type/boundary | high | Board visibility is stored as two independent sets that can contradict each other (`Settings.hiddenBoards`, `Settings.hiddenCategories`, Settings.kt:71-72 — read nowhere outside this file except a count in SettingsScreen.kt:86 and a bulk NSFW hide in SettingsViewModel.kt:103). Because there is no single source of truth, `onToggleBoardVisible` needs a special branch (lines 92-99) that expresses "sh | Confirmed | Open |
| F036 | app/src/main/java/dev/stan/yotsuba/feature/bookmarks/BookmarksList.kt:78 | 6 canonical layer | high | `BookmarksList(viewModel: BookmarksViewModel, …)` calls `viewModel.uiState.collectAsStateWithLifecycle()` at line 84, but its host has already collected the very same StateFlow at ThreadsScreen.kt:68 (`val bookmarks by bookmarksViewModel.uiState.collectAsStateWithLifecycle()`) to drive the app bar. The same double collection exists for history (HistoryList.kt:62 vs ThreadsScreen.kt:69). The seam i | Confirmed | Open |
| F037 | app/src/main/java/dev/stan/yotsuba/feature/bookmarks/BookmarksList.kt:96 | bug | high | `snapshotResults` is a `MutableSharedFlow(extraBufferCapacity = 1, DROP_OLDEST)` with replay 0 (BookmarksViewModel.kt:51-53) and its only collector is `LaunchedEffect(viewModel)` inside `BookmarksList`. `BookmarksList` is composed only for `ThreadsSegment.WATCHED` inside a `Crossfade` (ThreadsScreen.kt:143-150). `snapshot()` runs in `viewModelScope`, so the vault write outlives the composable: lon | Confirmed | Open |
| F038 | app/src/main/java/dev/stan/yotsuba/feature/bookmarks/BookmarksList.kt:115 | ui/ux | high | `if (state.loaded && state.bookmarks.isEmpty())` shows the empty state, `else` renders the PullToRefreshBox. `BookmarksUiState()` starts with `loaded = false, bookmarks = emptyList()` (BookmarksViewModel.kt:35), so between first composition and the first repository emission the screen is an empty LazyColumn — a blank area with no skeleton, no spinner, and then the rows pop in, shifting nothing int | Confirmed | Open |
| F039 | app/src/main/java/dev/stan/yotsuba/feature/bookmarks/BookmarksViewModel.kt:137 | 5 type/boundary | high | `snapshotting` is typed `Set<String>` (line 37) and the key format is a companion function `snapshotKey(board, threadNo) = "$board/$threadNo"` (line 137) that the *UI* has to call to interrogate the state: `BookmarksViewModel.snapshotKey(bookmark.board, bookmark.threadNo) in state.snapshotting` at BookmarksList.kt:147-148 and again at BookmarksList.kt:158. A private encoding detail of the ViewMode | Confirmed | Open |
| F040 | app/src/main/java/dev/stan/yotsuba/feature/home/HomeScreen.kt:83 | bug | high | `rememberPagerState(initialPage = savedPage.coerceIn(0, maxOf(pageCount - 1, 0)), pageCount = { pageCount })` is evaluated at line 83, before the `when` at line 115. `HomeViewModel.boards` (HomeViewModel.kt:23-25) starts at `null`, so on the very first composition `pageCount == 0` and `initialPage` collapses to `coerceIn(0, 0) == 0`. `rememberPagerState` only honours `initialPage` at creation, so  | Confirmed | Open |
| F041 | app/src/main/java/dev/stan/yotsuba/feature/home/HomeScreen.kt:95 | 6 canonical layer | high | Commit c503b00 ("Hoist the tab Scaffold into AppNavHost") established the rule stated in AppNavHost.kt:68-71: "Tab screens fill the top bar and FAB through TabScaffoldSlots; pushed screens bring their own Scaffold". BoardsScreen, ThreadsScreen and VaultScreen all take `slots: TabScaffoldSlots` and call `TabChrome`. HomeScreen is a top-level destination (`Route.Home` is in TopLevelDestination, and  | Confirmed | Open |
| F042 | app/src/main/java/dev/stan/yotsuba/feature/home/ReorderableTabRow.kt:73 | 4 magic/wrappers | high | `val widths = remember { mutableStateListOf<Int>() }` is resized by mutating snapshot state *during composition*: `while (widths.size < boards.size) widths.add(0); while (widths.size > boards.size) widths.removeAt(widths.lastIndex)`. The same list is then read in the same composition at lines 87 (`dropTarget(from, dragOffset, widths)`), 116/121 (indicator animation targets) and 135 (`widths[from]` | Confirmed | Open |
| F043 | app/src/main/java/dev/stan/yotsuba/feature/media/MediaScreen.kt:133 | bug | high | MediaFeedViewer already fires the long-press haptic before delegating: `onLongPress = { haptics.longPress(); onLongPressPage(page) }` (MediaFeedViewer.kt:243 for video, :258 for image). MediaScreen's onLongPressPage then fires a second one: `haptics.performHapticFeedback(HapticFeedbackType.LongPress)` (MediaScreen.kt:133). With `behaviour.holdToSave` on, one long-press produces two buzzes. It also | Confirmed | Open |
| F044 | app/src/main/java/dev/stan/yotsuba/feature/media/MediaScreen.kt:212 | 6 canonical layer | high | There are now three verbatim copies of the `NetworkError` → user-facing-string mapping: the canonical `errorPresentation` in the design system (core/designsystem/component/StateViews.kt:147, private), `refreshErrorMessage` added by 0c03f4b (feature/thread/ThreadScreen.kt:509), and `errorMessage` added by 933214b (this line). All six branches are identical string resources; the two feature copies w | Confirmed | Open |
| F045 | app/src/main/java/dev/stan/yotsuba/feature/media/VideoPage.kt:85 | 0 structural simplification | high | `VideoPage` is a single 320-line composable holding: ExoPlayer construction and release, the sound-post companion player, a lifecycle observer, a Player.Listener with six overrides, a 250 ms position poll, seek-hint timing, zoom/double-tap-seek wiring, the buffering/failed overlays and the whole transport bar (play, position text, Slider, duration text, mute). It carries eleven pieces of remembere | Confirmed | Open |
| F046 | app/src/main/java/dev/stan/yotsuba/feature/media/VideoPage.kt:141 | bug | high | The player is created by `remember(videoUri)` (line 113), but the three effects that configure it are keyed only on UI flags: `LaunchedEffect(selected, playing) { player.playWhenReady = ... }` (141), `LaunchedEffect(muted, soundPlayer) { player.volume = ... }` (143-146), `LaunchedEffect(autoAdvance, soundPlayer) { player.repeatMode = ... }` (148-151). `DisposableEffect(player, soundPlayer)` on lin | Confirmed | Open |
| F047 | app/src/main/java/dev/stan/yotsuba/feature/media/ViewerChrome.kt:111 | ui/ux | high | `ViewerTopChrome` lays out one Row: close IconButton, a `Column(Modifier.weight(1f))` with the file name and the '3 / 12 · 1.2 MB · 1920×1080 · subject' line, then `actions()` unbounded (ViewerChrome.kt:77-111). MediaFeedViewer already injects two actions (AutoAdvanceButton, PipButton — MediaFeedViewer.kt:275-276) before the caller's, and the vault viewer adds up to four more (replies, share, dele | Confirmed | Open |
| F048 | app/src/main/java/dev/stan/yotsuba/feature/settings/sections/FiltersSection.kt:78 | 6 canonical layer | high | All filter-list surgery on the domain model is written inline in the composable, in three inconsistent styles within 45 lines. Delete: `update { it.copy(filters = it.filters - filter) }` (line 82, removal by structural equality). Undo re-insert: `val at = index.coerceAtMost(s.filters.size); s.copy(filters = s.filters.toMutableList().apply { add(at, filter) })` (lines 86-87, a stale composition ind | Confirmed | Open |
| F049 | app/src/main/java/dev/stan/yotsuba/feature/settings/sections/FiltersSection.kt:180 | ui/ux | high | `FilterDialog` holds every field in plain `remember`: `var pattern by remember { mutableStateOf(initial.pattern) }` plus isRegex, field, boards, action, enabled, sample (lines 180-186). The dialog's own visibility is likewise `var editing by remember { mutableStateOf<Filter?>(null) }` (line 68). Rotating the phone, or any activity recreation (dark-mode flip, font-size change, process death restore | Confirmed | Open |
| F050 | app/src/main/java/dev/stan/yotsuba/feature/thread/components/PostBody.kt:62 | 0 structural simplification | high | Every post body in the list is wrapped in a `Crossfade(targetState = SpoilerState(revealedSpoilerIds, revealAll), ...)`. PostCard.kt:250-260 calls `PostBody` for every post whose body is non-empty, so a 400-post thread creates up to 400 Crossfades. Each one costs, per item composed during a fling: a `Transition` object and its animation bookkeeping, a `key(targetState)` keyed subcomposition group  | Confirmed | Open |
| F051 | app/src/main/java/dev/stan/yotsuba/feature/thread/components/PostBody.kt:90 | 6 canonical layer | high | The post's `AnnotatedString` is built inside composition: `remember(body, revealedSpoilerIds, revealAll, highlight, quoteLabels, colors, scheme) { ... buildAnnotatedString { body.segments.forEach { ... } } ... highlightMatches(...) }`. Per segment this allocates a `SpanStyle` through up to seven `copy()` calls (`segmentStyle`, :151-182), a `BodyTap`, a `LinkAnnotation.Clickable` with a `TextLinkSt | Rejected (Misreads the code: the build at PostBody.kt:90 is inside remember keyed on body/revealedSpoilerIds/revealAll/highlight/quoteLabels/colors/scheme, so it does not rerun on recomposition with unchanged i) | Deferred (rejected in verification) |
| F052 | app/src/main/java/dev/stan/yotsuba/feature/thread/components/PostCard.kt:117 | ui/ux | high | Three of the card's actions exist only as long-presses with no accessibility equivalent: the post action sheet (`Modifier.pointerInput { detectTapGestures(onLongPress = ...) }`, line 117-119), save-to-vault on the thumbnail (`combinedClickable(onLongClick = actions.onThumbnailLongPress)`, line 225-228) and the quotelink hold in `QuotedByRow` (line 318-321). Compose's `detectTapGestures`/`onLongCli | Confirmed | Open |
| F053 | app/src/main/java/dev/stan/yotsuba/feature/thread/components/PostCard.kt:152 | ui/ux | high | The poster-ID pill draws `color = Color.White` text on `posterIdColor(post.posterId, darkTheme)`, which in light theme is `Color.hsv(hue, 0.35f, 0.85f)` (line 62) — a pastel at 85% value. White on an 85%-value pastel is roughly 1.3:1 contrast, far below the 4.5:1 needed for labelSmall text; the ID is effectively unreadable on every light-theme board with user IDs. The pill is also the filter-by-ID | Confirmed | Open |
| F054 | app/src/main/java/dev/stan/yotsuba/feature/thread/components/PostCard.kt:207 | 4 magic/wrappers | high | There are now three parallel, independent encodings of "which surface is this post card on". (1) `fun actionsFor(post: ThreadPost, inPreview: Boolean)` in ThreadScreen.kt:233, branching at :241 and :251. (2) `PostCardActions.forPreview()` (PostCard.kt:94), applied at ThreadScreen.kt:302 — whose own KDoc at PostCard.kt:73 claims it "is the one place that decides which". (3) `val shared = actions.on | Rejected (Same underlying defect as F172 (shared-element ownership inferred from actions.onThumbnailLongPress at PostCard.kt:207), reported twice. F054's remedy is the worse of the two: it bolts a PostCardSurfa) | Deferred (rejected in verification) |
| F055 | app/src/main/java/dev/stan/yotsuba/feature/thread/ThreadScreen.kt:139 | bug | high | `LaunchedEffect(scrollTarget, state)` is keyed on the entire `state` object, but only needs `scrollTarget`. Every uiState emission — a poll landing, a spoiler reveal, a search keystroke, the highlight timer clearing at ThreadViewModel.kt:422 — cancels and restarts the effect. `onJumpToPost` (ThreadViewModel.kt:415-424) sets `highlightedPostNo` in the same `session.update` that arms the scroll, so  | Confirmed | Open |
| F056 | app/src/main/java/dev/stan/yotsuba/feature/thread/ThreadScreen.kt:164 | bug | high | ``` val refreshError = (state as? UiState.Success)?.data?.refreshError LaunchedEffect(refreshError) {     if (refreshError == null \|\| refreshErrorMessage == null) return@LaunchedEffect     viewModel.onRefreshErrorShown()     snackbar.showSnackbar(refreshErrorMessage) } ``` `onRefreshErrorShown()` (ThreadViewModel.kt:260) clears `session.refreshError`, which flows straight back into `uiState`, so | Confirmed | Open |
| F057 | app/src/main/java/dev/stan/yotsuba/feature/thread/ThreadScreen.kt:233 | 3 design cleanliness | high | `fun actionsFor(post, inPreview)` builds a fresh `PostCardActions` — ten new lambdas — on every call, and `postCard` (line 294) calls it inside the item content for every visible post on every recomposition. `PostCardActions` is a data class whose equality is lambda identity, so the `actions` parameter is never equal between frames and no `PostCard` can ever skip: any state change (a keystroke in  | Confirmed | Open |
| F058 | app/src/main/java/dev/stan/yotsuba/feature/thread/ThreadViewModel.kt:169 | 6 canonical layer | high | `uiState` ends `.stateIn(viewModelScope, ...)` with no `flowOn`, so the whole combine block (:124-167) runs on `viewModelScope`'s Main dispatcher: `searchMatches` over every post's `plainText`, `associateBy` over every post, `filterVerdicts` (the FilterMatcher over every post), a `filter` over every post for `repliesToMe`, `postStates` (a new `PostUiState` per post plus an `eachCount` grouping), ` | Confirmed | Open |
| F059 | app/src/main/java/dev/stan/yotsuba/feature/thread/ThreadViewModel.kt:464 | 6 canonical layer | high | ThreadViewModel.onSaveMedia (L464-483) and MediaViewModel.enqueueSave (feature/media/MediaViewModel.kt L189-214) build the same VaultSaveContext by hand: find the OP (`posts.firstOrNull { it.isOp }`), `threadSubject = op?.subject`, `opExcerpt = op?.body?.plainText?.takeIf { it.isNotBlank() }`, `post = ...`, `conversation = if (saveRepliesWithMedia) PostGraph.of(loaded).conversationAround(post.no)  | Confirmed | Open |
| F060 | app/src/main/java/dev/stan/yotsuba/feature/thread/ThreadViewModel.kt:518 | 2 spaghetti growth | high | ``` fun onVisiblePostsChanged(firstIndex: Int, lastIndex: Int?) {     val details = (result.value as? DataResult.Success)?.value ?: return     val rows = rows(details, session.value, filterVerdicts(details.posts, matcher.value)) ``` This rebuilds the entire row list from scratch on every call. The caller (ThreadScreen.kt:171-175) drives it from `snapshotFlow { listState.firstVisibleItemIndex to li | Confirmed | Open |
| F061 | app/src/main/java/dev/stan/yotsuba/feature/thread/ThreadViewModel.kt:524 | bug | high | 671545c made the row list poster-ID-filterable (`rows()` → `linearRows(visiblePosts(details.posts, session), …)`, ThreadViewModel.kt:574-576, where visiblePosts keeps only the OP and the selected ID) but left the read-mark path reading indices out of that same filtered list: onVisiblePostsChanged (line 518-525) does `rows.postAt(lastIndex)` → `bottomVisiblePostNo` → raiseReadMark (line 532-539) →  | Confirmed | Open |
| F062 | app/src/main/java/dev/stan/yotsuba/feature/vault/VaultDedupSheet.kt:265 | ui/ux | high | The per-group `TextButton(onClick = onApply, ...)` labelled 'Keep selected · N (12 MB)' calls `viewModel.applyGroup(group)` which deletes straight through `vault.delete` (VaultDedupViewModel.kt:118) — permanent, no trash, no undo, no confirmation. The very same screen puts an AlertDialog in front of the bulk version (VaultDedupSheet.kt:163-180), and the grid's delete gets a 30 s undo snackbar (Vau | Confirmed | Open |
| F063 | app/src/main/java/dev/stan/yotsuba/feature/vault/VaultDedupViewModel.kt:107 | bug | high | `applyAllSuggestions() = delete(_state.value.suggestedRemovals)` and `suggestedRemovals: List<String> get() = groups.flatMap { g -> g.redundant.map { it.url } }` (:41) read only the repository's suggested keeper — they ignore the `kept` map that `toggleKept` (:91) writes and that `applyGroup` (:100) honours via `keptIn(group)`. A user who unticks the suggested keeper in a group and ticks a differe | Confirmed | Open |
| F064 | app/src/main/java/dev/stan/yotsuba/feature/vault/VaultExplorer.kt:218 | 5 type/boundary | high | Commit 06914f1 introduced `val view = Triple(state.sort, state.reversed, state.filter)` (VaultExplorer.kt:114) and threads it through four private composables typed as `view: Any` (lines 218 `BoardList`, 256 `ThreadList`, 483 `MediaGrid`), used only as `LaunchedEffect(view) { listState.scrollToItem(0) }`. `Any` erases three distinct enums/booleans into an untyped blob whose only contract is `equal | Rejected (Duplicate of F187, which reports the same `view: Any` / Triple issue at its origin (VaultExplorer.kt:114) with the fuller fix. Real issue, but reported three times; keeping one.) | Deferred (rejected in verification) |
| F065 | app/src/main/java/dev/stan/yotsuba/feature/vault/VaultScreen.kt:174 | bug | high | `ImportPicker.sourcesFrom(context, uris)` (174) and `ImportPicker.sourcesFromTree(context, tree)` (185) run inside the activity-result callbacks, which execute on the main thread. Both do ContentResolver work: `sourcesFrom` issues one `query()` per picked file (ImportPicker.kt:18-21) and `sourcesFromTree` opens a cursor over an entire SAF folder and walks every row (ImportPicker.kt:27-50). Picking | Confirmed | Open |
| F066 | app/src/main/java/dev/stan/yotsuba/feature/vault/VaultScreen.kt:204 | 1 file size | high | VaultScreen.kt is 736 lines and `VaultScreen` itself runs 95→461. The `topBar = { ... }` lambda alone is lines 204-352: a four-way if/else chain (viewer / selecting / searchOpen / default) whose default arm inlines the import Box+IconButton+DropdownMenu (248-276), the sync progress Row (277-294), the sync Box+IconButton+DropdownMenu with four DropdownMenuItems (295-346) and the settings button — a | Confirmed | Open |
| F067 | app/src/main/java/dev/stan/yotsuba/feature/vault/VaultStats.kt:43 | 6 canonical layer | high | `VaultStats.of` re-implements, line for line, the aggregation `VaultViewModel.groupByBoard` already does in the same package. VaultStats.kt:44-47 `entries.groupBy { it.location.board }.map { (board, group) -> BoardStat(board, group.size, group.totalBytes) }` vs VaultViewModel.kt:721-736 `entries.groupBy { it.location.board }...map { VaultBoardSection(board, threads, group) }`. VaultStats.kt:48-52  | Confirmed | Open |
| F068 | app/src/main/java/dev/stan/yotsuba/feature/vault/VaultViewModel.kt:424 | 5 type/boundary | high | `@Suppress("UNCHECKED_CAST")` over a 6-arity `combine(...) { args: Array<Any?> -> ... }`, then six positional casts: `args[0] as List<VaultEntry>`, `args[1] as VaultSyncState`, ... `args[5] as Editing` (lines 425-433). The file already invented three private fold types (`Viewing` 311, `Activity` 316, `Editing` 329) whose stated purpose is "so the outer combine stays within five flows" (line 310) — | Confirmed | Open |
| F069 | app/src/main/java/dev/stan/yotsuba/feature/vault/VaultViewModel.kt:435 | 0 structural simplification | high | Three commits in this slice each bolted another whole-vault list projection onto the same UI state: 7521e0a added `visible`, f022e5a added `recent`, 47517e4 added `results`. On HEAD the combine at VaultViewModel.kt:434-450 materialises all of them on every emission (`visible = arrangeEntries(entries,…)`, `recent = visible.sortedByDescending{savedAt}.take(RECENT_LIMIT).let{ arrangeEntries(it,…) }`  | Confirmed | Open |
| F070 | app/src/main/java/dev/stan/yotsuba/feature/vault/VaultViewModel.kt:541 | 6 canonical layer | high | `mediaVault.entries()` is a cold flow: `savedMediaDao.all().map { rows.filter{...}.map { it.toVaultEntry() } }` (MediaVaultRepositoryImpl.kt:87). The ViewModel collects it three separate times — `uiState` (line 426), `stats` (line 472), `viewerThread` (line 541) — so every DB write runs three Room query subscriptions and three full row→VaultEntry mapping passes over the whole vault. Only two of th | Confirmed | Open |
| F071 | app/src/main/java/dev/stan/yotsuba/feature/vault/VaultViewModel.kt:593 | 0 structural simplification | high | 63650ec split the sync button into two menu items backed by two new methods but left the original whole. On HEAD `sync()` (line 593-606), `rescan()` (608-621) and `fetchReplies()` (623-635) are three copies of the same guard-and-state-machine (`if (syncState.value.running) return`, set `VaultSyncState(running = true)`, do work, reset, call back). `sync()` has no caller in app/src/main or app/src/a | Confirmed | Open |
| F072 | app/src/main/java/dev/stan/yotsuba/navigation/AppNavHost.kt:105 | 6 canonical layer | high | Commit 9519b06 ("Open thread and catalog links from browsers, shares and the widget") added the canonical intent path: `ExternalLinks.fromIntent` -> `ShellViewModel.pendingLink` -> the LaunchedEffect at AppNavHost.kt:97-104. It left the widget's own path (b446eb3) untouched, so the same concern now has two mechanisms side by side: lines 105-110 collect `WidgetDeepLink.pending`, a process-wide muta | Confirmed | Open |
| F073 | app/src/main/java/dev/stan/yotsuba/navigation/AppNavHost.kt:137 | 2 spaghetti growth | high | The nav-item loop is written twice, byte-for-byte apart from the `Rail`/`Bar` prefix: lines 116-124 (`NavigationRail`) and lines 137-145 (`NavigationBar`) both do `for (dest in TopLevelDestination.entries) { val selected = isSelected(dest); …Item(selected, onClick = { navigateTopLevel(dest) }, icon = { Icon(if (selected) dest.selectedIcon else dest.unselectedIcon, contentDescription = null) }, lab | Confirmed | Open |
| F074 | app/src/main/java/dev/stan/yotsuba/navigation/AppNavHost.kt:165 | 6 canonical layer | high | MotionSpecs.kt states its contract at lines 23-27: "Everything here collapses to its end state under reduced motion, so a call site never checks that itself", and provides `motionEnter()`/`motionExit()`/`rememberMotionSpec()` that do the check internally (MotionSpecs.kt:32, 62, 70). Commit e94cf4c ignored that and inlined the check four times at the call site: AppNavHost.kt:151 `val reduced = reme | Confirmed | Open |
| F075 | app/src/test/java/dev/stan/yotsuba/data/MediaDownloadQueueTest.kt:33 | 5 type/boundary | high | The file's own KDoc (lines 27-31) states the problem: "The queue owns a real IO-dispatcher worker (its scope is not injectable), so these tests run against real time". `MediaDownloadQueue` (data/repository/MediaDownloadQueue.kt:33) hard-codes `CoroutineScope(SupervisorJob() + Dispatchers.IO)`. The cost is spread across four test classes: this file uses `runBlocking` plus `withTimeout(5_000)` polli | Confirmed | Open |
| F076 | app/src/test/java/dev/stan/yotsuba/data/MediaDownloadQueueTest.kt:43 | 6 canonical layer | high | `MediaVaultRepository` (14 members) is stubbed by hand nine separate times across the slice: MediaDownloadQueueTest.kt:43 `GatedVault`, DedupQueueTest.kt:34 `RecordingVault`, VaultDedupViewModelTest.kt:60 `DeletingVault`, BookmarksViewModelTest.kt:86 `FakeVault`, MediaViewModelTest.kt:101 `FakeVault`, ThreadViewModelTest.kt:139 `FakeVault`, VaultViewModelTest.kt:71 `FakeVault`, ThreadQuoteTapTest. | Confirmed | Open |
| F077 | app/src/test/java/dev/stan/yotsuba/feature/thread/ThreadRefreshFailureTest.kt:56 | 6 canonical layer | high | `diff <(sed -n '56,125p' ThreadQuoteTapTest.kt) <(sed -n '56,122p' ThreadRefreshFailureTest.kt)` reports only three differences: a `@After` line, the two-line settings fake body, and one extra `previewNos` helper. Roughly 65 lines of `ThreadViewModel` wiring — anonymous `ThreadRepository`, `MediaVaultRepository`, `BoardRepository`, `BookmarkRepository`, `HistoryRepository`, `SettingsRepository`, ` | Confirmed | Open |
| F078 | app/schemas/dev.stan.yotsuba.core.database.YotsubaDatabase/6.json:1 | 5 type/boundary | medium | `exportSchema = true` (YotsubaDatabase.kt:124) and `room.schemaLocation` are set, but only 6..10.json exist while Migrations.kt defines MIGRATION_1_2 through MIGRATION_5_6 and Modules.kt:127 wires all nine. No test references `MigrationTestHelper` (grep of test/ and androidTest/ is empty). The exported schemas therefore guard nothing, and migrations 1->6 can never be verified against a real schema | Rejected (Facts check out (schemas 6-10 only, migrations 1_2..9_10, no MigrationTestHelper anywhere), but this is a test-coverage/process gap, not something the thermo-nuclear rules flag: no structural regressi) | Deferred (rejected in verification) |
| F079 | app/src/androidTest/java/dev/stan/yotsuba/BoardsFavouriteFlowTest.kt:63 | bug | medium | boardsSearch_filtersAndRestores claims to check restoration, but line 62 clears the field and line 63 immediately types "Tech" — which matches TestSeed.BOARD_TITLE = "Technology". The closing assertion at lines 64-65 (openBoardsTab(), then waitForText(BOARD_TITLE)) therefore passes whether or not the tab re-tap restores anything, and passes even if the query were never cleared. The `openBoardsTab( | Confirmed | Open |
| F080 | app/src/androidTest/java/dev/stan/yotsuba/di/TestRepositoryModule.kt:1 | 0 structural simplification | medium | One 401-line file carries three unrelated concerns: the TestSeed fixture object (lines 52-155, ~100 lines of domain-model literals), eleven fake repository implementations (lines 157-377, ~220 lines), and the actual Hilt wiring (lines 379-401, ~20 lines). It is 70% of the whole androidTest source set by line count, and the file's name advertises only the last of the three. Import order is already  | Rejected (The file is 401 lines, nowhere near the 1k-line threshold the rules flag, and its three parts (seed data, fakes, bindings) are one cohesive concern: the instrumented-test double graph. The proposed fi) | Deferred (rejected in verification) |
| F081 | app/src/androidTest/java/dev/stan/yotsuba/di/TestRepositoryModule.kt:324 | 6 canonical layer | medium | FakeSettingsRepository here (a MutableStateFlow<Settings> plus an update lambda) is a line-for-line reimplementation of app/src/test/java/dev/stan/yotsuba/fake/FakeSettings.kt, whose own KDoc says "Several ViewModel tests declare their own private copy of this; new tests should use this one." The androidTest source set is now the eighth copy. app/build.gradle.kts declares no shared test source set | Rejected (Line 324 is indeed FakeSettingsRepository, and it does overlap with app/src/test/.../fake/FakeSettings.kt — but the duplicate is 7 trivial lines (a MutableStateFlow plus an update lambda), and the two) | Deferred (rejected in verification) |
| F082 | app/src/androidTest/java/dev/stan/yotsuba/di/TestRepositoryModule.kt:370 | 4 magic/wrappers | medium | `val error = kotlinx.coroutines.runBlocking { vault.save(item, context) }` inside MediaSaveQueue.enqueue, written fully qualified so it dodges the import list. enqueue is a non-suspend function that Compose calls on the main thread during MediaSaveFlowTest, so this blocks the UI thread on a coroutine builder purely to reach a `suspend fun` whose body (lines 291-306) never actually suspends — it on | Confirmed | Open |
| F083 | app/src/androidTest/java/dev/stan/yotsuba/FlowTestHelpers.kt:26 | 4 magic/wrappers | medium | clickText hardcodes `substring = true, ignoreCase = true` with no way to opt out, and waitForContentDescription (line 19-24) does the same, while waitForText (line 12) does expose a `substring` parameter. That missing parameter has already spawned two workarounds: openBoardsTab (lines 31-33) is a one-off helper that exists solely to do an exact-match click, with a comment explaining that "Pick boa | Confirmed | Open |
| F084 | app/src/androidTest/java/dev/stan/yotsuba/HistoryClearFlowTest.kt:30 | 2 spaghetti growth | medium | Lines 30-36 are a verbatim copy of HistoryFlowTest.kt:29-39: openSeededThread(); click Back; waitForText("Threads"); clickText("Threads"); waitForText("Recent"); clickText("Recent"); waitForText(THREAD_SUBJECT). BookmarksFlowTest.kt:37-41 repeats the same Back-then-Threads-tab prefix for the "Watched" segment. Three copies of one navigation path, each with its own comment restating what the segmen | Confirmed | Open |
| F085 | app/src/androidTest/java/dev/stan/yotsuba/VaultFlowTest.kt:33 | 5 type/boundary | medium | The only assertion is a disjunction — waitUntil { "Saved media" present \|\| "Vault is empty" present } — hedged by a comment saying "either ... or ... must be on screen". The fakes make the outcome fully deterministic: FakeMediaVaultRepository.hasStorageAccess() returns true (line 284) and entries() starts as an empty MutableStateFlow (line 282), so exactly one of those two states can occur. The  | Confirmed | Open |
| F086 | app/src/main/java/dev/stan/yotsuba/core/database/dao/BookmarkDao.kt:60 | 3 design cleanliness | medium | `updateRefresh` (:43-57), `updateCounts` (:60-73) and `updateState` (:75-76) are three UPDATE statements over the same row where each is a strict column-subset of the previous: updateCounts is updateRefresh minus `postNos`, updateState is updateCounts minus counts and activity. That forces the caller to fan out — BookmarkRepositoryImpl.kt has four call sites (:53, :99, :104, :117, :125) picking be | Rejected (The three UPDATEs are real and overlapping, but the proposed collapse does not delete complexity: it replaces three explicit, self-documenting statements with one statement carrying three nullable par) | Deferred (rejected in verification) |
| F087 | app/src/main/java/dev/stan/yotsuba/core/database/dao/SavedMediaDao.kt:41 | 3 design cleanliness | medium | `byMd5` filters on a non-indexed column: `saved_media` declares no indices at all (confirmed in app/schemas/.../10.json — every entity has `"indices": []`), so every lookup is a full table scan. It is on the hot save path: MediaDownloadQueue.kt:55 calls `dedup.findByMd5(...)` for each queued item, i.e. once per media file saved, against a table sized to the whole vault. | Confirmed | Open |
| F088 | app/src/main/java/dev/stan/yotsuba/core/database/dao/SavedMediaDao.kt:47 | 6 canonical layer | medium | `ext NOT IN ('.webm', '.mp4')` hardcodes the video-extension set inside SQL. The same set is spelled out five more times in Kotlin: VaultEntry.kt:48, MediaItem.kt:31, VaultDedupRepositoryImpl.kt:74, MediaVaultRepositoryImpl.kt:237 and :575. There is no canonical constant, and the SQL copy is the one the compiler cannot help with — adding a video extension in Kotlin leaves this query silently queue | Confirmed | Open |
| F089 | app/src/main/java/dev/stan/yotsuba/core/database/dao/SavedMediaDao.kt:49 | 0 structural simplification | medium | `missingHashes(): List<SavedMediaEntity>` is the only way to ask how much backfill work remains, so VaultDedupRepositoryImpl.kt:34 implements `missingHashCount()` as `dao.missingHashes().size` — it materialises every unhashed row (18 columns each, including absolutePath and subject strings) into a list purely to read its size. The vault is designed to hold thousands of files, and this count is wha | Confirmed | Open |
| F090 | app/src/main/java/dev/stan/yotsuba/core/database/entity/Entities.kt:20 | 0 structural simplification | medium | `newReplies` and `unreadCount` are documented at :19 and :21 as "Superseded by readUpTo/postNos; no longer written, dropped in a later migration" — that later migration never came; both columns are still in the v10 schema (app/schemas/.../10.json bookmarks fields). No production code writes them (grep finds writes only in androidTest/di/TestRepositoryModule.kt:205, :214). Their domain twins in Boo | Confirmed | Open |
| F091 | app/src/main/java/dev/stan/yotsuba/core/database/Migrations.kt:6 | 3 design cleanliness | medium | Nine hand-written migrations, and nothing tests any of them: `grep -rln "MigrationTestHelper\|MIGRATION_" app/src/androidTest app/src/test` returns nothing. Worse, the exported schemas needed to test them have been deleted — app/schemas/dev.stan.yotsuba.core.database.YotsubaDatabase/ contains only 6.json through 10.json, so MigrationTestHelper cannot even construct a v1..v5 database. A single mism | Rejected (The facts check out (no MigrationTestHelper anywhere, only 6..10.json exported, no destructive fallback at Modules.kt:126-128), but this is a missing-test-coverage gap, not one of the structural/abstr) | Deferred (rejected in verification) |
| F092 | app/src/main/java/dev/stan/yotsuba/core/datastore/SettingsDataStore.kt:35 | 6 canonical layer | medium | `private val blobKey = stringPreferencesKey("settings")` is the canonical name of the settings blob, but it is private, so BackupRepositoryImpl.kt:126 redeclares the same literal as `val SETTINGS_BLOB = stringPreferencesKey("settings")` and reads the raw DataStore at :120 to decide whether the install is fresh. Two independent string literals name one key across two layers; renaming the key in one | Confirmed | Open |
| F093 | app/src/main/java/dev/stan/yotsuba/core/designsystem/component/SettingsRows.kt:42 | ui/ux | medium | `SwitchRow` puts `.clickable(enabled = enabled) { onToggle(!checked) }` on the `Row` (line 42) and simultaneously hands the `Switch` a live `onCheckedChange = onToggle` (line 51). The result is two independent, overlapping accessibility nodes for one setting: TalkBack announces a generic clickable row carrying the title text, and then a separate Switch node carrying the on/off state, with no assoc | Rejected (Same defect, same lines, same fix as F017 (which additionally covers TextRow/NavigationRow roles). Duplicate, not an independent finding.) | Deferred (rejected in verification) |
| F094 | app/src/main/java/dev/stan/yotsuba/core/designsystem/component/SettingsRows.kt:107 | ui/ux | medium | `ChipRow` threads `enabled` into the `FilterChip`s (line 113) but its label `Text(label, style = MaterialTheme.typography.bodyLarge)` at line 107 keeps full `onSurface` colour. `MediaSection.kt:50-57` uses this with `enabled = settings.doubleTapSeekEnabled`, so with double-tap seek off the "Seek step" heading looks live while the chips beneath it silently refuse taps. `SwitchRow` has the same gap: | Confirmed | Open |
| F095 | app/src/main/java/dev/stan/yotsuba/core/designsystem/component/SharedMedia.kt:42 | 4 magic/wrappers | medium | The four-argument `sharedMedia` overload is public API justified by a KDoc that is factually wrong: "Split out so the no-op contract can be checked on the JVM without composing anything." The function is annotated `@Composable` (line 47) and cannot be called without a composition; SharedMediaModifierTest.kt:44-49 duly spins up a Robolectric `ComponentActivity` and `setContent` to call it. No produ | Confirmed | Open |
| F096 | app/src/main/java/dev/stan/yotsuba/core/designsystem/component/ThreadSummaryRow.kt:37 | 5 type/boundary | medium | `Row(Modifier.padding(spacing.md).then(modifier), ...)` applies the caller's modifier *after* the component's own padding, inverting the Compose convention that the caller's modifier comes first. Any caller passing `Modifier.clickable {}` would get a click region inset by 12dp on every side; any caller passing a background would get one that stops short of the padding. It is currently latent only  | Confirmed | Open |
| F097 | app/src/main/java/dev/stan/yotsuba/core/designsystem/ReducedMotion.kt:24 | 4 magic/wrappers | medium | Commit 676971c added `fun isReducedMotion(context: Context, reduceMotionSetting: Boolean): Boolean = reduceMotionSetting \|\| isReducedMotion(context)` (lines 23-25). `rg isReducedMotion` shows the only callers of the two-arg overload are ReducedMotionTest.kt:35-36; production reads the setting through `LocalReduceMotion` in `rememberReducedMotion()` (line 32) instead. It is a two-line `\|\|` wrap | Confirmed | Open |
| F098 | app/src/main/java/dev/stan/yotsuba/core/designsystem/token/Elevation.kt:9 | 3 design cleanliness | medium | `Elevation`/`LocalElevation` is provided in Theme.kt:105 but never read: `rg -n 'LocalElevation\|level0\|level1\|level3' app/src/main -g '*.kt'` returns only the token file and the `provides` line. Same for `Spacing.xxl`: `rg -o 'spacing\.(xs\|sm\|md\|lg\|xl\|xxl)\b' app/src/main` counts xs 29, sm 58, md 40, lg 37, xl 2, xxl 0. | Confirmed | Open |
| F099 | app/src/main/java/dev/stan/yotsuba/core/network/ArchiveHosts.kt:33 | 5 type/boundary | medium | One concept (an archive) is spread over three parallel tables that must be kept in sync by hand: `boards` (14), `webBase` (25) and the `when` in apiUrl (46-50). `sourceFor` reads `boards.getValue(it)` for every ArchiveSource entry, so the moment someone adds a fourth entry to ArchiveSource (ThreadPost.kt:43) without also editing this map, *every* board lookup throws NoSuchElementException — and so | Confirmed | Open |
| F100 | app/src/main/java/dev/stan/yotsuba/core/update/Updater.kt:92 | 2 spaghetti growth | medium | Three copies of the same install-and-report block. downloadAndInstall:92-97 sets Installing, calls install(apk, allowSilent = true), catches Exception -> Failed("Install failed: ${e.message ?: "unknown error"}"); retryWithConfirmation:109-114 is the identical block with allowSilent = false wrapped in scope.launch; and the receiver's else branch (202-210) is a third place that decides between retry | Confirmed | Open |
| F101 | app/src/main/java/dev/stan/yotsuba/core/util/Urls.kt:33 | 5 type/boundary | medium | `val search = uri.fragment?.removePrefix("s=")?.replace('_', ' ')` treats *any* fragment on a `/<board>/catalog` URL as a search query. `removePrefix` is a no-op when the prefix is absent, so `https://boards.4chan.org/g/catalog#top` returns `InternalLink.Catalog("g", "top")` and opens the catalog pre-filtered to the nonsense query "top". The `s=` shape is the only documented search form (D26), and | Confirmed | Open |
| F102 | app/src/main/java/dev/stan/yotsuba/core/util/Urls.kt:43 | 6 canonical layer | medium | The protocol-relative fixup `if (x.startsWith("//")) "https:$x" else x` is written out four separate times: Urls.kt:23 (parseInternal), Urls.kt:43 (domainOf), core/text/PostHtmlParser.kt:136 (resolveUrl), core/media/SoundPost.kt:45. Three of those then build a `java.net.URI` from the result and two wrap it in `runCatching`. There is no canonical home, so any future rule (e.g. also accepting bare ` | Confirmed | Open |
| F103 | app/src/main/java/dev/stan/yotsuba/core/vault/VaultMeta.kt:67 | 0 structural simplification | medium | `VaultMetaCodec` (VaultMeta.kt:67-78) and `VaultPostsCodec` (VaultPosts.kt:124-135) are byte-for-byte the same object: the same `Json { ignoreUnknownKeys = true; prettyPrint = true; encodeDefaults = false }` configuration, the same `encode` and the same `decode` returning null via `runCatching`. Two Json instances are also built at class-init for what is one sidecar format policy, and a drift in o | Confirmed | Open |
| F104 | app/src/main/java/dev/stan/yotsuba/core/vault/VaultPosts.kt:66 | 6 canonical layer | medium | Package dependencies run both ways: `core/vault/VaultPosts.kt:82-84` imports `domain.model.PostMedia`/`ThreadPost` (and spells `dev.stan.yotsuba.domain.model.MediaItem` inline at line 187), `core/network/ArchiveHosts.kt:215` imports `domain.model.ArchiveSource`, while `domain/model/VaultEntry.kt:3` imports `core.vault.VaultPaths`, `domain/model/ThreadPost.kt:3` imports `core.text.PostText`, and ev | Confirmed | Open |
| F105 | app/src/main/java/dev/stan/yotsuba/core/vault/VideoStills.kt:33 | 4 magic/wrappers | medium | Line 33 reads `?: return Still(still.takeIf { it.isFile } ?: return null, duration)` — a return inside the argument of a return. It sits inside `if (!still.isFile)` (line 31), so `still.takeIf { it.isFile }` is null by construction and the outer `Still(...)` is unreachable: the expression can only ever evaluate to `return null`. Three control-flow constructs and a dead branch encode one outcome. | Confirmed | Open |
| F106 | app/src/main/java/dev/stan/yotsuba/core/work/VaultSyncWorker.kt:55 | 7 orchestration/atomicity | medium | On a metered connection with dataSaver on, the worker returns `Result.retry()`. This is a *periodic* worker (6h, INTERVAL_HOURS): retry schedules a backed-off re-run of the same period, so a phone on mobile data all day wakes the worker repeatedly (30s, 1m, 2m ... up to 5h) only to hit the same check and bail again. Nothing about the outcome changes between those wakeups — the setting and the netw | Confirmed | Open |
| F107 | app/src/main/java/dev/stan/yotsuba/core/work/VaultSyncWorker.kt:64 | 7 orchestration/atomicity | medium | `snapshotted = live.toSet()` marks every live bookmark as done, but snapshotThreads did not necessarily touch them all: MediaVaultRepositoryImpl.snapshotThreads:422 first drops non-remote targets (`targets.filter { it.isRemote }`), and runPass:456-476 counts per-thread outcomes as updated/gone/failed — a write failure or a network error increments `failed` and the pass continues. Those threads are | Confirmed | Open |
| F108 | app/src/main/java/dev/stan/yotsuba/data/repository/BackupRepositoryImpl.kt:77 | 4 magic/wrappers | medium | The write-to-temp-then-rename-with-fallback idiom is implemented twice, character for character in intent: BackupRepositoryImpl.kt:77-82 (`val tmp = File(file.parentFile, file.name + ".tmp"); tmp.writeText(...); if (!tmp.renameTo(file)) { file.writeText(tmp.readText()); tmp.delete() }`) and VaultStore.kt:168-175 (`writeSidecar`, same shape, writing `text` directly on the fallback path). Both write | Confirmed | Open |
| F109 | app/src/main/java/dev/stan/yotsuba/data/repository/BookmarkRepositoryImpl.kt:117 | 4 magic/wrappers | medium | The same eight-argument persistence call is copy-pasted twice — `dao.updateRefresh(refreshed.board, refreshed.threadNo, refreshed.replyCount, refreshed.imageCount, refreshed.state.name, refreshed.lastCheckedAt, refreshed.lastActivityAt, encodePostNos(refreshed.postNos))` at lines 53-57 and the identical shape for `fresh` at 117-120. The `dao.updateCounts(...)` + `mark.copy(replyCount = …, imageCou | Confirmed | Open |
| F110 | app/src/main/java/dev/stan/yotsuba/data/repository/HistoryRepositoryImpl.kt:64 | 4 magic/wrappers | medium | f9d8d51 added two private pass-through mappers — `HistoryEntity.toDomainWithReadMark() = toDomain().copy(maxReadPostNo = maxReadPostNo)` and `HistoryEntry.toFullEntity() = toEntity().copy(maxReadPostNo = maxReadPostNo)` — because the canonical mappers in Mappers.kt:240/250 deliberately drop the read mark. But `HistoryEntity.toDomain()` has exactly one caller in the whole app (this file, through th | Confirmed | Open |
| F111 | app/src/main/java/dev/stan/yotsuba/data/repository/Mappers.kt:164 | 6 canonical layer | medium | `archiveCommentToHtml` *generates* 4chan HTML (escaping, `<br>` joining, `<a class="quotelink">` and `<span class="quote">` emission) and lives in the data-layer DTO mapper file, while its exact counterpart `PostHtmlParser` — which consumes that markup and owns the class names it must match — lives in `core/text`. The two must agree on `quotelink` and `quote` and can drift silently. It also compil | Confirmed | Open |
| F112 | app/src/main/java/dev/stan/yotsuba/data/repository/MediaVaultRepositoryImpl.kt:164 | bug | medium | `delete(url)` removes the media file and its meta entry but never touches its captured still: `file.delete()` at line 164, then `store.updateMeta(dir) { it.remove(file.name) }`. The still lives at `.thumbs/<file name>.jpg` (VideoStills.kt:18-19) and is left behind. `pruneIfEmpty` treats `.thumbs` as a sidecar (VaultStore.kt:180) so it only disappears when the whole thread dir goes; a thread with o | Confirmed | Open |
| F113 | app/src/main/java/dev/stan/yotsuba/data/repository/MediaVaultRepositoryImpl.kt:233 | 6 canonical layer | medium | `exportToGallery` is 53 lines of pure Android MediaStore plumbing — API-level branch, `ContentValues`, pending-flag write, `resolver.delete` rollback, pre-29 `MediaScannerConnection` fallback — living in the vault repository. It touches no vault concept beyond looking up one row to get a path: `savedMediaDao.byUrl(url)` then `File(entity.absolutePath)`. Neighbouring media concerns already live els | Confirmed | Open |
| F114 | app/src/main/java/dev/stan/yotsuba/data/repository/MediaVaultRepositoryImpl.kt:237 | 6 canonical layer | medium | The "is this a video" predicate exists in four places with no canonical owner: `entity.ext == ".webm" \|\| entity.ext == ".mp4"` inline at MediaVaultRepositoryImpl.kt:237, `private fun isVideo(ext: String?)` at MediaVaultRepositoryImpl.kt:575, `private fun isVideo(row: SavedMediaEntity)` at VaultDedupRepositoryImpl.kt:74, and hardcoded again as SQL at SavedMediaDao.kt:47 (`ext NOT IN ('.webm', '.m | Confirmed | Open |
| F115 | app/src/main/java/dev/stan/yotsuba/data/repository/MediaVaultRepositoryImpl.kt:238 | 6 canonical layer | medium | exportToGallery re-declares the extension -> MIME table (L238-246: .jpg/.jpeg/.png/.gif/.webp/.webm/.mp4/else octet-stream) that already exists as `mimeOf(ext)` in feature/media/MediaShare.kt L26-34. The data layer copied a feature helper rather than the helper living somewhere both can reach. | Confirmed | Open |
| F116 | app/src/main/java/dev/stan/yotsuba/data/repository/MediaVaultRepositoryImpl.kt:238 | 6 canonical layer | medium | 1b5e650's exportToGallery re-types the extension→MIME table inline at lines 238-246 (`".jpg", ".jpeg" -> "image/jpeg"` … `".mp4" -> "video/mp4"`), byte-identical to the canonical `mimeOf` that 84b787a had just consolidated in feature/media/MediaShare.kt:26-35 — which the vault's own share path uses (VaultShare.kt calls `mimeOf`). The same commit also spelled the video test inline at line 237 (`ent | Rejected (Duplicate: it restates F115 (mime table copied from MediaShare.mimeOf) and F114 (isVideo predicate in four places) as one finding at an adjacent line (239 vs the actual when at 238), adding no distinc) | Deferred (rejected in verification) |
| F117 | app/src/main/java/dev/stan/yotsuba/data/repository/MediaVaultRepositoryImpl.kt:490 | 0 structural simplification | medium | `savedThreads()` returns `List<SavedThreadDir>` carrying `(dir, board, threadNo)`, and `SavedThreadDir` (line 499) exists solely for it. Its only caller, line 384-385, immediately discards `dir`: `savedThreads().filterNot { VaultLocation(it.board, it.threadNo) in skip }` then `targets.map { VaultLocation(it.board, it.threadNo) }`. The pass body then re-derives the very directory that was thrown aw | Confirmed | Open |
| F118 | app/src/main/java/dev/stan/yotsuba/data/repository/SavedMediaMappers.kt:98 | 5 type/boundary | medium | `location = if (board != null && board != VaultPaths.UNSORTED_DIR_NAME && threadNo != null) VaultLocation(board = board!!, threadNo = threadNo!!) else VaultLocation.Unsorted` -- two `!!` on smart-castable vals plus a hand-rolled copy of `VaultLocation.isUnsorted` (VaultEntry.kt:14). SavedMediaEntity.board/threadNo are nullable (Entities.kt:66-67) only for the `_unsorted` and url-only legacy rows ( | Confirmed | Open |
| F119 | app/src/main/java/dev/stan/yotsuba/data/repository/VaultDedupRepositoryImpl.kt:34 | 4 magic/wrappers | medium | `override suspend fun missingHashCount(): Int = withContext(Dispatchers.IO) { dao.missingHashes().size }`. `missingHashes()` is `SELECT * FROM saved_media WHERE ...` (SavedMediaDao.kt), so every row of the vault index — including `absolutePath`, `displayName`, `subject`, `thumbnailPath` — is materialised into `SavedMediaEntity` objects only to call `.size`. VaultDedupViewModel.kt:58 calls it on ev | Confirmed | Open |
| F120 | app/src/main/java/dev/stan/yotsuba/domain/model/Bookmark.kt:27 | 3 design cleanliness | medium | `@Deprecated("Derived: see unread") val newReplies: Int = 0` (l.27) and `@Deprecated("Derived: see unread") val unreadCount: Int = 0` (l.28) are write-only ghosts in the domain model. `BookmarkEntity.toDomain()` (data/repository/Mappers.kt:199-215) does not set either one and `Bookmark.toEntity()` (Mappers.kt:217-233) does not read either one, so on every real path both are the constructor default | Confirmed | Open |
| F121 | app/src/main/java/dev/stan/yotsuba/domain/model/Bookmark.kt:37 | 6 canonical layer | medium | The "subject, else a trimmed excerpt, else a synthetic label" rule is written out three times in three models: Bookmark.kt:37-38 `subject ?: opExcerpt.take(60).ifBlank { "/$board/$threadNo" }`, HistoryEntry.kt:14-15 (character-for-character identical), and CatalogThread.kt:17-18 `subject ?: excerpt.plainText.take(60).ifBlank { "#$no" }`. app/src/test/java/dev/stan/yotsuba/data/DisplayTitleTest.kt  | Confirmed | Open |
| F122 | app/src/main/java/dev/stan/yotsuba/domain/model/Filter.kt:28 | 4 magic/wrappers | medium | `val error: String? get() = if (isRegex) runCatching { Regex(pattern) }.exceptionOrNull()?.message else null` (l.28-29) compiles a regular expression, and constructs and discards an exception object, on every single read of what looks like a plain data-class property. It is read from Compose: feature/settings/sections/FiltersSection.kt:158 evaluates `filter.error?.let { ... }` once per filter row  | Confirmed | Open |
| F123 | app/src/main/java/dev/stan/yotsuba/domain/model/VaultEntry.kt:48 | 6 canonical layer | medium | "Is this extension a video" is hand-written five times across three layers with no shared definition: VaultEntry.kt:48 `val isVideo: Boolean get() = ext == ".webm" \|\| ext == ".mp4"`, MediaItem.kt:31 `val isVideo: Boolean get() = ext == ".webm" \|\| ext == ".mp4"`, data/repository/VaultDedupRepositoryImpl.kt:74 `private fun isVideo(row: SavedMediaEntity) = row.ext == ".webm" \|\| row.ext == ".mp4 | Confirmed | Open |
| F124 | app/src/main/java/dev/stan/yotsuba/domain/repository/BookmarkRepository.kt:3 | 3 design cleanliness | medium | e6b89d0 split Repositories.kt and Daos.kt one declaration per file by copying the original file's entire import block into every extracted file. Checked mechanically over the 12 files it produced: BookmarkRepository.kt has 13 unused imports (DataResult, Board, CatalogThread, HiddenThread, HistoryEntry, MediaItem, ImportSource, Settings, ThreadDetails, VaultSyncSummary, VaultEntry, VaultError, Vaul | Rejected (Duplicate of F030 at the same file and line, and less accurate (it counts MaintenanceRepository at 14 unused imports; the file has 15 imports and uses none). Its only extra content, the DAO files, is ) | Deferred (rejected in verification) |
| F125 | app/src/main/java/dev/stan/yotsuba/domain/repository/BookmarkRepository.kt:51 | 3 design cleanliness | medium | `@Deprecated("Unread is derived from readUpTo; use markSeen") suspend fun updateUnread(board: String, threadNo: Long, unread: Int)` (l.51-52) is an abstract member with no default body, and the one production implementation is `override suspend fun updateUnread(...) = Unit` (data/repository/BookmarkRepositoryImpl.kt:159). Nothing in main sources calls it. Because it is abstract, all seven JVM test | Confirmed | Open |
| F126 | app/src/main/java/dev/stan/yotsuba/domain/repository/MediaVaultRepository.kt:23 | 5 type/boundary | medium | 2249904 added `val storageAccess: Flow<Boolean> get() = flow { emit(hasStorageAccess()) }` and `fun refreshStorageAccess() {}` as interface defaults so existing fakes kept compiling. The contract now lies: an implementation that forgets to override gets a one-shot flow that never updates and a refresh that does nothing, with no compiler complaint. The same commit's sibling 44c137a added defaulted  | Rejected (The issue is real but it is a strict subset of F033, which flags the same two defaults at the same lines with the same remedy and covers the other nine besides. Reporting it separately splits one chan) | Deferred (rejected in verification) |
| F127 | app/src/main/java/dev/stan/yotsuba/domain/repository/MediaVaultRepository.kt:47 | 5 type/boundary | medium | Eleven of the interface's members have silent-fallback defaults: `trash = delete(url)` (47), `restoreTrashed = NotFound` (50), `purgeTrash {}` (53), `exportToGallery = Io("not supported")` (59), `snapshotThread = Io("not supported")` (99), `snapshotThreads = VaultSyncSummary()` (105-108), `renameThread`/`mergeThreads = Io("not supported")` (114-122), `storageAccess = flow { emit(hasStorageAccess() | Rejected (Duplicate of F033: identical member list, identical evidence, identical remedy (abstract members plus a shared FakeMediaVaultRepository base), plus F034's overload collapse. Nothing here is not alread) | Deferred (rejected in verification) |
| F128 | app/src/main/java/dev/stan/yotsuba/domain/repository/VaultDedupRepository.kt:31 | 4 magic/wrappers | medium | `VaultDedupRepository.None` exists only to be the default for `MediaDownloadQueue(dedup = VaultDedupRepository.None)` (MediaDownloadQueue.kt:31). Hilt always injects the real binding (DedupModule), and `rg -n 'VaultDedupRepository\.None' app/src -g '*.kt'` finds no other use; the tests that omit `dedup` (MediaDownloadQueueTest, MediaViewModelTest, ThreadViewModelTest, ThreadRefreshFailureTest, Thr | Confirmed | Open |
| F129 | app/src/main/java/dev/stan/yotsuba/feature/boards/BoardsScreen.kt:115 | ui/ux | medium | Tapping the star on a favourited board calls `viewModel.onToggleFavourite` immediately with no confirmation and no undo, and the row then vanishes from the Favourites section (the list is derived from `settings.favouriteBoards`, BoardsViewModel.kt:64). Removing the same favourite from Home offers an undo snackbar (HomeScreen.kt:131-135 via `showUndo`), and even hiding a single catalog thread is un | Confirmed | Open |
| F130 | app/src/main/java/dev/stan/yotsuba/feature/boards/BoardsViewModel.kt:92 | 2 spaghetti growth | medium | c65bad2 inserted a special case in the middle of `onToggleBoardVisible`: when the board's category is hidden, it un-hides the category and instead hides every sibling (`hiddenBoards = s.hiddenBoards - board + siblings`). This is a symptom of visibility being stored in two overlapping sets (`hiddenBoards` and `hiddenCategories`) whose overlap has no defined meaning — which is also why `isVisible`,  | Confirmed | Open |
| F131 | app/src/main/java/dev/stan/yotsuba/feature/boards/BoardsViewModel.kt:130 | ui/ux | medium | `search()` ranks results 0..3 (exact code, code prefix, code contains, title contains) and its KDoc claims this "puts /g/ first instead of every title with a g in it" — but the ranked list is immediately regrouped by category at line 48 (`BoardCategory.entries.mapNotNull { cat -> visible.filter { it.category == cat } }`) and rendered section by section in enum order (BoardsScreen.kt:129). The rank | Confirmed | Open |
| F132 | app/src/main/java/dev/stan/yotsuba/feature/bookmarks/BookmarksList.kt:166 | 2 spaghetti growth | medium | The remove-with-undo block is written twice in the same composable: lines 138-142 (swipe path) and lines 166-172 (sheet path) both do `viewModel.onRemove(bookmark); scope.launch { snackbar.showUndo(removedMessage, undoLabel) { viewModel.onUndoRemove(bookmark) } }`. The identical five-line shape is copied a third time into HistoryList.kt:100-104 with different strings — todo.md:33 extracted `SwipeT | Confirmed | Open |
| F133 | app/src/main/java/dev/stan/yotsuba/feature/bookmarks/BookmarksViewModel.kt:132 | 0 structural simplification | medium | `override fun onCleared() { refreshJob?.cancel() }` — `refreshJob` is launched in `viewModelScope` (line 86), which `ViewModel` cancels itself on clear, so the body is a no-op. The override also drops `super.onCleared()`, which is the kind of omission that becomes a leak the day a base class or a lifecycle release starts doing work there. The field itself is still needed for the re-entrancy guard  | Confirmed | Open |
| F134 | app/src/main/java/dev/stan/yotsuba/feature/catalog/CatalogPane.kt:158 | ui/ux | medium | Tapping the search icon (`CatalogActions`, CatalogScreen.kt:86) sets `searchQuery = ""` in the VM, which makes the `SearchField` appear at CatalogPane.kt:158-167 — but nothing requests focus or raises the keyboard, so the user must tap the field as a second step. Compare BoardsScreen.kt:94, where the field is permanently visible and needs no reveal. The thread's own search bar (ThreadScreen.kt:485 | Confirmed | Open |
| F135 | app/src/main/java/dev/stan/yotsuba/feature/catalog/CatalogScreen.kt:92 | ui/ux | medium | The layout button shows the icon of the *current* layout but acts as a cycler, so the icon reads as a state while behaving as a destination — and all three states share one `contentDescription`, `stringResource(R.string.catalog_layout_switch)` (line 99). TalkBack therefore announces the identical string in comfortable, compact and list mode: a screen-reader user can never tell which layout is acti | Confirmed | Open |
| F136 | app/src/main/java/dev/stan/yotsuba/feature/catalog/CatalogViewModel.kt:53 | 6 canonical layer | medium | `networkMonitor.status.stateIn(viewModelScope, SharingStarted.Eagerly, NetworkStatus.Unmetered)`. `NetworkMonitor.status` is a plain `callbackFlow` (NetworkMonitor.kt:35-46) — not shared — so every collector registers its own `ConnectivityManager.registerNetworkCallback`. `Eagerly` keeps that registration alive for the ViewModel's whole lifetime even when nobody is looking at the catalog, and Home | Confirmed | Open |
| F137 | app/src/main/java/dev/stan/yotsuba/feature/catalog/CatalogViewModel.kt:83 | 0 structural simplification | medium | `combine(result.flow, searchQuery, refreshing, layout, combine(hiddenNos, offline, matcher, ::Triple)) { res, query, isRefreshing, layout, (hidden, offline, matcher) -> ... }` — a nested `combine` producing an anonymous `Triple` that is destructured back out, purely to dodge the 5-argument arity limit. It also collects `settingsRepository.settings` twice (line 77 `layout` and line 80 `matcher`), s | Confirmed | Open |
| F138 | app/src/main/java/dev/stan/yotsuba/feature/catalog/CatalogViewModel.kt:86 | 6 canonical layer | medium | The `when (res) { null -> UiState.Loading; is DataResult.Failure -> UiState.Error(res.error); is DataResult.Success -> UiState.Success(...) }` ladder is copy-pasted verbatim across ViewModels: here (lines 86-117), BoardsViewModel.kt:41-70, and ThreadViewModel.kt:127. `LoadableFlow` and `UiState` already live together in core/util/LoadableFlow.kt and `LoadableFlow` documents the "null result means  | Confirmed | Open |
| F139 | app/src/main/java/dev/stan/yotsuba/feature/catalog/CatalogViewModel.kt:97 | 6 canonical layer | medium | Commits 43eb208 (catalog) and 7a7723a (thread) landed the same feature — apply filters, hide HIDE matches, stub STUB matches, count what was caught — in two different shapes one after the other. The catalog version (lines 97-106) mutates a `mutableMapOf` and a `var hiddenByFilter` from inside a `filter {}` predicate, i.e. a side-effecting predicate whose result depends on iteration order. The thre | Confirmed | Open |
| F140 | app/src/main/java/dev/stan/yotsuba/feature/history/HistoryList.kt:140 | 6 canonical layer | medium | The design system already owns both halves of this: `SearchField` (StateViews.kt:109-123, an OutlinedTextField with a hint res and singleLine) and `NoSearchResults` (StateViews.kt:99-106, EmptyState with `search_no_results_title` = "No matches" / `search_no_results_explanation` = "Nothing matches \"%s\"."). HistoryList reimplements both: `HistorySearchField` builds its own TextField with four hand | Confirmed | Open |
| F141 | app/src/main/java/dev/stan/yotsuba/feature/history/HistoryViewModel.kt:62 | bug | medium | `board.contains(q.removePrefix("/").removeSuffix("/"), ignoreCase = true)` — for the query `"/"` (or `"//"`) both strips fire and the argument becomes `""`. `String.contains("")` is always true, so typing a single slash in the history search box matches every entry instead of none. The `query.isBlank()` guard on line 59 does not catch it because `"/"` is not blank. A silent fallback papering over  | Confirmed | Open |
| F142 | app/src/main/java/dev/stan/yotsuba/feature/home/HomeScreen.kt:84 | bug | medium | Commit 3604285 documents at lines 66-68 that "The current page survives tab switches via rememberSaveable". It does not survive process death. `pageCount` is `boards?.size ?: 0` (line 82) and `boards` is a StateFlow whose initial value is deliberately `null` (HomeViewModel.kt:220-222, "Null until settings have loaded"), so on the first composition after process restore `pageCount == 0` and line 84 | Confirmed | Open |
| F143 | app/src/main/java/dev/stan/yotsuba/feature/home/HomeScreen.kt:98 | 4 magic/wrappers | medium | `val boardInfo by (currentViewModel?.boardInfo ?: remember { kotlinx.coroutines.flow.MutableStateFlow(null) }).collectAsStateWithLifecycle()` — a throwaway `MutableStateFlow` is allocated purely so the elvis chain type-checks, written with a fully-qualified name because the file deliberately avoids importing it. It also puts a `remember` on the right-hand side of an elvis, so the call is condition | Confirmed | Open |
| F144 | app/src/main/java/dev/stan/yotsuba/feature/home/HomeScreen.kt:116 | ui/ux | medium | `list == null -> Unit`: while the settings store is loading, Home renders an empty body under the app bar — no skeleton, no spinner. `LoadingSkeleton` (StateViews.kt:187) exists precisely for this and every other screen in the app uses it via `UiStateContent`. On a cold start the user sees a blank page, then the tab row and catalog appear at once, shifting everything down. | Confirmed | Open |
| F145 | app/src/main/java/dev/stan/yotsuba/feature/home/HomeScreen.kt:136 | 2 spaghetti growth | medium | Commit 131a5c2 splits one gesture across two components with no shared model. `ReorderableTabRow` owns the drag and decides the drop target with a hard-coded `val removeThreshold = with(density) { 32.dp.toPx() }` and `val overRemove = from != null && dragY > removeThreshold` (ReorderableTabRow.kt:80-81), then pushes that private state out through a bespoke two-boolean callback `onDragState(draggin | Confirmed | Open |
| F146 | app/src/main/java/dev/stan/yotsuba/feature/home/HomeViewModel.kt:45 | bug | medium | `removeFavourite` returns an undo closure over `var before: Set<String> = emptySet()`, but `before` is only assigned inside the `viewModelScope.launch` at line 46. The returned lambda is handed straight to the snackbar action (HomeScreen.kt:133-134) and can run before that coroutine's `settingsRepository.update` executes. When it does, `before` is still `emptySet()`, so the undo computes `emptySet | Confirmed | Open |
| F147 | app/src/main/java/dev/stan/yotsuba/feature/home/HomeViewModel.kt:45 | 7 orchestration/atomicity | medium | In `removeFavourite` (added by f11d0db), `var before: Set<String>` is assigned inside `viewModelScope.launch { settingsRepository.update { ... } }` but read from the returned undo lambda, which runs in a different coroutine whenever the user taps the snackbar's Undo. There is no ordering guarantee or memory barrier between the two. If Undo runs before the update coroutine has reached the assignmen | Confirmed | Open |
| F148 | app/src/main/java/dev/stan/yotsuba/feature/home/ReorderableTabRow.kt:135 | bug | medium | Line 87 guards the drag index (`if (from != null && from < widths.size)`) but line 135 does not: `val shift = if (from == null) 0 else shiftFor(index, from, target) * widths[from]`. `widths` is truncated to `boards.size` at line 74, so if `boards` shrinks while `dragging != null`, `widths[from]` throws IndexOutOfBoundsException. `dragging` is not cleared when `boards` changes — `pointerInput(board | Confirmed | Open |
| F149 | app/src/main/java/dev/stan/yotsuba/feature/home/ReorderableTabRow.kt:238 | ui/ux | medium | `BoardTab` is a `Box(...).clickable(onClick = onClick)` with no `role` and no selection semantics; selection is conveyed purely by text colour (line 244) and the drawn indicator at line 213-221. TalkBack announces it as an unlabelled button and never says "selected", so a screen-reader user cannot tell which board is on screen. The row deliberately reimplements `ScrollableTabRow` (see the KDoc at  | Confirmed | Open |
| F150 | app/src/main/java/dev/stan/yotsuba/feature/media/DownloadAction.kt:110 | 4 magic/wrappers | medium | `SaveStatusIcon` hardcodes `Color(0xFF81C784)` for saved (:110), `Color(0xFFE57373)` for failed (:125) and `Color(0xFF81C784).copy(alpha = 0.7f)` for already-saved (:130) — raw Material-2 palette hex, in a codebase whose theme defines a full tonal scheme plus semantic extras (theme/Color.kt YotsubaColors, theme/Theme.kt). They do not follow dynamic colour, and the failed red is a different red fro | Confirmed | Open |
| F151 | app/src/main/java/dev/stan/yotsuba/feature/media/MediaFeedViewer.kt:243 | ui/ux | medium | `MediaFeedViewer` fires the long-press haptic unconditionally before delegating: `onLongPress = { haptics.longPress(); onLongPressPage(page) }` at line 243 (video) and 258 (image). But `onLongPressPage` defaults to `{}` and VaultScreen.kt:646-658 passes nothing — so in the vault every long-press buzzes and does nothing. In MediaScreen the callback is gated on `state.behaviour.holdToSave` (MediaScr | Confirmed | Open |
| F152 | app/src/main/java/dev/stan/yotsuba/feature/media/MediaScreen.kt:115 | 2 spaghetti growth | medium | `pages = state.items.map { it.toViewerPage(context, state) }` runs on every recomposition of MediaScreen — and MediaScreen recomposes on every `saveStatuses` tick, every storage-access refresh and every settings change. `toViewerPage` (:222-256) allocates a fresh `ImageRequest.Builder(context)...build()` and a fresh `ViewerPage` per item per pass, for the whole thread's media list (often 100+ item | Confirmed | Open |
| F153 | app/src/main/java/dev/stan/yotsuba/feature/media/MediaScreen.kt:145 | ui/ux | medium | The live viewer's trailing lambda is `{ page, _ ->` — it discards the `openReplies` callback that `ThreadMediaViewer` passes (ThreadMediaViewer.kt:49, :113) and renders only DownloadAction and Share. The vault viewer uses it and renders a labelled, badge-counted Comment button (VaultScreen.kt:659-672). So in a live thread the reply sub-panel — a whole feature, with its own back stack and animation | Confirmed | Open |
| F154 | app/src/main/java/dev/stan/yotsuba/feature/media/MediaShare.kt:26 | 6 canonical layer | medium | `mimeOf(ext)` — the extension→MIME table — is a public function in `feature/media`. `feature/vault/VaultShare.kt:7` already reaches across features to import it, and `data/repository/MediaVaultRepositoryImpl.kt:237-246` carries a byte-for-byte copy of the same `when` block (`.jpg/.jpeg`, `.png`, `.gif`, `.webp`, `.webm`, `.mp4`, else octet-stream) because the data layer cannot import a feature pac | Confirmed | Open |
| F155 | app/src/main/java/dev/stan/yotsuba/feature/media/MediaShare.kt:44 | ui/ux | medium | `runCatching { context.startActivity(Intent.createChooser(intent, null)) }` swallows the failure with no `onFailure`; same in `shareVaultEntries` (VaultShare.kt:33). If the chooser cannot start (no handler, FileProvider path mismatch, a transient ActivityNotFound), the user taps Share on the viewer top bar or the selection bar and literally nothing happens — no sheet, no snackbar, no log surface.  | Confirmed | Open |
| F156 | app/src/main/java/dev/stan/yotsuba/feature/media/MediaViewModel.kt:79 | 4 magic/wrappers | medium | `MediaUiState` carries six pass-through members over `thread`/`saved`: `posts` (79), `backlinks` (80), `board` (81), `graph` (84), plus `loaded` (74) and `isSaved` (75). A grep across `app/src/main` finds zero production callers of `state.backlinks`, `state.board`, `state.isSaved(...)` or `state.loaded` — the composables read `thread.backlinks` / `thread.board` / `thread.graph` off `ViewerThread`  | Confirmed | Open |
| F157 | app/src/main/java/dev/stan/yotsuba/feature/media/MediaViewModel.kt:111 | 5 type/boundary | medium | `private val details = MutableStateFlow<ThreadDetails?>(null)` (111) holds exactly what `Source.Loaded(val details: ThreadDetails)` (107) already holds. `load()` writes both by hand (lines 134 and 136) and nothing keeps them in step afterwards. `details` is not part of the `combine` at 148 — it exists only so `enqueueSave` (190) and `conversationFor` can read the thread outside the UI pipeline. Tw | Confirmed | Open |
| F158 | app/src/main/java/dev/stan/yotsuba/feature/media/MediaViewModel.kt:128 | 7 orchestration/atomicity | medium | `load()` awaits `threadRepository.thread(board, threadNo)` (128), then — and only then — falls into `mediaVault.savedThread(...)` if it failed (131) and finally awaits `boardRepository.board(board)` (133). The board lookup is independent of the thread fetch and can itself hit the network: `BoardRepositoryImpl.board(code)` calls `boards()`, which does `api.boards(...)` whenever `cached` is null (Bo | Confirmed | Open |
| F159 | app/src/main/java/dev/stan/yotsuba/feature/media/MediaViewModel.kt:173 | 6 canonical layer | medium | Settings -> ViewerBehaviour mapping is written twice: MediaViewModel L173-178 (`ViewerBehaviour(keepScreenOn = settings.keepScreenOnWhileWatching, doubleTapSeek = settings.doubleTapSeekEnabled, seekStepSeconds = settings.seekStep.seconds, holdToSave = settings.holdToSave)`) and VaultViewModel L252-256 (same three fields, no holdToSave). The vault copy already drifted (it omits holdToSave silently) | Rejected (Misreads the code: VaultViewModel's omission of holdToSave is deliberate and documented ('Saving does not apply here', VaultViewModel.kt:249), not silent drift. What remains is a four-line data-class ) | Deferred (rejected in verification) |
| F160 | app/src/main/java/dev/stan/yotsuba/feature/media/MediaViewModel.kt:210 | 6 canonical layer | medium | `conversationFor` decides whether to capture replies by reading `uiState.value.saveReplies` (210) — i.e. the setting is round-tripped through a `stateIn(WhileSubscribed(5_000))` projection instead of being read from `settingsRepository`. `MediaUiState()`'s default is `saveReplies = false`, so any `enqueueSave` that runs while the UI state has no collector (5 s after the last subscriber drops) sile | Confirmed | Open |
| F161 | app/src/main/java/dev/stan/yotsuba/feature/media/SoundTrack.kt:45 | 6 canonical layer | medium | SoundTrack.kt:45-57 and VideoPage.kt:153-165 are the same block twice: `val lifecycleOwner = LocalLifecycleOwner.current`, `val shouldPlay = rememberUpdatedState(...)`, `DisposableEffect(lifecycleOwner, player) { LifecycleEventObserver { _, event -> ON_STOP -> player.playWhenReady = false; ON_START -> player.playWhenReady = shouldPlay.value; else -> {} } … }`. Identical down to the `else -> {}`. B | Confirmed | Open |
| F162 | app/src/main/java/dev/stan/yotsuba/feature/media/ViewerChrome.kt:69 | 6 canonical layer | medium | MotionSpecs.kt states the contract: 'Everything here collapses to its end state under reduced motion, so a call site never checks that itself' (MotionSpecs.kt:24-27), and provides rememberMotionSpec/motionEnter/motionExit. Three animations in this slice bypass it and animate regardless of `LocalReduceMotion` / the system animator scale: `AnimatedVisibility(enter = fadeIn(), exit = fadeOut())` for  | Confirmed | Open |
| F163 | app/src/main/java/dev/stan/yotsuba/feature/settings/sections/FiltersSection.kt:314 | 6 canonical layer | medium | `private val FilterField.labelRes` (314-323) and `private val FilterAction.labelRes` (325-329) are defined at the bottom of a UI file in package `feature.settings.sections`, while `SettingsLabels.kt` in `feature.settings` exists for exactly this and carries the doc comment "String resources for the settings enums, shared by the index summaries and the chip rows" with `internal val` label maps for  | Confirmed | Open |
| F164 | app/src/main/java/dev/stan/yotsuba/feature/settings/sections/StorageSection.kt:36 | 5 type/boundary | medium | Six of the twelve parameters carry defaults — `restoreAvailable: BackupInfo? = null, backupResult: BackupResult? = null, onExportBackup: () -> Unit = {}, onImportBackup: () -> Unit = {}, onDismissRestore: () -> Unit = {}, onBackupResultShown: () -> Unit = {}` (lines 36-41) — yet the single call site, `SettingsSectionScreen.kt:100-114`, passes every one of them. The defaults advertise an optional b | Confirmed | Open |
| F165 | app/src/main/java/dev/stan/yotsuba/feature/settings/sections/StorageSection.kt:45 | ui/ux | medium | `fun clear(bodyRes: Int, action: () -> Unit) = confirmThen(bodyRes) { action(); showMessage(cleared) }`. Every `action` here is a fire-and-forget `viewModelScope.launch` (SettingsViewModel.kt:106-108: `onClearCache`, `onClearHistory`, `onClearBookmarks`). The "Cleared" snackbar is therefore shown the instant the coroutine is *launched*, not when the work finishes and not conditionally on it succee | Confirmed | Open |
| F166 | app/src/main/java/dev/stan/yotsuba/feature/settings/SettingsSectionScreen.kt:53 | 7 orchestration/atomicity | medium | `viewModel: SettingsViewModel = hiltViewModel()` inside a NavHost destination scopes the ViewModel to the NavBackStackEntry, so every settings subscreen constructs its own `SettingsViewModel` — and every construction runs `init { viewModelScope.launch { if (backupRepository.isFreshInstall()) _restoreAvailable.value = backupRepository.available() } }` (SettingsViewModel.kt:58-62), which touches the | Confirmed | Open |
| F167 | app/src/main/java/dev/stan/yotsuba/feature/settings/SettingsViewModel.kt:45 | 4 magic/wrappers | medium | `private val backupRepository: BackupRepository = BackupRepository.None` sits on an `@Inject constructor` of a `@HiltViewModel`. Dagger ignores Kotlin default arguments entirely, and `core/di/Modules.kt:159` binds `BackupRepositoryImpl`, so in the app this default is unreachable. Its only consumer is a test that omits the argument (`app/src/test/java/dev/stan/yotsuba/feature/SettingsViewModelTest. | Confirmed | Open |
| F168 | app/src/main/java/dev/stan/yotsuba/feature/settings/UpdatesSection.kt:38 | ui/ux | medium | `var needsPermission by remember { mutableStateOf(false) }` is set to true when Install is tapped without the install-packages permission (line 123) and only ever cleared by a second successful tap (line 126). The tap also fires `onRequestInstallPermission()`, which leaves the app for the system "install unknown apps" screen. When the user grants the permission and returns, the composition is stil | Confirmed | Open |
| F169 | app/src/main/java/dev/stan/yotsuba/feature/thread/components/ExternalLinkDialog.kt:27 | ui/ux | medium | The `dismissButton` slot holds a `Row` with two visually identical `TextButton`s: "Always trust <domain>" and "Cancel". The trust action is persistent and security-relevant (it writes to `settings.trustedDomains`, ThreadViewModel.kt:511-515) and it *also* opens the link (ThreadScreen.kt:466-469), yet it sits immediately beside Cancel with the same weight and colour, in the slot users have learned  | Confirmed | Open |
| F170 | app/src/main/java/dev/stan/yotsuba/feature/thread/components/PostCard.kt:75 | 5 type/boundary | medium | `PostCardActions` encodes the card's three contexts (list, preview sheet, media-viewer reply panel) as ten independently-nullable callbacks plus a `forPreview()` method that nulls six of them (lines 94-99). The card then reads those nulls as mode switches rather than as "no handler": line 207 `val shared = actions.onThumbnailLongPress != null` decides whether to claim the shared-element key, and l | Confirmed | Open |
| F171 | app/src/main/java/dev/stan/yotsuba/feature/thread/components/PostCard.kt:86 | 2 spaghetti growth | medium | dab9640 replaced the backlink count chip with a "Quoted by" row but kept the old one alive behind a second nullable callback, encoding *which widget to draw* in which lambda happens to be non-null: PostCardActions.onBacklinksTap at line 86 is documented "Legacy count chip, shown only when [onBacklinkTap] is null (the media viewer's panel)", and PostCard.kt:262-276 branches `if (backlinkCount > 0 & | Confirmed | Open |
| F172 | app/src/main/java/dev/stan/yotsuba/feature/thread/components/PostCard.kt:207 | 4 magic/wrappers | medium | ``` // Only the list's own card shares with the viewer: a preview or the // viewer's reply panel would claim the same key twice on one screen. val shared = actions.onThumbnailLongPress != null val sharedWithViewer = if (shared) Modifier.sharedMedia(media.fullUrl) else Modifier val sharedWithCatalog = if (shared && post.isOp) Modifier.sharedMedia(media.thumbnailUrl) else Modifier ``` Whether the ca | Confirmed | Open |
| F173 | app/src/main/java/dev/stan/yotsuba/feature/thread/components/PostCard.kt:208 | 4 magic/wrappers | medium | Commit e3f3843 stacks two shared-element modifiers on one layout node: `.then(sharedWithViewer).then(sharedWithCatalog)` (lines 223-224), where `sharedWithViewer = Modifier.sharedMedia(media.fullUrl)` (line 208) and `sharedWithCatalog = Modifier.sharedMedia(media.thumbnailUrl)` (lines 209-211) for the OP. Each `sharedMedia` expands to a full `SharedTransitionScope.sharedBounds(...)` (SharedMedia.k | Rejected (The stacking is real (208-211 applied at 223-224), but the finding is speculation dressed as a defect: it asserts Compose 'is specified for one shared-content state per node' without evidence, and nam) | Deferred (rejected in verification) |
| F174 | app/src/main/java/dev/stan/yotsuba/feature/thread/components/PostCard.kt:329 | 6 canonical layer | medium | PostCard.SaveStatusBadge L329-359 and DownloadAction.SaveStatusIcon (feature/media/DownloadAction.kt L105-138) are the same `when (status)` over MediaSaveStatus with the same icons (DownloadDone/Schedule/spinner/Download-or-ErrorOutline/DownloadDone@0.7) and the same hard-coded colours 0xFF81C784 and 0xFFE57373 (PostCard L340, L351, L355; DownloadAction L110, L125, L130). Only sizes differ. | Rejected (Misreads the code: the two mappings are not the same. PostCard tints Queued Color.White while DownloadAction uses White.copy(alpha = 0.7f); PostCard's Failed icon is ErrorOutline, DownloadAction's is ) | Deferred (rejected in verification) |
| F175 | app/src/main/java/dev/stan/yotsuba/feature/thread/components/QuotePreviewSheet.kt:71 | 2 spaghetti growth | medium | `val scrollStates = remember { mutableMapOf<Long, LazyListState>() }` is then mutated during composition on the next two lines: `scrollStates.keys.retainAll(preview.path.toSet())` and `scrollStates.getOrPut(focus.no) { LazyListState() }`. Composition is meant to be side-effect free and can be re-run or abandoned; the eviction here runs on every recomposition of the sheet, including ones triggered  | Confirmed | Open |
| F176 | app/src/main/java/dev/stan/yotsuba/feature/thread/components/ThreadTopBar.kt:79 | ui/ux | medium | The subtitle slot is an if/else-if chain over three unrelated signals: the poster-ID filter chip, the "N replies to you" indicator, and the "N posts filtered" count (lines 79-102). Only one can ever show. So filtering by poster ID silently hides the fact that someone replied to you, and a single reply-to-you hides the filtered count — the user has no way to know information was dropped. The "repli | Confirmed | Open |
| F177 | app/src/main/java/dev/stan/yotsuba/feature/thread/components/ThreadTopBar.kt:96 | ui/ux | medium | The top bar's subtitle slot is a single `if / else if / else if` chain over three unrelated indicators (lines 79-102): the poster-ID filter chip, "N replies to you" (added by the thread-reader merge 9f0fbd0), and "N filtered" (added by 7a7723a at line 96). Each commit appended one more `else if` to the previous one's branch, so the three became mutually exclusive by accident rather than by design: | Rejected (Duplicate of F176 — same lines, same defect, same remedy; the only extra content is commit archaeology. Keep F176, which additionally catches the sub-48dp untyped tap target.) | Deferred (rejected in verification) |
| F178 | app/src/main/java/dev/stan/yotsuba/feature/thread/ThreadScreen.kt:106 | ui/ux | medium | `var searchOpen by remember { mutableStateOf(false) }` is likewise not saveable, while the query lives in the VM's `Session` (ThreadUiState.kt:129) and survives rotation. After a rotation the search bar (and its match counter and next/prev buttons, ThreadScreen.kt:479-506) is gone but `s.searchQuery` is still set, so every match stays highlighted in the post bodies (`highlight = s.searchQuery`, li | Confirmed | Open |
| F179 | app/src/main/java/dev/stan/yotsuba/feature/thread/ThreadScreen.kt:191 | ui/ux | medium | All three jump buttons animate across the whole list: `onTop = { listState.animateScrollToItem(0) }` (:191), `onFirstNew = { listState.animateScrollToItem(index) }` (:192), `onBottom = { listState.animateScrollToItem(rows.lastIndex.coerceAtLeast(0)) }` (:193); the catalog's scroll-to-top FAB does the same, `gridState.animateScrollToItem(0)` at CatalogPane.kt:222, reachable from any index past 8. ` | Confirmed | Open |
| F180 | app/src/main/java/dev/stan/yotsuba/feature/thread/ThreadScreen.kt:348 | 3 design cleanliness | medium | The thread list uses `items(count = s.rows.size, key = { ... })` with no `contentType`, while the item body (:360-378) renders four structurally different composables depending on the row type: a `PostCard` inside a padded Box, a `NewPostsDivider` (which additionally starts an `AnimatedVisibility` transition on first landing, :575), a `MoreRepliesRow` (a Text), and a `FilteredRow` (a Card). With a | Confirmed | Open |
| F181 | app/src/main/java/dev/stan/yotsuba/feature/thread/ThreadViewModel.kt:187 | 7 orchestration/atomicity | medium | The two visible-post consumers are set up side by side but debounce inconsistently: ``` topVisiblePostNo.filterNotNull().distinctUntilChanged().collectLatest { postNo ->     delay(500)     historyRepository.updateScrollPosition(board, threadNo, postNo) } ... bottomVisiblePostNo.filterNotNull().distinctUntilChanged().collect(::raiseReadMark) ``` `raiseReadMark` (line 532) does a `historyRepository. | Rejected (The code is at line 187 as cited, but the proposed fix breaks an existing test: ThreadViewModelTest.kt:516-523 drives onVisiblePostsChanged and asserts env.history.readMark/env.bookmarks.seen after on) | Deferred (rejected in verification) |
| F182 | app/src/main/java/dev/stan/yotsuba/feature/thread/ThreadViewModel.kt:543 | 6 canonical layer | medium | Lines 543-684 are a 140-line `companion object` holding the whole presentation-model pipeline — `visiblePosts`, `filterVerdicts`, `filteredRow`, `rows`, `linearRows`, `treeRows` (a 26-line index-walking loop with its own `error("hidden posts were dropped above")` invariant at line 610), `previewSheet`, `quoteLabels`, `postStates`, `postAt`, `searchMatches`, plus `MAX_TREE_DEPTH` which ThreadScreen | Confirmed | Open |
| F183 | app/src/main/java/dev/stan/yotsuba/feature/threads/ThreadsScreen.kt:73 | ui/ux | medium | `var searching by remember { mutableStateOf(false) }` sits one line below `var segment by rememberSaveable { ... }` (line 67) — the author saved the segment but not the search toggle. The query itself lives in `HistoryViewModel` (HistoryViewModel.kt:98 `private val query = MutableStateFlow("")`), and tab navigation uses `popUpTo(...) { saveState = true }` + `restoreState = true` (AppNavHost.kt:84- | Confirmed | Open |
| F184 | app/src/main/java/dev/stan/yotsuba/feature/threads/ThreadsScreen.kt:99 | 6 canonical layer | medium | The Threads shell owns another feature's app-bar state and wiring: it holds `searching` and `confirmClear` (lines 73-74), builds History's search and clear-all icon buttons inline (lines 99-117) including the `history.totalCount > 0` enablement rules, places `HistorySearchField` into the title slot (line 83) and hosts `HistoryClearDialog` (lines 161-170). Bookmarks does this the right way — one `B | Confirmed | Open |
| F185 | app/src/main/java/dev/stan/yotsuba/feature/vault/VaultDedupSheet.kt:311 | 4 magic/wrappers | medium | `DateFormat.getDateInstance(DateFormat.SHORT).format(Date(entry.savedAt))` is called inside `Thumb`, a composable rendered once per duplicate file inside a `LazyRow` inside a `LazyColumn` (250-251). `DateFormat.getDateInstance` builds a fresh SimpleDateFormat (pattern lookup + symbol tables) on every composition of every visible thumbnail while the user drags the similarity slider and the list res | Confirmed | Open |
| F186 | app/src/main/java/dev/stan/yotsuba/feature/vault/VaultEntrySheet.kt:76 | 6 canonical layer | medium | Four private near-identical "ListItem(headlineContent = Text, leadingContent = Icon, modifier = clickable)" sheet rows: VaultEntrySheet.SheetAction L76-82, PostActionSheet.SheetRow L69-75, BookmarksList.SheetAction L220-234 (adds enabled/supporting), CatalogPane.ThreadActionsSheet L247-261 (three inline ListItems). The sheet title Text with `padding(horizontal = 24.dp, vertical = 8.dp)` is also re | Confirmed | Open |
| F187 | app/src/main/java/dev/stan/yotsuba/feature/vault/VaultExplorer.kt:114 | 5 type/boundary | medium | `val view = Triple(state.sort, state.reversed, state.filter)` is allocated fresh on every recomposition of VaultExplorer and passed to three list composables declared as `view: Any` (:218 BoardList, :256 ThreadList, :483 MediaGrid). The compose compiler report names it explicitly — app-composables.txt:800-838 prints `restartable skippable fun BoardList(unstable view: Any ...)`, `... ThreadList(uns | Confirmed | Open |
| F188 | app/src/main/java/dev/stan/yotsuba/feature/vault/VaultExplorer.kt:117 | ui/ux | medium | `VaultUiState()` defaults `hasStorageAccess = false` (VaultViewModel.kt:191) and `uiState` is `stateIn(..., VaultUiState())` behind `.flowOn(compute)` (VaultViewModel.kt:468-469), so the first composed frame of the vault tab always takes the `!state.hasStorageAccess` branch and paints the 'The vault needs "All files access"' copy with a grant Button — even for a user who granted it long ago. The r | Confirmed | Open |
| F189 | app/src/main/java/dev/stan/yotsuba/feature/vault/VaultExplorer.kt:131 | 6 canonical layer | medium | Three bespoke empty/error states here reimplement what core/designsystem/component/StateViews.kt already provides: the no-content state (:131-141) is a bare Column of two Texts where `EmptyState(title, explanation, icon, action)` exists (StateViews.kt:68); the search-empty state (:145-151) is a centred Text where `NoSearchResults(query)` exists (StateViews.kt:99); the grant prompt (:117-129) is a  | Confirmed | Open |
| F190 | app/src/main/java/dev/stan/yotsuba/feature/vault/VaultExplorer.kt:340 | 6 canonical layer | medium | The same "SingleChoiceSegmentedButtonRow over an enum's entries, forEachIndexed, SegmentedButton(selected = x == option, shape = itemShape(i, size), label = stringResource(when(option) ...))" block appears four times: VaultExplorer.ModeSwitch L340-362, VaultExplorer.VaultChipRow L429-449 (icon variant), VaultDedupSheet.ModeControls L204-221, ThreadsScreen L127-137. Each carries its own when->R.str | Confirmed | Open |
| F191 | app/src/main/java/dev/stan/yotsuba/feature/vault/VaultExplorer.kt:483 | 5 type/boundary | medium | `MediaGrid(view: Any, ...)`, `ThreadList(view: Any, ...)` (:256) and `BoardList(view: Any, ...)` (:218) take an untyped `Any` whose only use is `LaunchedEffect(view) { scrollToItem(0) }`. The caller builds it as `val view = Triple(state.sort, state.reversed, state.filter)` (:114). The type says nothing, and a caller passing the wrong Triple (or `state` itself) still compiles and silently changes s | Rejected (Duplicate of F187 (same `view: Any` parameters, same fix, cited at MediaGrid rather than the origin).) | Deferred (rejected in verification) |
| F192 | app/src/main/java/dev/stan/yotsuba/feature/vault/VaultExplorer.kt:503 | ui/ux | medium | Each grid cell is a bare `Box(Modifier.aspectRatio(1f).combinedClickable(onClick = …, onLongClick = …))` with no `role` and no selection semantics; the tick that communicates selected/not-selected is an `Icon(..., contentDescription = null)` (:541-546) and the only other cue is a 6.dp inset on the thumbnail (:518). To TalkBack every cell in multi-select mode reads identically (the file name from M | Confirmed | Open |
| F193 | app/src/main/java/dev/stan/yotsuba/feature/vault/VaultExplorer.kt:604 | 6 canonical layer | medium | 656c59f added `internal fun formatDuration(ms: Long)` at VaultExplorer.kt:604-610 and a file-level `private val dateFormat = DateFormat.getDateInstance(DateFormat.MEDIUM)` + `internal fun savedDate(millis)` at :612-614, inside a 628-line Compose file. Both duplicate work that already exists elsewhere: VideoPage.kt:413-416 has `formatMs` doing the same m:ss formatting for the player's transport bar | Confirmed | Open |
| F194 | app/src/main/java/dev/stan/yotsuba/feature/vault/VaultScreen.kt:113 | 6 canonical layer | medium | `statsOpen` (113) and `dedupOpen` (114) are plain `remember { mutableStateOf(false) }`, not `rememberSaveable` and not in the ViewModel — while this very screen deliberately hoists explorer position, viewer url, query, inspected entry and pending delete into the VM because, per VaultViewModel.kt:262-264, "the adaptive layout switch on rotation (nav bar ↔ nav rail) rebuilds the screen and would wip | Rejected (The claimed symptom does not hold. The activity declares android:configChanges="orientation\|screenSize\|screenLayout\|…" (AndroidManifest.xml:32), so rotation does not recreate it, and the nav bar↔ra) | Deferred (rejected in verification) |
| F195 | app/src/main/java/dev/stan/yotsuba/feature/vault/VaultScreen.kt:193 | 2 spaghetti growth | medium | c96f983 added `BackHandler(enabled = state.viewer != null) { closeViewer() }` as the last handler and, in the same commit, bolted `&& state.viewer == null` onto the drill-down handler. Because BackHandler callbacks fire in reverse registration order (last-composed wins — the commit's own comment states this: "its BackHandler is composed later, so it wins while enabled"), the guard is redundant. Tw | Confirmed | Open |
| F196 | app/src/main/java/dev/stan/yotsuba/feature/vault/VaultScreen.kt:242 | 1 file size | medium | `VaultScreen` runs from line 95 to 461 — a single ~370-line composable holding notice/undo snackbar effects, two picker launchers, four BackHandlers, the whole top bar, the FAB, the explorer, the viewer overlay and five dialogs/sheets. The `actions = { ... }` lambda alone spans 242-350: four sibling `if (state.hasStorageAccess)` checks, two `Box`+`DropdownMenu` pairs and a `state.sync.running` bra | Rejected (Duplicate of F066: same file, same VaultScreen 95-461 span, same topBar/actions block, same proposed extraction into VaultTopBar with ImportMenu/SyncMenu. It cites line 242 (`actions = {`) instead of ) | Deferred (rejected in verification) |
| F197 | app/src/main/java/dev/stan/yotsuba/feature/vault/VaultScreen.kt:297 | ui/ux | medium | The toolbar button at line 297-298 is a Refresh icon labelled `R.string.vault_sync` ("sync"), but its dropdown has become a junk drawer of four unrelated things: Rescan and Fetch new replies (sync), Statistics (added by bfd7d5d, line 324-333) and Duplicates (line 334-343). Two of the four have nothing to do with refreshing; a user looking for vault statistics has no reason to open a refresh menu,  | Confirmed | Open |
| F198 | app/src/main/java/dev/stan/yotsuba/feature/vault/VaultScreen.kt:406 | ui/ux | medium | `val stats by viewModel.stats.collectAsStateWithLifecycle()` sits inside `if (statsOpen)` (VaultScreen.kt:405-407), so nothing subscribes to the `WhileSubscribed(5_000)` flow until the sheet is already being composed. The first value the sheet sees is therefore the `stateIn` initial value, `VaultStats.of(emptyList(), 0L)` (VaultViewModel.kt:475), whose `isEmpty` is true (VaultStats.kt:40). VaultSt | Confirmed | Open |
| F199 | app/src/main/java/dev/stan/yotsuba/feature/vault/VaultStatsSheet.kt:49 | 4 magic/wrappers | medium | `token/Spacing.kt` defines xs/sm/md/lg/xl/xxl and most of the app reads `LocalSpacing.current`, but the vault sheets and grid ignore it wholesale: VaultStatsSheet uses literal 16/12/32/24/8/4/2/96/3.dp (:49, :52, :61, :74, :87, :114, :122, :131, :144, :179, :184, :194, :202, :208) and never touches LocalSpacing; VaultDedupSheet does the same (:92, :95, :122, :159, :188, :190, :223, :248, :249, :25 | Confirmed | Open |
| F200 | app/src/main/java/dev/stan/yotsuba/feature/vault/VaultStatsSheet.kt:217 | 6 canonical layer | medium | `private val dateFormat: DateFormat = DateFormat.getDateInstance(DateFormat.MEDIUM)` here is byte-for-byte the same declaration as VaultExplorer.kt:612, which already backs the shared `internal fun savedDate(millis: Long)` (VaultExplorer.kt:614) used by ThreadList (281) and `entryDetails` (VaultEntrySheet.kt:90). Two copies of the same formatter in the same package, both capturing the default Loca | Confirmed | Open |
| F201 | app/src/main/java/dev/stan/yotsuba/feature/vault/VaultViewModel.kt:425 | 5 type/boundary | medium | `@Suppress("UNCHECKED_CAST") val uiState = combine(mediaVault.entries(), syncState, selection, viewing, activity, editing) { args -> val entries = args[0] as List<VaultEntry>; ... args[5] as Editing }` -- six unchecked casts from `Array<Any?>`. todo.md §1 records the same smell being fixed in ThreadViewModel ("14-flow array-combine with 8 unchecked casts"), and this file already builds typed sub-s | Rejected (Duplicate of F068 (same 6-arity array combine and unchecked casts at VaultViewModel.kt:424-433); F068 carries the simpler fold that keeps the existing Editing/Activity structure.) | Deferred (rejected in verification) |
| F202 | app/src/main/java/dev/stan/yotsuba/feature/vault/VaultViewModel.kt:471 | 7 orchestration/atomicity | medium | `val stats: StateFlow<VaultStats> = mediaVault.entries().map { VaultStats.of(it, System.currentTimeMillis()) }.flowOn(compute).stateIn(...)` opens a second independent collection of the same Room-backed flow that `uiState` (VaultViewModel.kt:452, `.flowOn(compute)` at 467-468) is already collecting. Every vault change therefore emits into two collectors that each walk and group the entire entry li | Confirmed | Open |
| F203 | app/src/main/java/dev/stan/yotsuba/feature/vault/VaultViewModel.kt:643 | 0 structural simplification | medium | Six commits in this slice each bolted one more field-plus-flow onto the same class (b7fa6cf delete dialog, d18372f delete outcome, 73afeae import/delete scoping, f8185ac playback, 2249904 storage access, 5107dca viewer thread). The result is 759 lines holding a 20-field `VaultUiState`, six combine inputs and roughly forty members covering sort/filter, drill-down, viewer, shuffle, import, sync, ren | Confirmed | Open |
| F204 | app/src/main/java/dev/stan/yotsuba/navigation/AppNavHost.kt:111 | bug | medium | `val openSettings = { navController.navigate(Route.Settings) }` — no `launchSingleTop`, no in-flight guard. The same lambda is handed to HomeScreen, BoardsScreen, ThreadsScreen and VaultScreen (lines 187, 193, 233, 239). Every other push in this file has the same shape (`navigate(Route.Thread(...))` at 185, 204, 216). A double tap on the gear (or on a bookmark row) dispatches two navigate() calls  | Confirmed | Open |
| F205 | app/src/main/java/dev/stan/yotsuba/navigation/AppNavHost.kt:164 | 6 canonical layer | medium | All four NavHost transition lambdas open with a reduced-motion branch: `if (reduced) EnterTransition.None else …` at 165-168, 170-171, 173-175, 177-180. `core/designsystem/MotionSpecs.kt` is the declared canonical home for this — its header says "Everything here collapses to its end state under reduced motion, so a call site never checks that itself" — and it already exports `motionEnter()`, `moti | Rejected (Duplicate of F074: same file, same four lambdas (164-180), same rule and same proposed move into MotionSpecs.kt. Reporting it twice adds nothing.) | Deferred (rejected in verification) |
| F206 | app/src/main/java/dev/stan/yotsuba/navigation/AppNavHost.kt:305 | 4 magic/wrappers | medium | `private fun NavController.hasEntry(route: Route) = runCatching { getBackStackEntry(route) }.isSuccess` — the common case (following a quote to a board whose catalog is not open) throws `IllegalArgumentException` from inside Navigation and swallows it to produce a boolean. Exception-driven control flow on the hot path of every quotelink tap. In the same function, line 289 buries a mutation inside  | Confirmed | Open |
| F207 | app/src/main/java/dev/stan/yotsuba/navigation/Route.kt:43 | 6 canonical layer | medium | `SettingsSectionId` lives in the navigation package and carries `titleRes: Int` and `icon: ImageVector` — pure Settings presentation data — which drags nine `androidx.compose.material.icons.outlined.*` imports and `dev.stan.yotsuba.R` into the routing file (Route.kt:4-15). The navigation layer only needs the enum as a serializable route argument; the icon and title are consumed solely by `Settings | Confirmed | Open |
| F208 | app/src/main/res/values/strings.xml:6 | 3 design cleanliness | medium | 16 string resources have no reference from Kotlin or XML. Checked each with `rg -c "R\.(string\|plurals)\.<name>\b" app/src -g '*.kt'` = 0 and no `@string/` use, and `rg getIdentifier app/src` is empty so there is no reflective lookup: tab_bookmarks (6), tab_history (7), boards_category_toggle (43), thread_spoiler (74), media_saved (89), bookmarks_archived_badge (122), bookmarks_new_replies (124), | Confirmed | Open |
| F209 | app/src/main/res/values/strings.xml:122 | 6 canonical layer | medium | Two competing conventions for the same resources coexist: a 284-line `strings.xml` organised by feature comment banners (`<!-- Bookmarks -->` line 118, `<!-- History -->` line 131, `<!-- Settings -->` line 145) *and* 15 per-feature `strings_*.xml` files covering the same features. The result is superseded twins with identical text: `strings.xml:122 bookmarks_archived_badge="Archived"` (dead) vs `s | Confirmed | Open |
| F210 | app/src/test/java/dev/stan/yotsuba/data/BackupRepositoryImplTest.kt:97 | 7 orchestration/atomicity | medium | `Env` defaults `scope: CoroutineScope = CoroutineScope(Job())` — a real, never-cancelled scope on `Dispatchers.Default`. `BackupRepositoryImpl` uses that scope in its constructor for a live auto-export collector (`.debounce(AUTO_EXPORT_DEBOUNCE_MS).launchIn(scope)`, BackupRepositoryImpl.kt:54-56). Five of the seven tests take the default (lines 111, 120, 132, 138, 164, 174, 185-188), several build | Confirmed | Open |
| F211 | app/src/test/java/dev/stan/yotsuba/feature/boards/BoardsViewModelTest.kt:64 | 4 magic/wrappers | medium | The same three-line Turbine helper — `dispatcher.scheduler.advanceUntilIdle(); return expectMostRecentItem()` — is written out six times with only the type parameter changed: BoardsViewModelTest.kt:64, CatalogViewModelTest.kt:122, HistoryViewModelTest.kt:103, SettingsViewModelTest.kt:136, MediaViewModelTest.kt:175, VaultViewModelTest.kt:137 (plus a `now()` variant at VaultViewModelTest.kt:143). Ea | Confirmed | Open |
| F212 | app/src/test/java/dev/stan/yotsuba/feature/catalog/CatalogViewModelTest.kt:49 | 6 canonical layer | medium | `private val dispatcher = StandardTestDispatcher()` + `@Before fun setUp() { Dispatchers.setMain(dispatcher) }` + `@After fun tearDown() { Dispatchers.resetMain() }` appears in 13 test classes (`grep -rln "Dispatchers.setMain" app/src/test` → 13 files: CatalogViewModelTest, BoardsViewModelTest, HistoryViewModelTest, SettingsViewModelTest, SettingsBackupViewModelTest, MediaViewModelTest, ThreadView | Confirmed | Open |
| F213 | app/src/test/java/dev/stan/yotsuba/feature/HistoryViewModelTest.kt:95 | 6 canonical layer | medium | `fake/FakeSettings.kt:9` carries the KDoc "Several ViewModel tests declare their own private copy of this; new tests should use this one." That instruction is ignored six more times, verbatim: HistoryViewModelTest.kt:95, BoardsViewModelTest.kt:51, CatalogViewModelTest.kt:75, MediaViewModelTest.kt:92, SettingsViewModelTest.kt:55 (all `class FakeSettingsRepository`, identical `MutableStateFlow` + `u | Confirmed | Open |
| F214 | app/src/test/java/dev/stan/yotsuba/feature/MediaViewModelTest.kt:92 | 6 canonical layer | medium | app/src/test/java/dev/stan/yotsuba/fake/FakeSettings.kt exists and its own doc says "Several ViewModel tests declare their own private copy of this", yet six tests still do: MediaViewModelTest L92, HistoryViewModelTest L95, ThreadViewModelTest L130, SettingsViewModelTest L55, boards/BoardsViewModelTest L51, catalog/CatalogViewModelTest L75 — byte-for-byte the same MutableStateFlow-backed class. Sa | Confirmed | Open |
| F215 | app/src/test/java/dev/stan/yotsuba/feature/ThreadViewModelTest.kt:526 | 2 spaghetti growth | medium | One 777-line file reads the same ui state three different ways. A member helper `private fun content(vm: ThreadViewModel): ThreadContent = (vm.uiState.value as UiState.Success).data` is declared at line 526 — buried between two `@Test` functions rather than with the other fixtures at the top. Five tests ignore it and inline the cast anyway (lines 682, 736, 751, 767, 774), and line 713 declares a * | Confirmed | Open |
| F216 | app/src/test/java/dev/stan/yotsuba/feature/VaultViewModelTest.kt:168 | 0 structural simplification | medium | `VaultViewModel(vault, FakeSettings(), boards, compute = Dispatchers.Unconfined)` is written out ~29 times in this 711-line file (lines 168, 188, 198, 217, 232, 269, 293, 309, 322, 345, 367, 392, 411, 435, 447, 475, 503, 521, 534, 547, 564, 579, 590, 611, 624, 643, 666, 675, 686, 704). Two variants sneak in a `SavedStateHandle` (411, 435) and two capture the settings instance (367, 502). Every oth | Confirmed | Open |
| F217 | app/src/test/java/dev/stan/yotsuba/util/NetworkErrorMappingTest.kt:16 | 6 canonical layer | medium | This whole 44-line class tests `toNetworkError`, which `ApiResultTest.kt` (same package `dev.stan.yotsuba.util`) already covers at lines 45-77 with the same cases: SocketTimeout→Timeout, UnknownHost/Connect→Offline, 404→NotFound, 429→RateLimited, 5xx→Server, 418→Unknown, arbitrary→Unknown. Both files even carry their own copy of the same builder — `ApiResultTest.kt:41 private fun httpException(cod | Confirmed | Open |
| F218 | app/src/test/java/dev/stan/yotsuba/vault/VaultPathsTest.kt:58 | 6 canonical layer | medium | Three tests in this file test `VaultMetaCodec`/`VaultThreadMeta`, not `VaultPaths`: `meta json round-trips and upserts by file name` (58-70), `meta remove drops the entry` (72-78) and `decode of garbage returns null` (80-83). `VaultMetaCodecTest.kt` already covers exactly these — `encode-decode round-trips` (29), `upsert replaces by fileName keeping order stable` (47), `remove drops by fileName` ( | Confirmed | Open |
| F219 | .github/workflows/ci.yml:56 | 7 orchestration/atomicity | low | Four separate Gradle invocations run back to back — `:app:testDebugUnitTest` (56), `:app:lintDebug` (59), `:app:compileDebugAndroidTestKotlin` (65), `:app:assembleDebug` (68) — each paying full configuration and daemon warm-up, and each serialised even though lint and the androidTest compile share no output with the unit tests. On a failure in step 1 the later signals are never produced, so a PR g | Rejected (Lines 56/59/65/68 are as cited, but this isn't a structural defect the rules flag. Named per-check steps are the direct, boring form: each failure is attributed to a step in the GitHub UI, and collaps) | Deferred (rejected in verification) |
| F220 | .gitignore:1 | 4 magic/wrappers | low | `.idea/` is partially tracked (git ls-files shows .idea/gradle.xml, misc.xml, runConfigurations.xml, etc.) while `.idea/modules.xml` and `.idea/yotsuba.iml` sit untracked in `git status` and the root .gitignore does not mention .idea at all; `.claude/` is also untracked and unignored. Every contributor's IDE will keep dirtying the tree. | Rejected (Out of scope for this review: the skill flags structural code-quality problems (abstractions, spaghetti branching, file size, layer boundaries), not repo hygiene in .gitignore. It is also not a 'magic) | Deferred (rejected in verification) |
| F221 | app/src/androidTest/java/dev/stan/yotsuba/SettingsFlowTest.kt:39 | 3 design cleanliness | low | The comment reads "Open Settings from the Boards gear", but the test never leaves the Home start destination; the gear it taps is HomeScreen's own (HomeScreen.kt:107-109, contentDescription from strings_home.xml:9 `home_settings` = "Settings"). BoardsScreen has a separate gear (AppNavHost.kt:190-194). The comment sends a future reader to the wrong screen while debugging. | Confirmed | Open |
| F222 | app/src/main/java/dev/stan/yotsuba/core/backup/BackupModule.kt:14 | 6 canonical layer | low | `@ApplicationScope` and its provider have exactly one consumer, BackupRepositoryImpl.kt:42 (`rg -n -w 'ApplicationScope' app/src/main`). Meanwhile MediaDownloadQueue.kt:33 and Updater.kt:52 each build their own `CoroutineScope(SupervisorJob() + Dispatchers.IO)` for the same 'process-lifetime work no screen owns' purpose. The abstraction exists but is not the canonical path; two of three singletons | Confirmed | Open |
| F223 | app/src/main/java/dev/stan/yotsuba/core/database/dao/DownloadedMediaDao.kt:3 | 4 magic/wrappers | low | An identical 11-line import block has been copy-pasted into five DAO files — BookmarkDao.kt:3-13, DownloadedMediaDao.kt:3-13, HiddenThreadDao.kt:3-13, HistoryDao.kt:3-13, SavedMediaDao.kt:3-13 — regardless of what each file uses. DownloadedMediaDao is the extreme case: it declares one query and imports eleven symbols, nine of which (Insert, OnConflictStrategy, Transaction, BookmarkEntity, HiddenTh | Confirmed | Open |
| F224 | app/src/main/java/dev/stan/yotsuba/core/database/dao/DownloadedMediaDao.kt:17 | 3 design cleanliness | low | The legacy `downloaded_media` table, `DownloadedMediaEntity`, `DownloadedMediaDao.allOnce()` and `YotsubaDatabase.downloadedMediaDao()` are kept alive solely by `VaultLegacyMigration` (VaultLegacyMigration.kt:37,52), a one-shot gated by the `vault_legacy_migrated_v1` preference (MediaVaultRepositoryImpl.kt:55). Not dead yet, but every DAO file imports `DownloadedMediaEntity` (HiddenThreadDao.kt:9, | Rejected (The cited line (DownloadedMediaDao.kt:17) is the one place the entity IS used; the only actionable part — unused `DownloadedMediaEntity` imports — lives at line 9 of four other files, and those files ) | Deferred (rejected in verification) |
| F225 | app/src/main/java/dev/stan/yotsuba/core/dedup/DHash.kt:64 | 4 magic/wrappers | low | The bit-emitting loop is `for (y in 0 until ROWS) { for (x in 0 until ROWS) { ... cell[y * COLS + x] vs cell[y * COLS + x + 1] ... } }`. The inner loop walks *columns* — it must run `COLS - 1` = 8 times — but is bounded by `ROWS`. It is correct only because ROWS (8) happens to equal COLS - 1 (8). Change the grid to 17x16 for a 256-bit hash and the hash silently becomes wrong rather than failing to | Confirmed | Open |
| F226 | app/src/main/java/dev/stan/yotsuba/core/designsystem/component/MediaThumbnail.kt:5 | 3 design cleanliness | low | `import androidx.compose.foundation.layout.fillMaxSize` is unused — the file's three branches use `modifier.background(...)` (lines 31-32, 41-42, 55) and never call `fillMaxSize`. CLAUDE.md states lint findings are treated as real, and this is a standard `UnusedImports` hit. | Confirmed | Open |
| F227 | app/src/main/java/dev/stan/yotsuba/core/designsystem/component/StateViews.kt:57 | 5 type/boundary | low | `UiStateContent` accepts a `modifier` and forwards it to `LoadingSkeleton` (line 61) and `ErrorState` (line 62), but the `UiState.Success` branch calls `content(state.data)` (line 63) and drops it. A caller that passes `modifier = Modifier.padding(16.dp)` gets padding while loading and while erroring, and none once data arrives — a silent, layout-shifting inconsistency in the shared screen shell.  | Confirmed | Open |
| F228 | app/src/main/java/dev/stan/yotsuba/core/designsystem/component/StateViews.kt:113 | 5 type/boundary | low | `SearchField(..., hintRes: Int, ...)` types a string-resource id as a bare `Int` and passes it straight to `stringResource(hintRes)` (line 119). Nothing stops a caller passing a drawable id, a plurals id, or an arbitrary integer; the failure surfaces at runtime as a `NotFoundException` or a wrong string. `ChipRow` in SettingsRows.kt takes real `String`s, so the module is inconsistent about this bo | Confirmed | Open |
| F229 | app/src/main/java/dev/stan/yotsuba/core/designsystem/MotionSpecs.kt:47 | 4 magic/wrappers | low | `LazyItemScope.animatedListItem()` (lines 36-44) and `LazyGridItemScope.animatedGridItem()` (lines 48-56) have byte-identical bodies — same reduced-motion guard, same `motion.medium`/`motion.medium`/`motion.short` triple — differing only in the receiver type. Retuning the list-item feel means remembering to edit both, and nothing enforces that. | Rejected (The duplication is real but trivial (three tween lines against two different scope receivers), and the proposed fix makes the code less legible, not more: a nullable Triple<FiniteAnimationSpec<Float>,) | Deferred (rejected in verification) |
| F230 | app/src/main/java/dev/stan/yotsuba/core/designsystem/theme/Color.kt:21 | 5 type/boundary | low | `LocalYotsubaColors`'s default block (lines 21-28) is a second, independently-maintained definition of the same four semantic colours that `yotsubaColors()` (lines 30-35) computes, and the two disagree on every field. The default sets `greentext = SeedColor` (#789922), a value the function never produces (#5E7A16 light, #A6C761 dark); `quotelink = Color(0xFF3D6B99)` versus `scheme.primary`; `spoil | Confirmed | Open |
| F231 | app/src/main/java/dev/stan/yotsuba/core/designsystem/theme/Theme.kt:104 | 3 design cleanliness | low | Lines 104-106 provide `LocalSpacing provides Spacing()`, `LocalElevation provides Elevation()`, `LocalMotion provides Motion()`. Each of those locals is declared as `staticCompositionLocalOf { Spacing() }` / `{ Elevation() }` / `{ Motion() }` (Spacing.kt:18, Elevation.kt:15, Motion.kt:13) — the provided value is the byte-for-byte same expression as the local's own default, since all three data cla | Confirmed | Open |
| F232 | app/src/main/java/dev/stan/yotsuba/core/di/DispatchersModule.kt:14 | 5 type/boundary | low | `@Provides fun computeDispatcher(): CoroutineDispatcher = Dispatchers.Default` binds the bare `CoroutineDispatcher` type in SingletonComponent with no qualifier. It has exactly one consumer, VaultViewModel.kt:241's `compute` parameter. Any future `CoroutineDispatcher` constructor parameter anywhere in the graph — an IO one, most obviously — silently resolves to `Dispatchers.Default` with no compil | Confirmed | Open |
| F233 | app/src/main/java/dev/stan/yotsuba/core/media/SoundPost.kt:37 | 4 magic/wrappers | low | ``` repeat(MAX_DECODE_PASSES) {     val next = percentDecode(decoded)     if (next == decoded) return@repeat     decoded = next } ``` `return@repeat` returns from the lambda, i.e. it is `continue`, not `break` — the comment above it ("decode until the string stops changing") reads as an early exit that does not exist. All three passes always run. It happens to produce the right answer only because | Confirmed | Open |
| F234 | app/src/main/java/dev/stan/yotsuba/core/network/ArchiveHosts.kt:49 | 2 spaghetti growth | low | `ArchiveSource.WAROSU` is a domain enum value (ThreadPost.kt:47, doc: "has no JSON API, so nothing reads from it yet") that `sourceFor` (245) resolves for 13 boards including /g/ and /biz/, after which `apiUrl` returns null (261) and ThreadRepositoryImpl.archivedThread (line 36) turns it into `NetworkError.NotFound` -- the same result as "no archive". The enum value adds a `when` arm and three tes | Rejected (ArchiveHosts.kt is 51 lines long, so line 232 does not exist and the nearby real code (the WAROSU arm at line 49) does not support the claim. The file is a single small lookup table with one two-arm `) | Deferred (rejected in verification) |
| F235 | app/src/main/java/dev/stan/yotsuba/core/network/NetworkMonitor.kt:49 | 4 magic/wrappers | low | `val metered: Flow<Boolean> = status.map { it.isMetered }.distinctUntilChanged()` has no callers: a grep over app/src finds only `networkMonitor.status` (CatalogViewModel.kt:53, MediaViewModel.kt:149) and `networkMonitor.current()` (Modules.kt:90, VaultSyncWorker.kt:55). It is a pass-through over a property the enum already exposes (NetworkStatus.isMetered, line 21), and it is dead. | Confirmed | Open |
| F236 | app/src/main/java/dev/stan/yotsuba/core/update/GithubReleases.kt:70 | 5 type/boundary | low | okhttp is pinned at 5.5.0 (gradle/libs.versions.toml:11), where `Response.body` is non-null. Line 70 asserts it anyway (`body!!.string()`) while Updater.kt:127 treats the same property as nullable (`val body = resp.body ?: throw ReleaseException("empty response")`, a branch that can never be taken). Two files disagree about the same contract, and both encode the okhttp-4 shape. | Confirmed | Open |
| F237 | app/src/main/java/dev/stan/yotsuba/core/util/Urls.kt:23 | 6 canonical layer | low | Protocol-relative URL normalisation `if (x.startsWith("//")) "https:$x" else x` appears in Urls.parseInternal L23, Urls.domainOf L43, PostHtmlParser.resolveUrl L136 and SoundPost.toUrl L45. | Rejected (Duplicate of F102 — same duplication, same four sites, same proposed fix, just cited at Urls.kt:23 instead of :43 and with a weaker write-up. Reporting the same extraction twice is exactly the nit-flo) | Deferred (rejected in verification) |
| F238 | app/src/main/java/dev/stan/yotsuba/core/vault/VaultMeta.kt:32 | 3 design cleanliness | low | The KDoc says "Replaces any entry with the same fileName, keeps order stable", but the body is `val kept = files.filterNot { it.fileName == entry.fileName }; return copy(files = kept + entry)` — an update moves the entry to the end of the list. Every re-save of an existing file (MediaVaultRepositoryImpl.kt:217, 541, 588 all call `upsert` on entries that already exist) reorders meta.json and shuffl | Confirmed | Open |
| F239 | app/src/main/java/dev/stan/yotsuba/core/vault/VaultPosts.kt:124 | 3 design cleanliness | low | VaultPostsCodec (VaultPosts.kt L124-136) and VaultMetaCodec (VaultMeta.kt L67-79) are the same object twice: identical `Json { ignoreUnknownKeys = true; prettyPrint = true; encodeDefaults = false }`, an encode, and a `runCatching { decodeFromString }.getOrNull()` decode. Same with the two transparent search TextFields (HistoryList.HistorySearchField L144-156 vs VaultScreen.SearchTopBar L535-545) a | Confirmed | Open |
| F240 | app/src/main/java/dev/stan/yotsuba/core/vault/VideoStills.kt:35 | bug | low | `val scaled = shrink(frame)` (line 35) is compressed to JPEG and then neither `frame` nor `scaled` is recycled; the `retriever` is released in `finally` but the bitmaps are not. `shrink` returns `frame` itself when the frame is already small (line 53), so a naive `frame.recycle()` would be wrong for the scaled case only. The frames are full video resolution (up to a few MB each) and `capture` is c | Confirmed | Open |
| F241 | app/src/main/java/dev/stan/yotsuba/core/widget/WatchedThreadsWidget.kt:125 | 6 canonical layer | low | `sizeBucket(size: DpSize): WidgetSizeBucket` is a pure function that lives in the Glance file, while its other half — `WidgetSizeBucket` and `rowLimitFor(bucket)` — lives in WidgetRows.kt with the comment "Pure data so the ordering can be tested on the JVM and the Glance composable stays a dumb renderer." The size→bucket decision is exactly the same kind of pure rule, but it is stranded in the fil | Confirmed | Open |
| F242 | app/src/main/java/dev/stan/yotsuba/core/widget/WatchedThreadsWidget.kt:174 | ui/ux | low | The header's Refresh control is a `Text` at 12.sp with `.padding(horizontal = 6.dp, vertical = 2.dp).clickable(...)` — roughly a 20dp-tall tap target on a home screen, well under the 48dp minimum, and it is styled as coloured text with no button affordance beyond colour. The header also renders unconditionally before the empty check (line 145 precedes line 146), so a widget with nothing watched re | Confirmed | Open |
| F243 | app/src/main/java/dev/stan/yotsuba/core/work/BookmarkRefreshWorker.kt:73 | 2 spaghetti growth | low | notify() gates one thing — may we post — three times, at three points in the function: areNotificationsEnabled() at 51, the SDK/POST_NOTIFICATIONS check at 73-76, and the SecurityException catch at 79-81. The middle check runs *after* the channel is created (52-58), the launch PendingIntent is built (59-61), strings are resolved (62-64) and the Notification is assembled (65-71), all of which is th | Confirmed | Open |
| F244 | app/src/main/java/dev/stan/yotsuba/core/work/VaultSyncWorker.kt:83 | 3 design cleanliness | low | WorkManagerVaultSyncScheduler L83-100 is a copy of WorkManagerBookmarkRefreshScheduler (core/work/BookmarkRefreshScheduler.kt L23-41): same isInitialized guard, same PeriodicWorkRequestBuilder + CONNECTED constraint, same KEEP enqueue; only the worker class, interval and unique name differ. Two interfaces (BookmarkRefreshScheduler, VaultSyncScheduler) with one identical method. | Confirmed | Open |
| F245 | app/src/main/java/dev/stan/yotsuba/data/repository/BookmarkRepositoryImpl.kt:29 | 5 type/boundary | low | `internal var clock: () -> Long = System::currentTimeMillis` is mutable state on a `@Singleton`, existing only so tests can swap it; `BackupRepositoryImpl.kt:59-60` does the same twice (`internal var rootOverride: File?`, `internal var ioDispatcher: CoroutineDispatcher`). The package already contains the clean answer to this problem: `VaultStore` (VaultStore.kt:23-25) takes the seam as a primary-c | Confirmed | Open |
| F246 | app/src/main/java/dev/stan/yotsuba/data/repository/Mappers.kt:71 | 4 magic/wrappers | low | The same three-line derivation is copy-pasted at lines 71-73 and 130-132: `body.segments.mapNotNull { (it.annotation as? PostAnnotation.QuotelinkSameThread)?.postNo }.distinct()`. It is a pure function of `PostText`, and `PostText` (PostAnnotations.kt:55-60) already hosts exactly this kind of derived accessor as `plainText`. Every consumer of `ThreadPost.quotedPostNos` (PostGraph.kt:28, 32, 62, 10 | Confirmed | Open |
| F247 | app/src/main/java/dev/stan/yotsuba/data/repository/Mappers.kt:168 | 4 magic/wrappers | low | `archiveCommentToHtml` (added by 98a1729) compiles `Regex("&gt;&gt;(\\d+)")` inside the `joinToString` lambda, so a fresh Regex is built for every line of every post in an archived thread — and the file already demonstrates the right pattern seven lines above at line 161, where `ARCHIVE_QUOTELINK` is hoisted to a top-level `private val`. Two spellings of the same pattern also now have to be kept i | Confirmed | Open |
| F248 | app/src/main/java/dev/stan/yotsuba/data/repository/Mappers.kt:240 | 5 type/boundary | low | `HistoryEntity.toDomain()` (240-248) and `HistoryEntry.toEntity()` (250-258) omit `maxReadPostNo`, so HistoryRepositoryImpl.kt:63-68 adds two private wrappers `toDomainWithReadMark`/`toFullEntity` that `.copy(maxReadPostNo = maxReadPostNo)` back on. The comment says the drop is "on purpose for callers that only record visits", but `record` goes through `dao.record` which never writes maxReadPostNo | Confirmed | Open |
| F249 | app/src/main/java/dev/stan/yotsuba/data/repository/MediaVaultRepositoryImpl.kt:375 | 4 magic/wrappers | low | Two overrides exist where one would do: `syncSavedThreads(onProgress) = syncSavedThreads(onProgress, emptySet())` at lines 375-377 is a pure pass-through to the override on 379. The interface makes it worse — MediaVaultRepository.kt:88-91 declares the 2-arg form with a *default body* `= syncSavedThreads(onProgress)` that silently drops `skip`, so any implementor overriding only the 1-arg form (as  | Rejected (The observation is right — MediaVaultRepository.kt:88-91 gives the 2-arg form a default body that silently drops `skip` — but the proposed fix does not compile as claimed. Roughly ten fakes override o) | Deferred (rejected in verification) |
| F250 | app/src/main/java/dev/stan/yotsuba/data/repository/SavedMediaMappers.kt:99 | 5 type/boundary | low | `VaultLocation(board = board!!, threadNo = threadNo!!)` uses double-bang inside a branch that has already proven both non-null on the line above (`if (board != null && board != VaultPaths.UNSORTED_DIR_NAME && threadNo != null)`). `SavedMediaEntity` is a `data class` with `val board: String?` / `val threadNo: Long?` (Entities.kt:66-67) in the same Gradle module, so Kotlin smart-casts them and the ` | Rejected (Duplicate of F118 at the same expression (SavedMediaMappers.kt:98-99) with the same fix; on its own, dropping two !! is a taste-level nit that the rules would not flag separately.) | Deferred (rejected in verification) |
| F251 | app/src/main/java/dev/stan/yotsuba/domain/model/MediaItem.kt:29 | 3 design cleanliness | low | `hasSound` has no readers: `rg -n -w hasSound app/src` returns only the declaration; MediaFeedViewer.kt:272 does `current?.soundUrl != null` directly. `isAnimated` (line 32) is referenced only from MappersTest.kt:107-113, never from main. `MediaViewModel`'s `isSaved(url)` (MediaViewModel.kt:75) is likewise test-only (MediaViewModelTest.kt:296/303); main code uses `savedPath`/`saved` directly. | Confirmed | Open |
| F252 | app/src/main/java/dev/stan/yotsuba/domain/model/PostGraph.kt:15 | 4 magic/wrappers | low | The secondary constructor at l.15-17, `constructor(byNo: Map<Long, ThreadPost>, backlinks: Map<Long, List<Long>>) : this(byNo.values.toList(), backlinks)`, is documented as "Map form kept for the media viewer; iteration order of the map is the thread order" — but the media viewer does not use it. Every construction site in the repo passes a List: feature/media/ViewerThread.kt:23 `PostGraph(posts,  | Confirmed | Open |
| F253 | app/src/main/java/dev/stan/yotsuba/domain/repository/BookmarkRepository.kt:41 | 5 type/boundary | low | `refreshAll(...) = BookmarkRefreshSummary()`, `setPinned(...) {}` and `removeDead() {}` have no-op defaults in the interface (lines 41-42, 54, 57) even though BookmarkRepositoryImpl overrides all three and production calls them (BookmarkRefreshWorker, bookmarks screen). Same silent-fallback pattern as MediaVaultRepository: a fake that omits `removeDead` makes the test pass while doing nothing. His | Confirmed | Open |
| F254 | app/src/main/java/dev/stan/yotsuba/domain/repository/MediaVaultRepository.kt:114 | 5 type/boundary | low | ade3157 added two more repository methods with bodies on the interface that quietly fail: line 114 `suspend fun renameThread(...): VaultError? = VaultError.Io("not supported")` and line 122 the same for `mergeThreads`. There is exactly one production implementor (MediaVaultRepositoryImpl), so these defaults exist only so test fakes need not be updated — at the cost of an interface that promises an | Rejected (Real but duplicative: renameThread/mergeThreads are two of the eleven defaults already itemised at the same lines by F033, whose fix (abstract members plus a typed VaultError.Unsupported) is this find) | Deferred (rejected in verification) |
| F255 | app/src/main/java/dev/stan/yotsuba/feature/bookmarks/BookmarksList.kt:101 | 6 canonical layer | low | VaultError -> R.string mapping (NoAccess->vault_error_no_access, NotFound->vault_error_not_found, Io->vault_error_io) exists in BookmarksList L100-105 (three pre-resolved strings) and DownloadAction.errorLabel (feature/media/DownloadAction.kt L140-144). | Confirmed | Open |
| F256 | app/src/main/java/dev/stan/yotsuba/feature/bookmarks/BookmarksList.kt:111 | 3 design cleanliness | low | The comment says "Auto-refresh whenever the tab comes back on screen (throttled in the ViewModel), so the pills update without a manual pull", and BookmarksViewModel.kt:73-75 says "Fired every time the screen comes on screen … at most once a minute so tab-hopping doesn't hammer the API". Neither is what happens. `OnResumeEffect` fires on a `Lifecycle.Event.ON_RESUME`, which the Activity emits only | Rejected (Misreads the code. OnResumeEffect wraps LifecycleResumeEffect, whose block runs when the effect enters composition while the owner is already RESUMED (the lifecycle observer is replayed to the current) | Deferred (rejected in verification) |
| F257 | app/src/main/java/dev/stan/yotsuba/feature/bookmarks/BookmarksList.kt:190 | ui/ux | low | `BookmarkActionSheet` hardcodes spacing that the tokens already name: `Column(Modifier.padding(bottom = 24.dp))` (line 190), `padding(horizontal = 24.dp, vertical = 8.dp)` (line 195) — `Spacing.xl` is 24.dp and `Spacing.sm` is 8.dp (token/Spacing.kt:13-14) — and `Arrangement.spacedBy(4.dp)` at line 326 where `Spacing.xs` is 4.dp. The same file reads `LocalSpacing.current` at lines 85 and 307 for i | Confirmed | Open |
| F258 | app/src/main/java/dev/stan/yotsuba/feature/bookmarks/BookmarksViewModel.kt:33 | 5 type/boundary | low | Refresh progress crosses the ViewModel/UI boundary as `val checking: Pair<Int, Int>?` — "Non-null while a refresh pass is running: boards done / boards total". `onRefreshAll` seeds it with the meaningless `0 to 0` (line 87) before the repository reports a total, which forces the consumer to paper over that state: `BookmarksList.kt:239-240` destructures the pair and then guards `if (total > 0)`. Th | Confirmed | Open |
| F259 | app/src/main/java/dev/stan/yotsuba/feature/media/MediaScreen.kt:184 | ui/ux | low | While a share is being prepared the IconButton is `enabled = !sharing` and its content swaps to a bare `CircularProgressIndicator(Modifier.size(24.dp), …)` with no contentDescription (:184-188). A disabled IconButton with unlabelled content leaves the control with no accessible name for the duration of the download, so a screen-reader user loses the button they just pressed. The vault viewer's sha | Confirmed | Open |
| F260 | app/src/main/java/dev/stan/yotsuba/feature/media/VideoPage.kt:112 | 6 canonical layer | low | VideoPage builds an ExoPlayer L112-118 exactly as rememberSoundPlayer does in SoundTrack.kt L24-31 (`ExoPlayer.Builder(context).build().apply { setMediaItem(fromUri); repeatMode = REPEAT_MODE_ONE; prepare() }`), and both install the same LifecycleEventObserver (VideoPage L153-165, SoundTrack L45-57: ON_STOP -> playWhenReady = false, ON_START -> playWhenReady = shouldPlay.value). | Rejected (The lifecycle-observer half is the same defect as F161 (already confirmed); what is left is a six-line ExoPlayer.Builder block, and the proposed rememberLoopingPlayer(uri: String?) returns a nullable ) | Deferred (rejected in verification) |
| F261 | app/src/main/java/dev/stan/yotsuba/feature/settings/sections/FiltersSection.kt:80 | 3 design cleanliness | low | Two fully-qualified references sit in a file with 40 explicit imports: `androidx.compose.runtime.key(filter.id)` at line 80 and `horizontalArrangement = androidx.compose.foundation.layout.Arrangement.spacedBy(spacing.sm)` at line 236. No other file in the slice does this — the neighbouring `Row`, `Column`, `Spacer` and `Modifier` are all imported normally at lines 4-10. Line 236 in particular push | Confirmed | Open |
| F262 | app/src/main/java/dev/stan/yotsuba/feature/settings/sections/FiltersSection.kt:158 | 3 design cleanliness | low | `filter.error?.let { Text(stringResource(R.string.filters_invalid_regex), …) }` discards `it` — the captured error message — and prints a fixed generic string, while `FilterDialog` at line 209 shows the real text via `supportingText = regexError?.let { { Text(it, …) } }`. The row therefore tells the user a pattern is broken without saying how, and the unused lambda parameter is a lint warning wait | Confirmed | Open |
| F263 | app/src/main/java/dev/stan/yotsuba/feature/settings/UpdatesSection.kt:30 | 3 design cleanliness | low | `UpdatesSection` is the only section body that does not live in `feature/settings/sections/`. The other eight (AboutSection, AppearanceSection, BoardsSection, FiltersSection, LinksSection, MediaSection, ReadingSection, StorageSection) are all in that package and are imported one-per-line at SettingsSectionScreen.kt:33-40, while `SettingsSectionId.UPDATES -> UpdatesSection(...)` at line 115 resolve | Confirmed | Open |
| F264 | app/src/main/java/dev/stan/yotsuba/feature/settings/UpdatesSection.kt:33 | 3 design cleanliness | low | `onInstall: (dev.stan.yotsuba.core.update.Release) -> Unit` spells the type fully qualified in the signature even though `dev.stan.yotsuba.core.update.Updater` is imported two lines above (line 22). Separately, this file sits at feature/settings/UpdatesSection.kt while the other eight section bodies live in feature/settings/sections/ — SettingsSectionScreen imports seven siblings from `…settings.s | Confirmed | Open |
| F265 | app/src/main/java/dev/stan/yotsuba/feature/thread/components/ThreadGallerySheet.kt:62 | ui/ux | low | `items(posts, key = { it.no }) { post -> val media = post.presentMedia ?: return@items ... }` emits an empty grid cell for any post without present media, leaving a hole in the grid. The caller passes `s.mediaPosts` (ThreadScreen.kt:441), which ThreadViewModel.kt:160 already filters to `presentMedia != null`, so the guard is dead — but if it ever fires the user sees gaps rather than a tighter grid | Confirmed | Open |
| F266 | app/src/main/java/dev/stan/yotsuba/feature/thread/ThreadScreen.kt:230 | 6 canonical layer | low | `UiStateContent(state, onRetry = { viewModel.load() })` retries through the cache, while CatalogPane.kt:154 retries with `viewModel::retry`, which `CatalogViewModel.retry()` (CatalogViewModel.kt:75) defines as `load(forceRefresh = true, showLoading = true)` with the comment "bypass the cache like pull-to-refresh, but show the loading shell". Two screens, the same error surface, two different meani | Confirmed | Open |
| F267 | app/src/main/java/dev/stan/yotsuba/feature/thread/ThreadScreen.kt:309 | 3 design cleanliness | low | Lines 309-334 are two copies of the same banner — same `labelMedium` style, same `onSurfaceVariant` colour, same `fillMaxWidth().background(surfaceVariant).padding(spacing.sm)` modifier chain — differing only in which string they resolve. Twenty-four lines of the screen's top-level layout are consumed by a chunk that carries one bit of information (offline copy vs archived). | Confirmed | Open |
| F268 | app/src/main/java/dev/stan/yotsuba/feature/thread/ThreadScreen.kt:415 | 6 canonical layer | low | Plain-text share intent (`Intent(ACTION_SEND).apply { type = "text/plain"; putExtra(EXTRA_TEXT, ...) }; startActivity(createChooser(...))`) is duplicated in ThreadScreen L416-420 and ThreadTopBar L131-135 (the top-bar copy also lacks the runCatching the screen copy has). feature/media/MediaShare.kt already owns the file-share sibling; VaultShare.kt L20 and MediaShare.kt L38 both spell `context.pac | Confirmed | Open |
| F269 | app/src/main/java/dev/stan/yotsuba/feature/thread/ThreadViewModel.kt:89 | 5 type/boundary | low | 2b7dd4e folded the thread's session flows into one state — a good move — but exposed the mutable holder: line 88-89 `/** Exposed for tests: every change to it is one atomic emission. */ val session = MutableStateFlow(Session())`. It is public and writable, so any composable holding the ViewModel can call `viewModel.session.value = …` and bypass every invariant the VM's `on*` methods enforce (e.g.  | Confirmed | Open |
| F270 | app/src/main/java/dev/stan/yotsuba/feature/thread/ThreadViewModel.kt:290 | 6 canonical layer | low | recordHistory L290-300 and onToggleBookmark L329-345 both derive the same row fields from the OP: `subject = op.subject, opExcerpt = op.body.plainText.take(200), thumbnailUrl = op.presentMedia?.thumbnailUrl`. Meanwhile domain has three copies of `displayTitle = subject ?: opExcerpt.take(60).ifBlank { ... }` (Bookmark.kt L38, HistoryEntry.kt L15, CatalogThread.kt L18) and MediaVaultRepositoryImpl.w | Rejected (Thin-abstraction nit, not a rule-6 violation. The ViewModel repetition is three field assignments at two sites in one file, constructing two different domain types; a ThreadSummary value object would ) | Deferred (rejected in verification) |
| F271 | app/src/main/java/dev/stan/yotsuba/feature/thread/ThreadViewModel.kt:599 | ui/ux | low | ``` val visible = visiblePosts(details.posts, session).mapTo(HashSet()) { it.no } val nodes = PostGraph.of(details).tree().filter { it.post.no in visible && ... } ``` The tree is built over all posts and then filtered, but each surviving node keeps the `depth` it had in the unfiltered tree. With a poster-ID filter active (`onFilterPosterId`, line 405), a reply whose parent was filtered out still r | Confirmed | Open |
| F272 | app/src/main/java/dev/stan/yotsuba/feature/vault/VaultExplorer.kt:203 | ui/ux | low | The search branch handles its empty case explicitly (145-151, "vault_search_empty"), but the thread grid (201-211) and the Recent grid (172-183) render `MediaGrid` unconditionally. Set the type filter to Videos inside an image-only thread and the screen is a chip row above a blank area, with the shuffle FAB hidden too (VaultScreen.kt:355) — nothing says the filter is what emptied it. | Confirmed | Open |
| F273 | app/src/main/java/dev/stan/yotsuba/feature/vault/VaultExplorer.kt:233 | 6 canonical layer | low | "N items · <size>" is assembled three times: BoardList L233-234, ThreadList L279-281 (plus ' · date'), VaultScreen.vaultSubtitle L730-731; MergeDialog L502 does the count alone. VaultDedupSheet.Thumb L290-294 re-derives the thumbnail model (`!isVideo -> File(path); thumbnailPath != null -> File(thumbnailPath); else null`) that MediaThumb L558-562 already encodes for VaultEntry. | Confirmed | Open |
| F274 | app/src/main/java/dev/stan/yotsuba/feature/vault/VaultExplorer.kt:247 | 2 spaghetti growth | low | Commit 2619e61 ("Animate list changes") added `animatedListItem()` / `animatedGridItem()` to the vault's three lists. Three commits later, 06914f1 ("Scroll the vault lists back to the top when sort, direction or filter changes") silently reverted all three — `modifier = animatedListItem().clickable {...}` became `Modifier.clickable {...}` (now VaultExplorer.kt:251 in `BoardList`, :330 in `ThreadLi | Confirmed | Open |
| F275 | app/src/main/java/dev/stan/yotsuba/feature/vault/VaultExplorer.kt:612 | 6 canonical layer | low | Five independent DateFormat spellings for the same job: VaultExplorer L612 and VaultStatsSheet L217 both declare `private val dateFormat = DateFormat.getDateInstance(DateFormat.MEDIUM)`; VaultDedupSheet L311 calls `DateFormat.getDateInstance(DateFormat.SHORT)` inside a composable per row; ThreadScreen L311 `DateFormat.getDateInstance()`; StorageSection L145-147 `getDateTimeInstance(MEDIUM, SHORT)` | Rejected (Misreads the code. The five call sites are not "the same job": DateFormat.SHORT (VaultDedupSheet:311), the default style (ThreadScreen:311) and getDateTimeInstance(MEDIUM, SHORT) (StorageSection:146) ) | Deferred (rejected in verification) |
| F276 | app/src/main/java/dev/stan/yotsuba/feature/vault/VaultScreen.kt:412 | 7 orchestration/atomicity | low | Tapping a thread in the statistics sheet (ca18442) does `viewModel.openBoard(location.board)` then `viewModel.openThread(location)` on consecutive lines 412-413. `openBoard` sets `selection` to `VaultSelection(board = board)` (thread = null) and `openThread` then copies `thread` in — two separate `MutableStateFlow` writes, so `uiState` emits an intermediate state at the board level before the thre | Confirmed | Open |
| F277 | app/src/main/java/dev/stan/yotsuba/feature/vault/VaultScreen.kt:735 | ui/ux | low | `private fun defaultImportName(count: Int): String = if (count > 0) "Imported ($count)" else "Imported"` — a hardcoded English string that becomes the persisted thread directory name and the title shown in the thread list. Every other user-visible string on this screen goes through `stringResource` (118-130, 245, 254, ...), and the vault has a localized label for its sibling cases (`R.string.vault | Confirmed | Open |
| F278 | app/src/main/java/dev/stan/yotsuba/feature/vault/VaultStatsSheet.kt:67 | 3 design cleanliness | low | `items(stats.perBoard, key = { it.board }) { BoardBar(it, stats.perBoard.first().bytes) }` recomputes `stats.perBoard.first()` inside every row's composition to obtain the same scale denominator, and `BoardBar` then re-derives the fraction with a `if (largest > 0)` guard (VaultStatsSheet.kt:132). The guard is dead code by construction — `perBoard` is only reached when `!stats.isEmpty`, and a board | Rejected (Taste nit resting on a misread. perBoard is sorted bytes-descending (VaultStats.kt:47), so first().bytes is the max and the call is a trivial O(1) list head read per row, not a meaningful recomputatio) | Deferred (rejected in verification) |
| F279 | app/src/main/java/dev/stan/yotsuba/feature/vault/VaultViewModel.kt:475 | 4 magic/wrappers | low | `stateIn(viewModelScope, SharingStarted.WhileSubscribed(5_000), VaultStats.of(emptyList(), 0L))` builds the sentinel initial value by running the real aggregation over an empty list with a fabricated clock of `0L`. The `0L` is meaningless — `of` documents `now` as the end of the rolling week windows (VaultStats.kt:32-35) — and it only happens to be harmless because there are no entries to bucket.  | Confirmed | Open |
| F280 | app/src/main/java/dev/stan/yotsuba/navigation/AppNavHost.kt:116 | 3 design cleanliness | low | The tab-item loop is written twice with only the composable name changed: NavigationRail L116-124 (NavigationRailItem) and NavigationBar L137-145 (NavigationBarItem), each computing `isSelected(dest)`, icon = selected ? selectedIcon : unselectedIcon, label = stringResource(labelRes), onClick = navigateTopLevel(dest). | Rejected (The duplication is real at L114-124 and L136-146, but it is ~7 lines of declarative Compose slot wiring across two Material3 composables (NavigationRailItem/NavigationBarItem) that share no supertype ) | Deferred (rejected in verification) |
| F281 | app/src/main/java/dev/stan/yotsuba/navigation/ExternalLinks.kt:21 | 6 canonical layer | low | `text?.let { URL.findAll(it) }?.map { m -> Urls.parseInternal(...) }?.firstOrNull { it != null }` builds an intermediate `Sequence<InternalLink?>` and then filters nulls out of it by hand — three chained safe calls to express "first URL in the text that parses". Kotlin's canonical helper for exactly this is `firstNotNullOfOrNull`. | Confirmed | Open |
| F282 | app/src/main/res/drawable/ic_launcher_monochrome.xml:1 | 4 magic/wrappers | low | `ic_launcher_monochrome.xml` and `ic_launcher_foreground.xml` are byte-identical: same `108dp`/`108` viewport, same `android:fillColor="#FFFFFF"`, same 3-subpath `pathData`. Two files, one drawable — a themed-icon tweak to the glyph now has to be made twice or the launcher's themed icon silently diverges from its coloured one. | Confirmed | Open |
| F283 | app/src/test/java/dev/stan/yotsuba/network/NetworkLayerTest.kt:7 | 3 design cleanliness | low | `import dev.stan.yotsuba.core.util.Urls` — `grep -n "Urls" NetworkLayerTest.kt` returns only line 7, the import itself. Nothing in the file references `Urls`. CLAUDE.md states "lint errors are real crashes, not pedantry", so dead imports in the test sources are the kind of thing that should not be accumulating. | Rejected (The unused import at line 7 is real (grep finds `Urls` only on that line), but it is a cosmetic lint nit, not something this review's rules flag: no structural regression, no branching or abstraction ) | Deferred (rejected in verification) |
| F284 | app/src/test/java/dev/stan/yotsuba/vault/VaultSnapshotTest.kt:23 | 6 canonical layer | low | `diff <(sed -n '22,49p' VaultPruneTest.kt) <(sed -n '23,42p' VaultSnapshotTest.kt)` shows the only difference is VaultPruneTest's extra `savedDir` helper: the `@get:Rule val tmp`, `private val store by lazy { VaultStore(tmp.root) }`, the `post(no, quotes, isOp)` builder, the `/** OP 1; 2 -> 1; 3 -> 2; 4 stray; 5 -> 4; 6 -> 3. */ thread()` fixture and the `meta(dir)` decoder are byte-identical acro | Confirmed | Open |
| F285 | gradle/libs.versions.toml:27 | 4 magic/wrappers | low | Stale catalog entries: `retrofitSerialization = "3.0.0"` (line 28) duplicates `retrofit = "3.0.0"` for a module from the same artifact family; `okhttp-logging` (line 55) is declared but `HttpLoggingInterceptor` appears nowhere in app/src and the library is not in build.gradle.kts; `kotlin-android` plugin alias (line 75) is declared but neither build.gradle.kts applies it (AGP 9.3.2 uses built-in K | Confirmed | Open |
| F286 | gradle/libs.versions.toml:60 | 4 magic/wrappers | low | Two catalog entries are declared and referenced by nothing: `okhttp-logging` (line 60) — grepping `*.kts` for `okhttp.logging`/`okhttp-logging` outside the catalog returns no hits — and the `kotlin-android` plugin alias (line 81), which neither `build.gradle.kts` nor `app/build.gradle.kts` applies. Separately, `androidx-test-runner` (74) and `androidx-test-core` (75) inline `version = "1.7.0"` whi | Rejected (Duplicate of F285 — same okhttp-logging, kotlin-android and inline-test-version observations with no additional content; keeping both would double-report one issue.) | Deferred (rejected in verification) |
