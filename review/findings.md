# Findings ledger

Source of truth for the thermo-nuclear review of 2026-08-30. One row per finding.
`rule` is a rule number from the review SKILL.md, `bug`, or `ui/ux`. `verdict` is the Phase 2 result; `state` is the Phase 3 result.

| id | file:line | rule | severity | evidence | verdict | state |
|---|---|---|---|---|---|---|
| F001 | app/src/androidTest/java/dev/stan/yotsuba/CatalogFlowTest.kt:40 | bug | critical | openCatalog() starts with composeRule.waitForText(TestSeed.BOARD_TITLE) but never navigates off the start destination. AppNavHost.kt:160 sets startDestination = Route.Home; HomeScreen.kt:78-122 renders boards from settings.favouriteBoards, and Settings.kt:70 defaults favouriteBoards = emptySet(), so | Confirmed | Fixed |
| F002 | app/src/androidTest/java/dev/stan/yotsuba/HiddenThreadFlowTest.kt:31 | bug | critical | Same defect as CatalogFlowTest: the test opens with composeRule.waitForText(TestSeed.BOARD_TITLE) directly on the Home start destination, which shows only the "No favourite boards yet" empty state (HomeScreen.kt:118-124, strings_home.xml:4-6) because Settings.kt:70 defaults favouriteBoards to emptyS | Confirmed | Fixed |
| F003 | app/src/main/AndroidManifest.xml:23 | bug | critical | `android:allowBackup="true"` is set with no `android:dataExtractionRules` and no `android:fullBackupContent`; `app/src/main/res/xml/` contains only `file_paths.xml` and `watched_threads_widget_info.xml`. With targetSdk 37 that means Android Auto Backup ships the whole `databases/` and `datastore/` t | Confirmed | Fixed |
| F004 | app/src/main/java/dev/stan/yotsuba/core/database/dao/BookmarkDao.kt:33 | bug | critical | `markSeen` is `UPDATE bookmarks SET readUpTo = MAX(COALESCE(readUpTo, 0), :postNo) WHERE board = :board AND threadNo = :threadNo` — the WHERE clause has no "only when it rises" condition, so the row is written on every call even when the value is unchanged. `ThreadViewModel.raiseReadMark` (:532-539) | Confirmed | Fixed |
| F005 | app/src/main/java/dev/stan/yotsuba/core/designsystem/component/TabScaffoldSlots.kt:50 | bug | critical | `SideEffect { if (slots.owner !== token) { slots.owner = token; ... } }` runs after EVERY successful recomposition of `TabChrome`, not once per mount. The KDoc at line 33-34 claims "the newest caller wins", but nothing here encodes recency — it is last-writer-wins on each recomposition. During a tab | Confirmed | Fixed |
| F006 | app/src/main/java/dev/stan/yotsuba/core/text/PostHtmlParser.kt:150 | bug | critical | decodeEntity does `entity.drop(2).toIntOrNull(16)?.let { String(Character.toChars(it)) }` (line 150) and `entity.drop(1).toIntOrNull()?.let { String(Character.toChars(it)) }` (line 152). `Character.toChars` throws IllegalArgumentException for any int that is not a valid code point (negative, or > 0x | Confirmed | Fixed |
| F007 | app/src/main/java/dev/stan/yotsuba/data/repository/MediaVaultRepositoryImpl.kt:564 | bug | critical | `rescan()` rebuilds every row via `savedMediaEntity(meta, f, file)` (SavedMediaMappers.kt:42-60) and then calls `savedMediaDao.replaceAll(rebuilt)`, which is `clearAll()` + `insertAll()` (SavedMediaDao.kt:57-62). The sidecar-based mapper sets no `md5`, `phash` or `pixelSize`, and those columns defau | Confirmed | Fixed |
| F008 | app/src/main/java/dev/stan/yotsuba/feature/thread/ThreadScreen.kt:295 | 2 spaghetti growth | critical | The compose compiler report (app/build/compose_compiler/app-composables.txt:663-672) shows `restartable skippable ... fun PostCard(... stable actions: PostCardActions ...)`. Because `PostCardActions` is reported stable (app-classes.txt:1671), Compose skips PostCard only when the instance compares `e | Confirmed | Fixed |
| F009 | app/src/main/java/dev/stan/yotsuba/feature/thread/ThreadScreen.kt:354 | bug | critical | The LazyColumn key lambda contains escaped dollar signs that never interpolate: ``` is ThreadRow.MoreReplies -> "more-${'$'}{row.parentNo}" is ThreadRow.Filtered -> "filtered-${'$'}{row.postNo}" ``` `${'$'}` is a template expression yielding the literal character `$`; the following `{row.parentNo}`  | Confirmed | Fixed |
| F010 | app/src/main/java/dev/stan/yotsuba/feature/thread/ThreadScreen.kt:418 | bug | critical | Same escaping artifact in the share intent: `putExtra(Intent.EXTRA_TEXT, "${'$'}{Urls.threadWebUrl(board, threadNo)}#p${'$'}{post.no}")`. The shared text is the literal string `${Urls.threadWebUrl(board, threadNo)}#p${post.no}` — not a URL. "Share post link" from PostActionSheet (PostActionSheet.kt: | Confirmed | Fixed |
| F011 | app/src/main/java/dev/stan/yotsuba/feature/thread/ThreadViewModel.kt:520 | 6 canonical layer | critical | `onVisiblePostsChanged` rebuilds the ENTIRE row list from scratch on every visible-index change: `val rows = rows(details, session.value, filterVerdicts(details.posts, matcher.value))`. It is driven from ThreadScreen.kt:171-175, a `snapshotFlow { listState.firstVisibleItemIndex to ...visibleItemsInf | Confirmed | Fixed |
| F012 | app/src/main/java/dev/stan/yotsuba/feature/vault/VaultStatsSheet.kt:69 | bug | critical | `items(stats.biggestThreads, key = { it.location })` passes a `VaultLocation` — a plain `data class` (domain/model/VaultEntry.kt:13), neither Parcelable nor Serializable — as a LazyColumn item key. Compose routes every item key through `SaveableStateHolderImpl.SaveableStateProvider`, which does `req | Confirmed | Fixed |
| F013 | app/src/test/java/dev/stan/yotsuba/database/RoomTest.kt:28 | bug | critical | `YotsubaDatabase` is at `version = 10` and `core/di/Modules.kt:127` wires nine hand-written migrations (`MIGRATION_1_2` … `MIGRATION_9_10` in `core/database/Migrations.kt`), and schemas are exported to `app/schemas/dev.stan.yotsuba.core.database.YotsubaDatabase`. The only Room test in the repo is th | Rejected (Not a defect at line 28 — that line just builds an in-memory DB for DAO tests, which is exactly what it should do; the claim is a test-coverage gap, and the the) | Deferred (rejected in verification: Not a defect at line 28 — that line just builds an in-memory DB for DAO tests, which is exactly what it should do; the claim is a test-coverage gap, and the thermo-nuclear rules cover structure/abstraction/spaghetti, not missing tests. The proposed fix is also wrong as written: app/schemas/dev.stan.yotsuba.core.database.YotsubaDatabase contains only 6.json–10.json, so MigrationTestHelper.createDatabase(TEST_DB, n) for n in 1..5 (and the end-to-end 1→10) cannot work — no exported schema exists for those versions.) |
| F014 | app/src/androidTest/java/dev/stan/yotsuba/BoardsFavouriteFlowTest.kt:39 | 5 type/boundary | high | `val fake = settingsRepository as FakeSettingsRepository` — an unchecked downcast repeated in four files: here, CatalogFlowTest.kt:64, RevealSpoilersFlowTest.kt:36, SettingsFlowTest.kt:36. Each also declares `@Inject lateinit var settingsRepository: SettingsRepository` purely to cast it away. The ca | Confirmed | Fixed |
| F015 | app/src/androidTest/java/dev/stan/yotsuba/FlowTestHelpers.kt:10 | 2 spaghetti growth | high | All 12 instrumented test classes copy the identical 15-line preamble: `@get:Rule(order = 0) val hiltRule = HiltAndroidRule(this)`, `@get:Rule(order = 1) val composeRule = createAndroidComposeRule<MainActivity>()`, and `@Before fun setUp() { hiltRule.inject() }` — BoardsFlowTest.kt:25-34, BoardsFavou | Confirmed | Fixed |
| F016 | app/src/main/java/dev/stan/yotsuba/core/datastore/SettingsDataStore.kt:57 | bug | high | `migrateLegacy()` does `dataStore.edit { p -> p.clear(); p[blobKey] = ... }` on the single app-wide `user_preferences` DataStore (core/di/Modules.kt:68, :143). That store is shared: MediaVaultRepositoryImpl.kt:55 defines `VAULT_MIGRATED = booleanPreferencesKey("vault_legacy_migrated_v1")` and writes | Confirmed | Fixed |
| F017 | app/src/main/java/dev/stan/yotsuba/core/designsystem/component/SettingsRows.kt:42 | ui/ux | high | `SwitchRow` puts `Modifier.clickable { onToggle(!checked) }` on the Row (line 42) and also passes `onCheckedChange = onToggle` to the `Switch` (line 51). To TalkBack that is two separate focusable nodes: the Row announces "Reduce motion. Reduce animations… double tap to activate" with no on/off stat | Confirmed | Fixed |
| F018 | app/src/main/java/dev/stan/yotsuba/core/designsystem/ReducedMotion.kt:29 | 6 canonical layer | high | Three separate problems converge on this one 33-line file. (a) `rememberReducedMotion()` does `remember(context) { isReducedMotion(context) }` — a `Settings.Global.getFloat` ContentResolver lookup — at every call site instance. Call sites include `MotionSpecs.kt:38` (`animatedListItem`, invoked per  | Confirmed | Fixed |
| F019 | app/src/main/java/dev/stan/yotsuba/core/update/Updater.kt:205 | bug | high | statusReceiver's `else` branch retries whenever `allowSilent` is true, but `allowSilent` only records how the session was *created*, not whether the user was already shown the installer. Flow on any Android 12+ device where the silent update is declined: commit with USER_ACTION_NOT_REQUIRED -> STATU | Confirmed | Fixed |
| F020 | app/src/main/java/dev/stan/yotsuba/core/util/UiState.kt:32 | 7 orchestration/atomicity | high | `load()` does `if (showLoading) result.value = null; return scope.launch { result.value = fetch(forceRefresh) }` and keeps no reference to the previous job, so overlapping loads all run and the *last to complete* wins. CatalogViewModel calls `load()` in `init` (CatalogViewModel.kt:62) and exposes `l | Confirmed | Fixed |
| F021 | app/src/main/java/dev/stan/yotsuba/core/widget/WidgetDeepLink.kt:13 | 0 structural simplification | high | There are two independent mechanisms for the same concept — "an intent asked us to open something". `ShellViewModel.pendingLink` (a StateFlow of `Urls.InternalLink`, survives rotation by living in a ViewModel) and `WidgetDeepLink` (a process-wide `object` with its own MutableStateFlow and a bespoke  | Confirmed | Fixed |
| F022 | app/src/main/java/dev/stan/yotsuba/core/work/BookmarkRefreshScheduler.kt:24 | 0 structural simplification | high | WorkManagerBookmarkRefreshScheduler (BookmarkRefreshScheduler.kt:24-41) and WorkManagerVaultSyncScheduler (VaultSyncWorker.kt:84-99) are the same class twice: same @Singleton @Inject context, same `if (!WorkManager.isInitialized()) return` guard with the same comment ("Same guard as the bookmark sch | Confirmed | Fixed |
| F023 | app/src/main/java/dev/stan/yotsuba/core/work/BookmarkRefreshScheduler.kt:33 | bug | high | b4a6c33 added `Settings.bookmarkRefreshMinutes` (Settings.kt:63) and a user-facing chip row for it (ReadingSection.kt:44-50, options 15/30/60/180). Nothing reads it: `grep -rn bookmarkRefreshMinutes app/src/main` returns only the Settings field and the ReadingSection row. The scheduler built one com | Confirmed | Fixed |
| F024 | app/src/main/java/dev/stan/yotsuba/core/work/BookmarkRefreshWorker.kt:44 | bug | high | b4a6c33 added `Settings.bookmarkNotifications` (Settings.kt:65) with a switch in ReadingSection.kt:51-56 titled "Notify on new replies". The only consumer is ShellViewModel.kt:32, which uses it to decide whether to *ask for the POST_NOTIFICATIONS permission*. The worker that actually posts the notif | Confirmed | Fixed |
| F025 | app/src/main/java/dev/stan/yotsuba/data/repository/MediaVaultRepositoryImpl.kt:58 | 0 structural simplification | high | One class holds eleven unrelated concerns across 625 lines: storage-access state (69-85), save (95-144), snapshot read (146-156), delete (158-174), an in-memory trash state machine with its own `Trashed` type and unsynchronised `mutableMapOf` (176-231), MediaStore gallery export (233-285), local-fil | Confirmed | Fixed |
| F026 | app/src/main/java/dev/stan/yotsuba/data/repository/MediaVaultRepositoryImpl.kt:100 | 6 canonical layer | high | The thread directory is assembled by hand in four places: MediaVaultRepositoryImpl.save L100-105 (`File(File(store.root, sanitizeSegment(board)), threadDirName(...)).apply { mkdirs() }`), importLocalThread L299-302, VaultLegacyMigration.migrateMatched L106-109, VaultStore.snapshot L116-119 (and thre | Confirmed | Fixed |
| F027 | app/src/main/java/dev/stan/yotsuba/data/repository/MediaVaultRepositoryImpl.kt:531 | 4 magic/wrappers | high | `val fromMeta = store.updateMeta(from) { it }` uses a read-modify-write with an identity transform purely to *read* meta.json — it re-encodes and rewrites the file (VaultStore.kt:56-63 always calls `writeSidecar`) as a side effect of a read. The reason is that `VaultStore` exposes no read: the decod | Confirmed | Fixed |
| F028 | app/src/main/java/dev/stan/yotsuba/data/repository/MediaVaultRepositoryImpl.kt:541 | 7 orchestration/atomicity | high | Inside the per-file loop of `mergeThreads`, lines 541-542 call `store.updateMeta(into) { it.upsert(...) }` and `store.updateMeta(from) { it.remove(...) }` once per file. Each `updateMeta` is a full read-decode-transform-encode-write of a meta.json whose `files` list grows with N (VaultStore.kt:56-63 | Confirmed | Fixed |
| F029 | app/src/main/java/dev/stan/yotsuba/domain/model/Bookmark.kt:25 | 0 structural simplification | high | Three @Deprecated fields on the domain model are dead in practice. `lastSeenPostNo`: nothing ever writes a non-null value — BookmarkDao.markSeen (BookmarkDao.kt:33-37) only touches readUpTo, ThreadViewModel.kt:340 and BackupFile.kt:130 pass null, Mappers.kt:209/227 just round-trip the column. `newRe | Confirmed | Fixed |
| F030 | app/src/main/java/dev/stan/yotsuba/domain/repository/BoardRepository.kt:3 | 3 design cleanliness | high | Eight of the twelve repository interfaces carry a byte-identical 15-line import header (DataResult, Board, Bookmark, CatalogThread, HiddenThread, HistoryEntry, MediaItem, ImportSource, Settings, ThreadDetails, VaultSyncSummary, VaultEntry, VaultError, VaultSaveContext, Flow) that was clearly copy-pa | Confirmed | Fixed |
| F031 | app/src/main/java/dev/stan/yotsuba/domain/repository/BookmarkRepository.kt:48 | 5 type/boundary | high | Interface carries three dead/vestigial members: `markSeen(..., replyCount: Int)` whose doc says "[replyCount] is ignored; it stays for source compatibility" (impl line 223 drops it); `@Deprecated updateUnread` which has zero production callers (grep over main finds only the override at BookmarkRepos | Confirmed | Fixed |
| F032 | app/src/main/java/dev/stan/yotsuba/domain/repository/BookmarkRepository.kt:52 | 5 type/boundary | high | 71a3003 replaced the two unread counters with `readUpTo` but kept the whole old API alive as dead weight. `updateUnread` is a mandatory interface member whose only implementation is `= Unit` (BookmarkRepositoryImpl.kt:159) and which has zero production callers — every other occurrence is a no-op ove | Confirmed | Fixed |
| F033 | app/src/main/java/dev/stan/yotsuba/domain/repository/MediaVaultRepository.kt:14 | 5 type/boundary | high | MediaVaultRepository declares 20 members and 11 of them ship a default body: `storageAccess` (l.23), `refreshStorageAccess` (l.25), `trash` (l.47), `restoreTrashed` (l.50), `purgeTrash` (l.53), `exportToGallery` (l.59), `syncSavedThreads(onProgress, skip)` (l.91), `snapshotThread` (l.99), `snapshotT | Confirmed | Fixed |
| F034 | app/src/main/java/dev/stan/yotsuba/domain/repository/MediaVaultRepository.kt:88 | 5 type/boundary | high | Two overloads of the same operation differ only by a `skip: Set<VaultLocation>` parameter, and the two-arg default silently drops it: `suspend fun syncSavedThreads(onProgress: (done: Int, total: Int) -> Unit, skip: Set<VaultLocation>): VaultSyncSummary = syncSavedThreads(onProgress)` (l.88-91). The  | Confirmed | Fixed |
| F035 | app/src/main/java/dev/stan/yotsuba/feature/boards/BoardsViewModel.kt:88 | 5 type/boundary | high | Board visibility is stored as two independent sets that can contradict each other (`Settings.hiddenBoards`, `Settings.hiddenCategories`, Settings.kt:71-72 — read nowhere outside this file except a count in SettingsScreen.kt:86 and a bulk NSFW hide in SettingsViewModel.kt:103). Because there is no si | Confirmed | Fixed |
| F036 | app/src/main/java/dev/stan/yotsuba/feature/bookmarks/BookmarksList.kt:78 | 6 canonical layer | high | `BookmarksList(viewModel: BookmarksViewModel, …)` calls `viewModel.uiState.collectAsStateWithLifecycle()` at line 84, but its host has already collected the very same StateFlow at ThreadsScreen.kt:68 (`val bookmarks by bookmarksViewModel.uiState.collectAsStateWithLifecycle()`) to drive the app bar.  | Confirmed | Fixed |
| F037 | app/src/main/java/dev/stan/yotsuba/feature/bookmarks/BookmarksList.kt:96 | bug | high | `snapshotResults` is a `MutableSharedFlow(extraBufferCapacity = 1, DROP_OLDEST)` with replay 0 (BookmarksViewModel.kt:51-53) and its only collector is `LaunchedEffect(viewModel)` inside `BookmarksList`. `BookmarksList` is composed only for `ThreadsSegment.WATCHED` inside a `Crossfade` (ThreadsScreen | Confirmed | Fixed |
| F038 | app/src/main/java/dev/stan/yotsuba/feature/bookmarks/BookmarksList.kt:115 | ui/ux | high | `if (state.loaded && state.bookmarks.isEmpty())` shows the empty state, `else` renders the PullToRefreshBox. `BookmarksUiState()` starts with `loaded = false, bookmarks = emptyList()` (BookmarksViewModel.kt:35), so between first composition and the first repository emission the screen is an empty La | Confirmed | Fixed |
| F039 | app/src/main/java/dev/stan/yotsuba/feature/bookmarks/BookmarksViewModel.kt:137 | 5 type/boundary | high | `snapshotting` is typed `Set<String>` (line 37) and the key format is a companion function `snapshotKey(board, threadNo) = "$board/$threadNo"` (line 137) that the *UI* has to call to interrogate the state: `BookmarksViewModel.snapshotKey(bookmark.board, bookmark.threadNo) in state.snapshotting` at B | Confirmed | Fixed |
| F040 | app/src/main/java/dev/stan/yotsuba/feature/home/HomeScreen.kt:83 | bug | high | `rememberPagerState(initialPage = savedPage.coerceIn(0, maxOf(pageCount - 1, 0)), pageCount = { pageCount })` is evaluated at line 83, before the `when` at line 115. `HomeViewModel.boards` (HomeViewModel.kt:23-25) starts at `null`, so on the very first composition `pageCount == 0` and `initialPage`  | Confirmed | Fixed |
| F041 | app/src/main/java/dev/stan/yotsuba/feature/home/HomeScreen.kt:95 | 6 canonical layer | high | Commit c503b00 ("Hoist the tab Scaffold into AppNavHost") established the rule stated in AppNavHost.kt:68-71: "Tab screens fill the top bar and FAB through TabScaffoldSlots; pushed screens bring their own Scaffold". BoardsScreen, ThreadsScreen and VaultScreen all take `slots: TabScaffoldSlots` and c | Confirmed | Fixed |
| F042 | app/src/main/java/dev/stan/yotsuba/feature/home/ReorderableTabRow.kt:73 | 4 magic/wrappers | high | `val widths = remember { mutableStateListOf<Int>() }` is resized by mutating snapshot state *during composition*: `while (widths.size < boards.size) widths.add(0); while (widths.size > boards.size) widths.removeAt(widths.lastIndex)`. The same list is then read in the same composition at lines 87 (`d | Confirmed | Fixed |
| F043 | app/src/main/java/dev/stan/yotsuba/feature/media/MediaScreen.kt:133 | bug | high | MediaFeedViewer already fires the long-press haptic before delegating: `onLongPress = { haptics.longPress(); onLongPressPage(page) }` (MediaFeedViewer.kt:243 for video, :258 for image). MediaScreen's onLongPressPage then fires a second one: `haptics.performHapticFeedback(HapticFeedbackType.LongPress | Confirmed | Fixed |
| F044 | app/src/main/java/dev/stan/yotsuba/feature/media/MediaScreen.kt:212 | 6 canonical layer | high | There are now three verbatim copies of the `NetworkError` → user-facing-string mapping: the canonical `errorPresentation` in the design system (core/designsystem/component/StateViews.kt:147, private), `refreshErrorMessage` added by 0c03f4b (feature/thread/ThreadScreen.kt:509), and `errorMessage` add | Confirmed | Fixed |
| F045 | app/src/main/java/dev/stan/yotsuba/feature/media/VideoPage.kt:85 | 0 structural simplification | high | `VideoPage` is a single 320-line composable holding: ExoPlayer construction and release, the sound-post companion player, a lifecycle observer, a Player.Listener with six overrides, a 250 ms position poll, seek-hint timing, zoom/double-tap-seek wiring, the buffering/failed overlays and the whole tra | Confirmed | Fixed |
| F046 | app/src/main/java/dev/stan/yotsuba/feature/media/VideoPage.kt:141 | bug | high | The player is created by `remember(videoUri)` (line 113), but the three effects that configure it are keyed only on UI flags: `LaunchedEffect(selected, playing) { player.playWhenReady = ... }` (141), `LaunchedEffect(muted, soundPlayer) { player.volume = ... }` (143-146), `LaunchedEffect(autoAdvance, | Confirmed | Fixed |
| F047 | app/src/main/java/dev/stan/yotsuba/feature/media/ViewerChrome.kt:111 | ui/ux | high | `ViewerTopChrome` lays out one Row: close IconButton, a `Column(Modifier.weight(1f))` with the file name and the '3 / 12 · 1.2 MB · 1920×1080 · subject' line, then `actions()` unbounded (ViewerChrome.kt:77-111). MediaFeedViewer already injects two actions (AutoAdvanceButton, PipButton — MediaFeedVie | Confirmed | Fixed |
| F048 | app/src/main/java/dev/stan/yotsuba/feature/settings/sections/FiltersSection.kt:78 | 6 canonical layer | high | All filter-list surgery on the domain model is written inline in the composable, in three inconsistent styles within 45 lines. Delete: `update { it.copy(filters = it.filters - filter) }` (line 82, removal by structural equality). Undo re-insert: `val at = index.coerceAtMost(s.filters.size); s.copy(f | Confirmed | Fixed |
| F049 | app/src/main/java/dev/stan/yotsuba/feature/settings/sections/FiltersSection.kt:180 | ui/ux | high | `FilterDialog` holds every field in plain `remember`: `var pattern by remember { mutableStateOf(initial.pattern) }` plus isRegex, field, boards, action, enabled, sample (lines 180-186). The dialog's own visibility is likewise `var editing by remember { mutableStateOf<Filter?>(null) }` (line 68). Rot | Confirmed | Fixed |
| F050 | app/src/main/java/dev/stan/yotsuba/feature/thread/components/PostBody.kt:62 | 0 structural simplification | high | Every post body in the list is wrapped in a `Crossfade(targetState = SpoilerState(revealedSpoilerIds, revealAll), ...)`. PostCard.kt:250-260 calls `PostBody` for every post whose body is non-empty, so a 400-post thread creates up to 400 Crossfades. Each one costs, per item composed during a fling: a | Confirmed | Fixed |
| F051 | app/src/main/java/dev/stan/yotsuba/feature/thread/components/PostBody.kt:90 | 6 canonical layer | high | The post's `AnnotatedString` is built inside composition: `remember(body, revealedSpoilerIds, revealAll, highlight, quoteLabels, colors, scheme) { ... buildAnnotatedString { body.segments.forEach { ... } } ... highlightMatches(...) }`. Per segment this allocates a `SpanStyle` through up to seven `co | Rejected (Misreads the code: the build at PostBody.kt:90 is inside remember keyed on body/revealedSpoilerIds/revealAll/highlight/quoteLabels/colors/scheme, so it does not) | Deferred (rejected in verification: Misreads the code: the build at PostBody.kt:90 is inside remember keyed on body/revealedSpoilerIds/revealAll/highlight/quoteLabels/colors/scheme, so it does not rerun on recomposition with unchanged inputs — the claim that it 'repeats on every recomposition triggered by a new ThreadContent instance' is wrong. What remains is one AnnotatedString build per item realization, which is ordinary Compose; the proposed LruCache or VM-side span pipeline adds a cache-key surface and pushes styling into the ViewModel for an unmeasured gain.) |
| F052 | app/src/main/java/dev/stan/yotsuba/feature/thread/components/PostCard.kt:117 | ui/ux | high | Three of the card's actions exist only as long-presses with no accessibility equivalent: the post action sheet (`Modifier.pointerInput { detectTapGestures(onLongPress = ...) }`, line 117-119), save-to-vault on the thumbnail (`combinedClickable(onLongClick = actions.onThumbnailLongPress)`, line 225-2 | Confirmed | Fixed |
| F053 | app/src/main/java/dev/stan/yotsuba/feature/thread/components/PostCard.kt:152 | ui/ux | high | The poster-ID pill draws `color = Color.White` text on `posterIdColor(post.posterId, darkTheme)`, which in light theme is `Color.hsv(hue, 0.35f, 0.85f)` (line 62) — a pastel at 85% value. White on an 85%-value pastel is roughly 1.3:1 contrast, far below the 4.5:1 needed for labelSmall text; the ID i | Confirmed | Fixed |
| F054 | app/src/main/java/dev/stan/yotsuba/feature/thread/components/PostCard.kt:207 | 4 magic/wrappers | high | There are now three parallel, independent encodings of "which surface is this post card on". (1) `fun actionsFor(post: ThreadPost, inPreview: Boolean)` in ThreadScreen.kt:233, branching at :241 and :251. (2) `PostCardActions.forPreview()` (PostCard.kt:94), applied at ThreadScreen.kt:302 — whose own  | Rejected (Same underlying defect as F172 (shared-element ownership inferred from actions.onThumbnailLongPress at PostCard.kt:207), reported twice. F054's remedy is the wo) | Deferred (rejected in verification: Same underlying defect as F172 (shared-element ownership inferred from actions.onThumbnailLongPress at PostCard.kt:207), reported twice. F054's remedy is the worse of the two: it bolts a PostCardSurface enum onto PostCardActions — a new field on a data class that is otherwise pure handlers — and demands rebuilding the action sets in ThreadScreen, touching SubThreadPanel.kt:139 too, for a distinction only one line inside PostCard actually needs. That is adding a mode flag, not deleting one. The claim that forPreview()'s KDoc 'decides which' surface is also a misread: that KDoc is about which handlers are inert, not about transitions. Keeping F172 as the canonical finding.) |
| F055 | app/src/main/java/dev/stan/yotsuba/feature/thread/ThreadScreen.kt:139 | bug | high | `LaunchedEffect(scrollTarget, state)` is keyed on the entire `state` object, but only needs `scrollTarget`. Every uiState emission — a poll landing, a spoiler reveal, a search keystroke, the highlight timer clearing at ThreadViewModel.kt:422 — cancels and restarts the effect. `onJumpToPost` (ThreadV | Confirmed | Fixed |
| F056 | app/src/main/java/dev/stan/yotsuba/feature/thread/ThreadScreen.kt:164 | bug | high | ``` val refreshError = (state as? UiState.Success)?.data?.refreshError LaunchedEffect(refreshError) {     if (refreshError == null \|\| refreshErrorMessage == null) return@LaunchedEffect     viewModel.onRefreshErrorShown()     snackbar.showSnackbar(refreshErrorMessage) } ``` `onRefreshErrorShown()`  | Confirmed | Fixed |
| F057 | app/src/main/java/dev/stan/yotsuba/feature/thread/ThreadScreen.kt:233 | 3 design cleanliness | high | `fun actionsFor(post, inPreview)` builds a fresh `PostCardActions` — ten new lambdas — on every call, and `postCard` (line 294) calls it inside the item content for every visible post on every recomposition. `PostCardActions` is a data class whose equality is lambda identity, so the `actions` parame | Confirmed | Fixed |
| F058 | app/src/main/java/dev/stan/yotsuba/feature/thread/ThreadViewModel.kt:169 | 6 canonical layer | high | `uiState` ends `.stateIn(viewModelScope, ...)` with no `flowOn`, so the whole combine block (:124-167) runs on `viewModelScope`'s Main dispatcher: `searchMatches` over every post's `plainText`, `associateBy` over every post, `filterVerdicts` (the FilterMatcher over every post), a `filter` over every | Confirmed | Fixed |
| F059 | app/src/main/java/dev/stan/yotsuba/feature/thread/ThreadViewModel.kt:464 | 6 canonical layer | high | ThreadViewModel.onSaveMedia (L464-483) and MediaViewModel.enqueueSave (feature/media/MediaViewModel.kt L189-214) build the same VaultSaveContext by hand: find the OP (`posts.firstOrNull { it.isOp }`), `threadSubject = op?.subject`, `opExcerpt = op?.body?.plainText?.takeIf { it.isNotBlank() }`, `post | Confirmed | Fixed |
| F060 | app/src/main/java/dev/stan/yotsuba/feature/thread/ThreadViewModel.kt:518 | 2 spaghetti growth | high | ``` fun onVisiblePostsChanged(firstIndex: Int, lastIndex: Int?) {     val details = (result.value as? DataResult.Success)?.value ?: return     val rows = rows(details, session.value, filterVerdicts(details.posts, matcher.value)) ``` This rebuilds the entire row list from scratch on every call. The c | Confirmed | Fixed |
| F061 | app/src/main/java/dev/stan/yotsuba/feature/thread/ThreadViewModel.kt:524 | bug | high | 671545c made the row list poster-ID-filterable (`rows()` → `linearRows(visiblePosts(details.posts, session), …)`, ThreadViewModel.kt:574-576, where visiblePosts keeps only the OP and the selected ID) but left the read-mark path reading indices out of that same filtered list: onVisiblePostsChanged (l | Confirmed | Fixed |
| F062 | app/src/main/java/dev/stan/yotsuba/feature/vault/VaultDedupSheet.kt:265 | ui/ux | high | The per-group `TextButton(onClick = onApply, ...)` labelled 'Keep selected · N (12 MB)' calls `viewModel.applyGroup(group)` which deletes straight through `vault.delete` (VaultDedupViewModel.kt:118) — permanent, no trash, no undo, no confirmation. The very same screen puts an AlertDialog in front of | Confirmed | Fixed |
| F063 | app/src/main/java/dev/stan/yotsuba/feature/vault/VaultDedupViewModel.kt:107 | bug | high | `applyAllSuggestions() = delete(_state.value.suggestedRemovals)` and `suggestedRemovals: List<String> get() = groups.flatMap { g -> g.redundant.map { it.url } }` (:41) read only the repository's suggested keeper — they ignore the `kept` map that `toggleKept` (:91) writes and that `applyGroup` (:100) | Confirmed | Fixed |
| F064 | app/src/main/java/dev/stan/yotsuba/feature/vault/VaultExplorer.kt:218 | 5 type/boundary | high | Commit 06914f1 introduced `val view = Triple(state.sort, state.reversed, state.filter)` (VaultExplorer.kt:114) and threads it through four private composables typed as `view: Any` (lines 218 `BoardList`, 256 `ThreadList`, 483 `MediaGrid`), used only as `LaunchedEffect(view) { listState.scrollToItem( | Rejected (Duplicate of F187, which reports the same `view: Any` / Triple issue at its origin (VaultExplorer.kt:114) with the fuller fix. Real issue, but reported three ti) | Deferred (rejected in verification: Duplicate of F187, which reports the same `view: Any` / Triple issue at its origin (VaultExplorer.kt:114) with the fuller fix. Real issue, but reported three times; keeping one.) |
| F065 | app/src/main/java/dev/stan/yotsuba/feature/vault/VaultScreen.kt:174 | bug | high | `ImportPicker.sourcesFrom(context, uris)` (174) and `ImportPicker.sourcesFromTree(context, tree)` (185) run inside the activity-result callbacks, which execute on the main thread. Both do ContentResolver work: `sourcesFrom` issues one `query()` per picked file (ImportPicker.kt:18-21) and `sourcesFro | Confirmed | Fixed |
| F066 | app/src/main/java/dev/stan/yotsuba/feature/vault/VaultScreen.kt:204 | 1 file size | high | VaultScreen.kt is 736 lines and `VaultScreen` itself runs 95→461. The `topBar = { ... }` lambda alone is lines 204-352: a four-way if/else chain (viewer / selecting / searchOpen / default) whose default arm inlines the import Box+IconButton+DropdownMenu (248-276), the sync progress Row (277-294), th | Confirmed | Fixed |
| F067 | app/src/main/java/dev/stan/yotsuba/feature/vault/VaultStats.kt:43 | 6 canonical layer | high | `VaultStats.of` re-implements, line for line, the aggregation `VaultViewModel.groupByBoard` already does in the same package. VaultStats.kt:44-47 `entries.groupBy { it.location.board }.map { (board, group) -> BoardStat(board, group.size, group.totalBytes) }` vs VaultViewModel.kt:721-736 `entries.gro | Confirmed | Fixed |
| F068 | app/src/main/java/dev/stan/yotsuba/feature/vault/VaultViewModel.kt:424 | 5 type/boundary | high | `@Suppress("UNCHECKED_CAST")` over a 6-arity `combine(...) { args: Array<Any?> -> ... }`, then six positional casts: `args[0] as List<VaultEntry>`, `args[1] as VaultSyncState`, ... `args[5] as Editing` (lines 425-433). The file already invented three private fold types (`Viewing` 311, `Activity` 316 | Confirmed | Fixed |
| F069 | app/src/main/java/dev/stan/yotsuba/feature/vault/VaultViewModel.kt:435 | 0 structural simplification | high | Three commits in this slice each bolted another whole-vault list projection onto the same UI state: 7521e0a added `visible`, f022e5a added `recent`, 47517e4 added `results`. On HEAD the combine at VaultViewModel.kt:434-450 materialises all of them on every emission (`visible = arrangeEntries(entries | Confirmed | Fixed |
| F070 | app/src/main/java/dev/stan/yotsuba/feature/vault/VaultViewModel.kt:541 | 6 canonical layer | high | `mediaVault.entries()` is a cold flow: `savedMediaDao.all().map { rows.filter{...}.map { it.toVaultEntry() } }` (MediaVaultRepositoryImpl.kt:87). The ViewModel collects it three separate times — `uiState` (line 426), `stats` (line 472), `viewerThread` (line 541) — so every DB write runs three Room q | Confirmed | Fixed |
| F071 | app/src/main/java/dev/stan/yotsuba/feature/vault/VaultViewModel.kt:593 | 0 structural simplification | high | 63650ec split the sync button into two menu items backed by two new methods but left the original whole. On HEAD `sync()` (line 593-606), `rescan()` (608-621) and `fetchReplies()` (623-635) are three copies of the same guard-and-state-machine (`if (syncState.value.running) return`, set `VaultSyncSta | Confirmed | Fixed |
| F072 | app/src/main/java/dev/stan/yotsuba/navigation/AppNavHost.kt:105 | 6 canonical layer | high | Commit 9519b06 ("Open thread and catalog links from browsers, shares and the widget") added the canonical intent path: `ExternalLinks.fromIntent` -> `ShellViewModel.pendingLink` -> the LaunchedEffect at AppNavHost.kt:97-104. It left the widget's own path (b446eb3) untouched, so the same concern now  | Confirmed | Fixed |
| F073 | app/src/main/java/dev/stan/yotsuba/navigation/AppNavHost.kt:137 | 2 spaghetti growth | high | The nav-item loop is written twice, byte-for-byte apart from the `Rail`/`Bar` prefix: lines 116-124 (`NavigationRail`) and lines 137-145 (`NavigationBar`) both do `for (dest in TopLevelDestination.entries) { val selected = isSelected(dest); …Item(selected, onClick = { navigateTopLevel(dest) }, icon  | Confirmed | Fixed |
| F074 | app/src/main/java/dev/stan/yotsuba/navigation/AppNavHost.kt:165 | 6 canonical layer | high | MotionSpecs.kt states its contract at lines 23-27: "Everything here collapses to its end state under reduced motion, so a call site never checks that itself", and provides `motionEnter()`/`motionExit()`/`rememberMotionSpec()` that do the check internally (MotionSpecs.kt:32, 62, 70). Commit e94cf4c i | Confirmed | Fixed |
| F075 | app/src/test/java/dev/stan/yotsuba/data/MediaDownloadQueueTest.kt:33 | 5 type/boundary | high | The file's own KDoc (lines 27-31) states the problem: "The queue owns a real IO-dispatcher worker (its scope is not injectable), so these tests run against real time". `MediaDownloadQueue` (data/repository/MediaDownloadQueue.kt:33) hard-codes `CoroutineScope(SupervisorJob() + Dispatchers.IO)`. The c | Confirmed | Deferred (Needs MediaDownloadQueue to take an injectable scope (production code, media slice); test rewrite blocked on that.) |
| F076 | app/src/test/java/dev/stan/yotsuba/data/MediaDownloadQueueTest.kt:43 | 6 canonical layer | high | `MediaVaultRepository` (14 members) is stubbed by hand nine separate times across the slice: MediaDownloadQueueTest.kt:43 `GatedVault`, DedupQueueTest.kt:34 `RecordingVault`, VaultDedupViewModelTest.kt:60 `DeletingVault`, BookmarksViewModelTest.kt:86 `FakeVault`, MediaViewModelTest.kt:101 `FakeVault | Confirmed | Fixed |
| F077 | app/src/test/java/dev/stan/yotsuba/feature/thread/ThreadRefreshFailureTest.kt:56 | 6 canonical layer | high | `diff <(sed -n '56,125p' ThreadQuoteTapTest.kt) <(sed -n '56,122p' ThreadRefreshFailureTest.kt)` reports only three differences: a `@After` line, the two-line settings fake body, and one extra `previewNos` helper. Roughly 65 lines of `ThreadViewModel` wiring — anonymous `ThreadRepository`, `MediaVau | Confirmed | Fixed |
| F078 | app/schemas/dev.stan.yotsuba.core.database.YotsubaDatabase/6.json:1 | 5 type/boundary | medium | `exportSchema = true` (YotsubaDatabase.kt:124) and `room.schemaLocation` are set, but only 6..10.json exist while Migrations.kt defines MIGRATION_1_2 through MIGRATION_5_6 and Modules.kt:127 wires all nine. No test references `MigrationTestHelper` (grep of test/ and androidTest/ is empty). The expor | Rejected (Facts check out (schemas 6-10 only, migrations 1_2..9_10, no MigrationTestHelper anywhere), but this is a test-coverage/process gap, not something the thermo-nu) | Deferred (rejected in verification: Facts check out (schemas 6-10 only, migrations 1_2..9_10, no MigrationTestHelper anywhere), but this is a test-coverage/process gap, not something the thermo-nuclear rules flag: no structural regression, no spaghetti, no type/boundary problem in code. Rule 5 is about types and contracts in source, not about exported Room schema JSON. The cited artefact is a generated file and 'line 1' of it anchors nothing. The alternative fix (dropping exportSchema/schemaLocation) would delete the only artefacts a future migration test could use and would change the build's outputs, making the situation worse, not cleaner.) |
| F079 | app/src/androidTest/java/dev/stan/yotsuba/BoardsFavouriteFlowTest.kt:63 | bug | medium | boardsSearch_filtersAndRestores claims to check restoration, but line 62 clears the field and line 63 immediately types "Tech" — which matches TestSeed.BOARD_TITLE = "Technology". The closing assertion at lines 64-65 (openBoardsTab(), then waitForText(BOARD_TITLE)) therefore passes whether or not th | Confirmed | Fixed |
| F080 | app/src/androidTest/java/dev/stan/yotsuba/di/TestRepositoryModule.kt:1 | 0 structural simplification | medium | One 401-line file carries three unrelated concerns: the TestSeed fixture object (lines 52-155, ~100 lines of domain-model literals), eleven fake repository implementations (lines 157-377, ~220 lines), and the actual Hilt wiring (lines 379-401, ~20 lines). It is 70% of the whole androidTest source se | Rejected (The file is 401 lines, nowhere near the 1k-line threshold the rules flag, and its three parts (seed data, fakes, bindings) are one cohesive concern: the instrum) | Deferred (rejected in verification: The file is 401 lines, nowhere near the 1k-line threshold the rules flag, and its three parts (seed data, fakes, bindings) are one cohesive concern: the instrumented-test double graph. The proposed fix deletes no complexity, it just spreads the same code across three files, which rule 3 explicitly deprecates. The import-order remark is a cosmetic nit.) |
| F081 | app/src/androidTest/java/dev/stan/yotsuba/di/TestRepositoryModule.kt:324 | 6 canonical layer | medium | FakeSettingsRepository here (a MutableStateFlow<Settings> plus an update lambda) is a line-for-line reimplementation of app/src/test/java/dev/stan/yotsuba/fake/FakeSettings.kt, whose own KDoc says "Several ViewModel tests declare their own private copy of this; new tests should use this one." The an | Rejected (Line 324 is indeed FakeSettingsRepository, and it does overlap with app/src/test/.../fake/FakeSettings.kt — but the duplicate is 7 trivial lines (a MutableState) | Deferred (rejected in verification: Line 324 is indeed FakeSettingsRepository, and it does overlap with app/src/test/.../fake/FakeSettings.kt — but the duplicate is 7 trivial lines (a MutableStateFlow plus an update lambda), and the two live in source sets that cannot see each other by design. The proposed remedy is to add a whole new sharedTest source set to app/build.gradle.kts and rewire the Hilt @Binds at line 388 into a @Provides, i.e. build-system churn out of proportion to the payoff, and the classes aren't even identical (FakeSettings takes an `initial` param and has no @Inject/@Singleton, which Hilt needs here). The 'eighth copy' framing also overstates it: the KDoc's remark about ViewModel tests concerns the unit-test source set, not this file. Low-value nit under the rules' own 'don't flood the review with nits' bar.) |
| F082 | app/src/androidTest/java/dev/stan/yotsuba/di/TestRepositoryModule.kt:370 | 4 magic/wrappers | medium | `val error = kotlinx.coroutines.runBlocking { vault.save(item, context) }` inside MediaSaveQueue.enqueue, written fully qualified so it dodges the import list. enqueue is a non-suspend function that Compose calls on the main thread during MediaSaveFlowTest, so this blocks the UI thread on a coroutin | Confirmed | Fixed |
| F083 | app/src/androidTest/java/dev/stan/yotsuba/FlowTestHelpers.kt:26 | 4 magic/wrappers | medium | clickText hardcodes `substring = true, ignoreCase = true` with no way to opt out, and waitForContentDescription (line 19-24) does the same, while waitForText (line 12) does expose a `substring` parameter. That missing parameter has already spawned two workarounds: openBoardsTab (lines 31-33) is a on | Confirmed | Fixed |
| F084 | app/src/androidTest/java/dev/stan/yotsuba/HistoryClearFlowTest.kt:30 | 2 spaghetti growth | medium | Lines 30-36 are a verbatim copy of HistoryFlowTest.kt:29-39: openSeededThread(); click Back; waitForText("Threads"); clickText("Threads"); waitForText("Recent"); clickText("Recent"); waitForText(THREAD_SUBJECT). BookmarksFlowTest.kt:37-41 repeats the same Back-then-Threads-tab prefix for the "Watche | Confirmed | Fixed |
| F085 | app/src/androidTest/java/dev/stan/yotsuba/VaultFlowTest.kt:33 | 5 type/boundary | medium | The only assertion is a disjunction — waitUntil { "Saved media" present \|\| "Vault is empty" present } — hedged by a comment saying "either ... or ... must be on screen". The fakes make the outcome fully deterministic: FakeMediaVaultRepository.hasStorageAccess() returns true (line 284) and entries( | Confirmed | Fixed |
| F086 | app/src/main/java/dev/stan/yotsuba/core/database/dao/BookmarkDao.kt:60 | 3 design cleanliness | medium | `updateRefresh` (:43-57), `updateCounts` (:60-73) and `updateState` (:75-76) are three UPDATE statements over the same row where each is a strict column-subset of the previous: updateCounts is updateRefresh minus `postNos`, updateState is updateCounts minus counts and activity. That forces the calle | Rejected (The three UPDATEs are real and overlapping, but the proposed collapse does not delete complexity: it replaces three explicit, self-documenting statements with o) | Deferred (rejected in verification: The three UPDATEs are real and overlapping, but the proposed collapse does not delete complexity: it replaces three explicit, self-documenting statements with one statement carrying three nullable params plus a `touchActivity` guard flag, and the finding itself admits the naive version would break behaviour (updateState must leave lastActivityAt untouched; the COALESCE form as written would clobber it, and passing the row's existing value from the caller is a read-modify-write the current single statement avoids). Taste-level restructuring with a behaviour hazard, not a structural regression the rules flag.) |
| F087 | app/src/main/java/dev/stan/yotsuba/core/database/dao/SavedMediaDao.kt:41 | 3 design cleanliness | medium | `byMd5` filters on a non-indexed column: `saved_media` declares no indices at all (confirmed in app/schemas/.../10.json — every entity has `"indices": []`), so every lookup is a full table scan. It is on the hot save path: MediaDownloadQueue.kt:55 calls `dedup.findByMd5(...)` for each queued item, i | Confirmed | Deferred (Index on saved_media.md5 needs a Room schema bump and migration; schema changes out of scope for this pass.) |
| F088 | app/src/main/java/dev/stan/yotsuba/core/database/dao/SavedMediaDao.kt:47 | 6 canonical layer | medium | `ext NOT IN ('.webm', '.mp4')` hardcodes the video-extension set inside SQL. The same set is spelled out five more times in Kotlin: VaultEntry.kt:48, MediaItem.kt:31, VaultDedupRepositoryImpl.kt:74, MediaVaultRepositoryImpl.kt:237 and :575. There is no canonical constant, and the SQL copy is the one | Confirmed | Deferred (Canonical VideoFormats constant belongs in core/media with call sites in data/; a DAO-only bound would be a half-fix.) |
| F089 | app/src/main/java/dev/stan/yotsuba/core/database/dao/SavedMediaDao.kt:49 | 0 structural simplification | medium | `missingHashes(): List<SavedMediaEntity>` is the only way to ask how much backfill work remains, so VaultDedupRepositoryImpl.kt:34 implements `missingHashCount()` as `dao.missingHashes().size` — it materialises every unhashed row (18 columns each, including absolutePath and subject strings) into a l | Confirmed | Fixed |
| F090 | app/src/main/java/dev/stan/yotsuba/core/database/entity/Entities.kt:20 | 0 structural simplification | medium | `newReplies` and `unreadCount` are documented at :19 and :21 as "Superseded by readUpTo/postNos; no longer written, dropped in a later migration" — that later migration never came; both columns are still in the v10 schema (app/schemas/.../10.json bookmarks fields). No production code writes them (gr | Confirmed | Deferred (Dropping newReplies/unreadCount/lastSeenPostNo needs a v11 schema and migration.) |
| F091 | app/src/main/java/dev/stan/yotsuba/core/database/Migrations.kt:6 | 3 design cleanliness | medium | Nine hand-written migrations, and nothing tests any of them: `grep -rln "MigrationTestHelper\|MIGRATION_" app/src/androidTest app/src/test` returns nothing. Worse, the exported schemas needed to test them have been deleted — app/schemas/dev.stan.yotsuba.core.database.YotsubaDatabase/ contains only 6 | Rejected (The facts check out (no MigrationTestHelper anywhere, only 6..10.json exported, no destructive fallback at Modules.kt:126-128), but this is a missing-test-cover) | Deferred (rejected in verification: The facts check out (no MigrationTestHelper anywhere, only 6..10.json exported, no destructive fallback at Modules.kt:126-128), but this is a missing-test-coverage gap, not one of the structural/abstraction problems this review's rules flag, and part of the fix is impossible: 1.json..5.json are not recoverable — `git log --all` for those paths returns nothing, so they were never committed.) |
| F092 | app/src/main/java/dev/stan/yotsuba/core/datastore/SettingsDataStore.kt:35 | 6 canonical layer | medium | `private val blobKey = stringPreferencesKey("settings")` is the canonical name of the settings blob, but it is private, so BackupRepositoryImpl.kt:126 redeclares the same literal as `val SETTINGS_BLOB = stringPreferencesKey("settings")` and reads the raw DataStore at :120 to decide whether the insta | Confirmed | Fixed |
| F093 | app/src/main/java/dev/stan/yotsuba/core/designsystem/component/SettingsRows.kt:42 | ui/ux | medium | `SwitchRow` puts `.clickable(enabled = enabled) { onToggle(!checked) }` on the `Row` (line 42) and simultaneously hands the `Switch` a live `onCheckedChange = onToggle` (line 51). The result is two independent, overlapping accessibility nodes for one setting: TalkBack announces a generic clickable r | Rejected (Same defect, same lines, same fix as F017 (which additionally covers TextRow/NavigationRow roles). Duplicate, not an independent finding.) | Deferred (rejected in verification: Same defect, same lines, same fix as F017 (which additionally covers TextRow/NavigationRow roles). Duplicate, not an independent finding.) |
| F094 | app/src/main/java/dev/stan/yotsuba/core/designsystem/component/SettingsRows.kt:107 | ui/ux | medium | `ChipRow` threads `enabled` into the `FilterChip`s (line 113) but its label `Text(label, style = MaterialTheme.typography.bodyLarge)` at line 107 keeps full `onSurface` colour. `MediaSection.kt:50-57` uses this with `enabled = settings.doubleTapSeekEnabled`, so with double-tap seek off the "Seek ste | Confirmed | Fixed |
| F095 | app/src/main/java/dev/stan/yotsuba/core/designsystem/component/SharedMedia.kt:42 | 4 magic/wrappers | medium | The four-argument `sharedMedia` overload is public API justified by a KDoc that is factually wrong: "Split out so the no-op contract can be checked on the JVM without composing anything." The function is annotated `@Composable` (line 47) and cannot be called without a composition; SharedMediaModifie | Confirmed | Fixed |
| F096 | app/src/main/java/dev/stan/yotsuba/core/designsystem/component/ThreadSummaryRow.kt:37 | 5 type/boundary | medium | `Row(Modifier.padding(spacing.md).then(modifier), ...)` applies the caller's modifier *after* the component's own padding, inverting the Compose convention that the caller's modifier comes first. Any caller passing `Modifier.clickable {}` would get a click region inset by 12dp on every side; any cal | Confirmed | Fixed |
| F097 | app/src/main/java/dev/stan/yotsuba/core/designsystem/ReducedMotion.kt:24 | 4 magic/wrappers | medium | Commit 676971c added `fun isReducedMotion(context: Context, reduceMotionSetting: Boolean): Boolean = reduceMotionSetting \|\| isReducedMotion(context)` (lines 23-25). `rg isReducedMotion` shows the only callers of the two-arg overload are ReducedMotionTest.kt:35-36; production reads the setting thro | Confirmed | Fixed |
| F098 | app/src/main/java/dev/stan/yotsuba/core/designsystem/token/Elevation.kt:9 | 3 design cleanliness | medium | `Elevation`/`LocalElevation` is provided in Theme.kt:105 but never read: `rg -n 'LocalElevation\|level0\|level1\|level3' app/src/main -g '*.kt'` returns only the token file and the `provides` line. Same for `Spacing.xxl`: `rg -o 'spacing\.(xs\|sm\|md\|lg\|xl\|xxl)\b' app/src/main` counts xs 29, sm 5 | Confirmed | Fixed |
| F099 | app/src/main/java/dev/stan/yotsuba/core/network/ArchiveHosts.kt:33 | 5 type/boundary | medium | One concept (an archive) is spread over three parallel tables that must be kept in sync by hand: `boards` (14), `webBase` (25) and the `when` in apiUrl (46-50). `sourceFor` reads `boards.getValue(it)` for every ArchiveSource entry, so the moment someone adds a fourth entry to ArchiveSource (ThreadPo | Confirmed | Fixed |
| F100 | app/src/main/java/dev/stan/yotsuba/core/update/Updater.kt:92 | 2 spaghetti growth | medium | Three copies of the same install-and-report block. downloadAndInstall:92-97 sets Installing, calls install(apk, allowSilent = true), catches Exception -> Failed("Install failed: ${e.message ?: "unknown error"}"); retryWithConfirmation:109-114 is the identical block with allowSilent = false wrapped i | Confirmed | Fixed |
| F101 | app/src/main/java/dev/stan/yotsuba/core/util/Urls.kt:33 | 5 type/boundary | medium | `val search = uri.fragment?.removePrefix("s=")?.replace('_', ' ')` treats *any* fragment on a `/<board>/catalog` URL as a search query. `removePrefix` is a no-op when the prefix is absent, so `https://boards.4chan.org/g/catalog#top` returns `InternalLink.Catalog("g", "top")` and opens the catalog pr | Confirmed | Fixed |
| F102 | app/src/main/java/dev/stan/yotsuba/core/util/Urls.kt:43 | 6 canonical layer | medium | The protocol-relative fixup `if (x.startsWith("//")) "https:$x" else x` is written out four separate times: Urls.kt:23 (parseInternal), Urls.kt:43 (domainOf), core/text/PostHtmlParser.kt:136 (resolveUrl), core/media/SoundPost.kt:45. Three of those then build a `java.net.URI` from the result and two  | Confirmed | Fixed |
| F103 | app/src/main/java/dev/stan/yotsuba/core/vault/VaultMeta.kt:67 | 0 structural simplification | medium | `VaultMetaCodec` (VaultMeta.kt:67-78) and `VaultPostsCodec` (VaultPosts.kt:124-135) are byte-for-byte the same object: the same `Json { ignoreUnknownKeys = true; prettyPrint = true; encodeDefaults = false }` configuration, the same `encode` and the same `decode` returning null via `runCatching`. Two | Confirmed | Fixed |
| F104 | app/src/main/java/dev/stan/yotsuba/core/vault/VaultPosts.kt:66 | 6 canonical layer | medium | Package dependencies run both ways: `core/vault/VaultPosts.kt:82-84` imports `domain.model.PostMedia`/`ThreadPost` (and spells `dev.stan.yotsuba.domain.model.MediaItem` inline at line 187), `core/network/ArchiveHosts.kt:215` imports `domain.model.ArchiveSource`, while `domain/model/VaultEntry.kt:3`  | Confirmed | Deferred (Breaking the core<->domain import cycle means splitting VaultPaths/PostText/DataResult and re-pointing 25+ importers across every feature package; needs its own serialized change on main.) |
| F105 | app/src/main/java/dev/stan/yotsuba/core/vault/VideoStills.kt:33 | 4 magic/wrappers | medium | Line 33 reads `?: return Still(still.takeIf { it.isFile } ?: return null, duration)` — a return inside the argument of a return. It sits inside `if (!still.isFile)` (line 31), so `still.takeIf { it.isFile }` is null by construction and the outer `Still(...)` is unreachable: the expression can only e | Confirmed | Fixed |
| F106 | app/src/main/java/dev/stan/yotsuba/core/work/VaultSyncWorker.kt:55 | 7 orchestration/atomicity | medium | On a metered connection with dataSaver on, the worker returns `Result.retry()`. This is a *periodic* worker (6h, INTERVAL_HOURS): retry schedules a backed-off re-run of the same period, so a phone on mobile data all day wakes the worker repeatedly (30s, 1m, 2m ... up to 5h) only to hit the same chec | Confirmed | Fixed |
| F107 | app/src/main/java/dev/stan/yotsuba/core/work/VaultSyncWorker.kt:64 | 7 orchestration/atomicity | medium | `snapshotted = live.toSet()` marks every live bookmark as done, but snapshotThreads did not necessarily touch them all: MediaVaultRepositoryImpl.snapshotThreads:422 first drops non-remote targets (`targets.filter { it.isRemote }`), and runPass:456-476 counts per-thread outcomes as updated/gone/faile | Confirmed | Fixed |
| F108 | app/src/main/java/dev/stan/yotsuba/data/repository/BackupRepositoryImpl.kt:77 | 4 magic/wrappers | medium | The write-to-temp-then-rename-with-fallback idiom is implemented twice, character for character in intent: BackupRepositoryImpl.kt:77-82 (`val tmp = File(file.parentFile, file.name + ".tmp"); tmp.writeText(...); if (!tmp.renameTo(file)) { file.writeText(tmp.readText()); tmp.delete() }`) and VaultSto | Confirmed | Fixed |
| F109 | app/src/main/java/dev/stan/yotsuba/data/repository/BookmarkRepositoryImpl.kt:117 | 4 magic/wrappers | medium | The same eight-argument persistence call is copy-pasted twice — `dao.updateRefresh(refreshed.board, refreshed.threadNo, refreshed.replyCount, refreshed.imageCount, refreshed.state.name, refreshed.lastCheckedAt, refreshed.lastActivityAt, encodePostNos(refreshed.postNos))` at lines 53-57 and the ident | Confirmed | Fixed |
| F110 | app/src/main/java/dev/stan/yotsuba/data/repository/HistoryRepositoryImpl.kt:64 | 4 magic/wrappers | medium | f9d8d51 added two private pass-through mappers — `HistoryEntity.toDomainWithReadMark() = toDomain().copy(maxReadPostNo = maxReadPostNo)` and `HistoryEntry.toFullEntity() = toEntity().copy(maxReadPostNo = maxReadPostNo)` — because the canonical mappers in Mappers.kt:240/250 deliberately drop the read | Confirmed | Fixed |
| F111 | app/src/main/java/dev/stan/yotsuba/data/repository/Mappers.kt:164 | 6 canonical layer | medium | `archiveCommentToHtml` *generates* 4chan HTML (escaping, `<br>` joining, `<a class="quotelink">` and `<span class="quote">` emission) and lives in the data-layer DTO mapper file, while its exact counterpart `PostHtmlParser` — which consumes that markup and owns the class names it must match — lives  | Confirmed | Fixed |
| F112 | app/src/main/java/dev/stan/yotsuba/data/repository/MediaVaultRepositoryImpl.kt:164 | bug | medium | `delete(url)` removes the media file and its meta entry but never touches its captured still: `file.delete()` at line 164, then `store.updateMeta(dir) { it.remove(file.name) }`. The still lives at `.thumbs/<file name>.jpg` (VideoStills.kt:18-19) and is left behind. `pruneIfEmpty` treats `.thumbs` as | Confirmed | Fixed |
| F113 | app/src/main/java/dev/stan/yotsuba/data/repository/MediaVaultRepositoryImpl.kt:233 | 6 canonical layer | medium | `exportToGallery` is 53 lines of pure Android MediaStore plumbing — API-level branch, `ContentValues`, pending-flag write, `resolver.delete` rollback, pre-29 `MediaScannerConnection` fallback — living in the vault repository. It touches no vault concept beyond looking up one row to get a path: `save | Confirmed | Fixed |
| F114 | app/src/main/java/dev/stan/yotsuba/data/repository/MediaVaultRepositoryImpl.kt:237 | 6 canonical layer | medium | The "is this a video" predicate exists in four places with no canonical owner: `entity.ext == ".webm" \|\| entity.ext == ".mp4"` inline at MediaVaultRepositoryImpl.kt:237, `private fun isVideo(ext: String?)` at MediaVaultRepositoryImpl.kt:575, `private fun isVideo(row: SavedMediaEntity)` at VaultDed | Confirmed | Fixed |
| F115 | app/src/main/java/dev/stan/yotsuba/data/repository/MediaVaultRepositoryImpl.kt:238 | 6 canonical layer | medium | exportToGallery re-declares the extension -> MIME table (L238-246: .jpg/.jpeg/.png/.gif/.webp/.webm/.mp4/else octet-stream) that already exists as `mimeOf(ext)` in feature/media/MediaShare.kt L26-34. The data layer copied a feature helper rather than the helper living somewhere both can reach. | Confirmed | Fixed |
| F116 | app/src/main/java/dev/stan/yotsuba/data/repository/MediaVaultRepositoryImpl.kt:238 | 6 canonical layer | medium | 1b5e650's exportToGallery re-types the extension→MIME table inline at lines 238-246 (`".jpg", ".jpeg" -> "image/jpeg"` … `".mp4" -> "video/mp4"`), byte-identical to the canonical `mimeOf` that 84b787a had just consolidated in feature/media/MediaShare.kt:26-35 — which the vault's own share path uses  | Rejected (Duplicate: it restates F115 (mime table copied from MediaShare.mimeOf) and F114 (isVideo predicate in four places) as one finding at an adjacent line (239 vs th) | Deferred (rejected in verification: Duplicate: it restates F115 (mime table copied from MediaShare.mimeOf) and F114 (isVideo predicate in four places) as one finding at an adjacent line (239 vs the actual when at 238), adding no distinct defect. Keep F114 and F115.) |
| F117 | app/src/main/java/dev/stan/yotsuba/data/repository/MediaVaultRepositoryImpl.kt:490 | 0 structural simplification | medium | `savedThreads()` returns `List<SavedThreadDir>` carrying `(dir, board, threadNo)`, and `SavedThreadDir` (line 499) exists solely for it. Its only caller, line 384-385, immediately discards `dir`: `savedThreads().filterNot { VaultLocation(it.board, it.threadNo) in skip }` then `targets.map { VaultLoc | Confirmed | Fixed |
| F118 | app/src/main/java/dev/stan/yotsuba/data/repository/SavedMediaMappers.kt:98 | 5 type/boundary | medium | `location = if (board != null && board != VaultPaths.UNSORTED_DIR_NAME && threadNo != null) VaultLocation(board = board!!, threadNo = threadNo!!) else VaultLocation.Unsorted` -- two `!!` on smart-castable vals plus a hand-rolled copy of `VaultLocation.isUnsorted` (VaultEntry.kt:14). SavedMediaEntity | Confirmed | Fixed |
| F119 | app/src/main/java/dev/stan/yotsuba/data/repository/VaultDedupRepositoryImpl.kt:34 | 4 magic/wrappers | medium | `override suspend fun missingHashCount(): Int = withContext(Dispatchers.IO) { dao.missingHashes().size }`. `missingHashes()` is `SELECT * FROM saved_media WHERE ...` (SavedMediaDao.kt), so every row of the vault index — including `absolutePath`, `displayName`, `subject`, `thumbnailPath` — is materia | Confirmed | Fixed |
| F120 | app/src/main/java/dev/stan/yotsuba/domain/model/Bookmark.kt:27 | 3 design cleanliness | medium | `@Deprecated("Derived: see unread") val newReplies: Int = 0` (l.27) and `@Deprecated("Derived: see unread") val unreadCount: Int = 0` (l.28) are write-only ghosts in the domain model. `BookmarkEntity.toDomain()` (data/repository/Mappers.kt:199-215) does not set either one and `Bookmark.toEntity()` ( | Confirmed | Fixed |
| F121 | app/src/main/java/dev/stan/yotsuba/domain/model/Bookmark.kt:37 | 6 canonical layer | medium | The "subject, else a trimmed excerpt, else a synthetic label" rule is written out three times in three models: Bookmark.kt:37-38 `subject ?: opExcerpt.take(60).ifBlank { "/$board/$threadNo" }`, HistoryEntry.kt:14-15 (character-for-character identical), and CatalogThread.kt:17-18 `subject ?: excerpt. | Confirmed | Fixed |
| F122 | app/src/main/java/dev/stan/yotsuba/domain/model/Filter.kt:28 | 4 magic/wrappers | medium | `val error: String? get() = if (isRegex) runCatching { Regex(pattern) }.exceptionOrNull()?.message else null` (l.28-29) compiles a regular expression, and constructs and discards an exception object, on every single read of what looks like a plain data-class property. It is read from Compose: featur | Confirmed | Fixed |
| F123 | app/src/main/java/dev/stan/yotsuba/domain/model/VaultEntry.kt:48 | 6 canonical layer | medium | "Is this extension a video" is hand-written five times across three layers with no shared definition: VaultEntry.kt:48 `val isVideo: Boolean get() = ext == ".webm" \|\| ext == ".mp4"`, MediaItem.kt:31 `val isVideo: Boolean get() = ext == ".webm" \|\| ext == ".mp4"`, data/repository/VaultDedupReposit | Confirmed | Fixed |
| F124 | app/src/main/java/dev/stan/yotsuba/domain/repository/BookmarkRepository.kt:3 | 3 design cleanliness | medium | e6b89d0 split Repositories.kt and Daos.kt one declaration per file by copying the original file's entire import block into every extracted file. Checked mechanically over the 12 files it produced: BookmarkRepository.kt has 13 unused imports (DataResult, Board, CatalogThread, HiddenThread, HistoryEnt | Rejected (Duplicate of F030 at the same file and line, and less accurate (it counts MaintenanceRepository at 14 unused imports; the file has 15 imports and uses none). It) | Deferred (rejected in verification: Duplicate of F030 at the same file and line, and less accurate (it counts MaintenanceRepository at 14 unused imports; the file has 15 imports and uses none). Its only extra content, the DAO files, is folded into the F030 fix note.) |
| F125 | app/src/main/java/dev/stan/yotsuba/domain/repository/BookmarkRepository.kt:51 | 3 design cleanliness | medium | `@Deprecated("Unread is derived from readUpTo; use markSeen") suspend fun updateUnread(board: String, threadNo: Long, unread: Int)` (l.51-52) is an abstract member with no default body, and the one production implementation is `override suspend fun updateUnread(...) = Unit` (data/repository/Bookmark | Confirmed | Fixed |
| F126 | app/src/main/java/dev/stan/yotsuba/domain/repository/MediaVaultRepository.kt:23 | 5 type/boundary | medium | 2249904 added `val storageAccess: Flow<Boolean> get() = flow { emit(hasStorageAccess()) }` and `fun refreshStorageAccess() {}` as interface defaults so existing fakes kept compiling. The contract now lies: an implementation that forgets to override gets a one-shot flow that never updates and a refre | Rejected (The issue is real but it is a strict subset of F033, which flags the same two defaults at the same lines with the same remedy and covers the other nine besides.) | Deferred (rejected in verification: The issue is real but it is a strict subset of F033, which flags the same two defaults at the same lines with the same remedy and covers the other nine besides. Reporting it separately splits one change across two findings.) |
| F127 | app/src/main/java/dev/stan/yotsuba/domain/repository/MediaVaultRepository.kt:47 | 5 type/boundary | medium | Eleven of the interface's members have silent-fallback defaults: `trash = delete(url)` (47), `restoreTrashed = NotFound` (50), `purgeTrash {}` (53), `exportToGallery = Io("not supported")` (59), `snapshotThread = Io("not supported")` (99), `snapshotThreads = VaultSyncSummary()` (105-108), `renameThr | Rejected (Duplicate of F033: identical member list, identical evidence, identical remedy (abstract members plus a shared FakeMediaVaultRepository base), plus F034's overl) | Deferred (rejected in verification: Duplicate of F033: identical member list, identical evidence, identical remedy (abstract members plus a shared FakeMediaVaultRepository base), plus F034's overload collapse. Nothing here is not already covered.) |
| F128 | app/src/main/java/dev/stan/yotsuba/domain/repository/VaultDedupRepository.kt:31 | 4 magic/wrappers | medium | `VaultDedupRepository.None` exists only to be the default for `MediaDownloadQueue(dedup = VaultDedupRepository.None)` (MediaDownloadQueue.kt:31). Hilt always injects the real binding (DedupModule), and `rg -n 'VaultDedupRepository\.None' app/src -g '*.kt'` finds no other use; the tests that omit `de | Confirmed | Fixed |
| F129 | app/src/main/java/dev/stan/yotsuba/feature/boards/BoardsScreen.kt:115 | ui/ux | medium | Tapping the star on a favourited board calls `viewModel.onToggleFavourite` immediately with no confirmation and no undo, and the row then vanishes from the Favourites section (the list is derived from `settings.favouriteBoards`, BoardsViewModel.kt:64). Removing the same favourite from Home offers an | Confirmed | Fixed |
| F130 | app/src/main/java/dev/stan/yotsuba/feature/boards/BoardsViewModel.kt:92 | 2 spaghetti growth | medium | c65bad2 inserted a special case in the middle of `onToggleBoardVisible`: when the board's category is hidden, it un-hides the category and instead hides every sibling (`hiddenBoards = s.hiddenBoards - board + siblings`). This is a symptom of visibility being stored in two overlapping sets (`hiddenBo | Confirmed | Deferred (Collapsing hidden boards and hidden categories into one set changes which boards appear (boards added later to a hidden category) and needs a settings migration; not behaviour-preserving.) |
| F131 | app/src/main/java/dev/stan/yotsuba/feature/boards/BoardsViewModel.kt:130 | ui/ux | medium | `search()` ranks results 0..3 (exact code, code prefix, code contains, title contains) and its KDoc claims this "puts /g/ first instead of every title with a g in it" — but the ranked list is immediately regrouped by category at line 48 (`BoardCategory.entries.mapNotNull { cat -> visible.filter { it | Confirmed | Fixed |
| F132 | app/src/main/java/dev/stan/yotsuba/feature/bookmarks/BookmarksList.kt:166 | 2 spaghetti growth | medium | The remove-with-undo block is written twice in the same composable: lines 138-142 (swipe path) and lines 166-172 (sheet path) both do `viewModel.onRemove(bookmark); scope.launch { snackbar.showUndo(removedMessage, undoLabel) { viewModel.onUndoRemove(bookmark) } }`. The identical five-line shape is c | Confirmed | Fixed |
| F133 | app/src/main/java/dev/stan/yotsuba/feature/bookmarks/BookmarksViewModel.kt:132 | 0 structural simplification | medium | `override fun onCleared() { refreshJob?.cancel() }` — `refreshJob` is launched in `viewModelScope` (line 86), which `ViewModel` cancels itself on clear, so the body is a no-op. The override also drops `super.onCleared()`, which is the kind of omission that becomes a leak the day a base class or a li | Confirmed | Fixed |
| F134 | app/src/main/java/dev/stan/yotsuba/feature/catalog/CatalogPane.kt:158 | ui/ux | medium | Tapping the search icon (`CatalogActions`, CatalogScreen.kt:86) sets `searchQuery = ""` in the VM, which makes the `SearchField` appear at CatalogPane.kt:158-167 — but nothing requests focus or raises the keyboard, so the user must tap the field as a second step. Compare BoardsScreen.kt:94, where th | Confirmed | Fixed |
| F135 | app/src/main/java/dev/stan/yotsuba/feature/catalog/CatalogScreen.kt:92 | ui/ux | medium | The layout button shows the icon of the *current* layout but acts as a cycler, so the icon reads as a state while behaving as a destination — and all three states share one `contentDescription`, `stringResource(R.string.catalog_layout_switch)` (line 99). TalkBack therefore announces the identical st | Confirmed | Fixed |
| F136 | app/src/main/java/dev/stan/yotsuba/feature/catalog/CatalogViewModel.kt:53 | 6 canonical layer | medium | `networkMonitor.status.stateIn(viewModelScope, SharingStarted.Eagerly, NetworkStatus.Unmetered)`. `NetworkMonitor.status` is a plain `callbackFlow` (NetworkMonitor.kt:35-46) — not shared — so every collector registers its own `ConnectivityManager.registerNetworkCallback`. `Eagerly` keeps that regist | Confirmed | Fixed |
| F137 | app/src/main/java/dev/stan/yotsuba/feature/catalog/CatalogViewModel.kt:83 | 0 structural simplification | medium | `combine(result.flow, searchQuery, refreshing, layout, combine(hiddenNos, offline, matcher, ::Triple)) { res, query, isRefreshing, layout, (hidden, offline, matcher) -> ... }` — a nested `combine` producing an anonymous `Triple` that is destructured back out, purely to dodge the 5-argument arity lim | Confirmed | Fixed |
| F138 | app/src/main/java/dev/stan/yotsuba/feature/catalog/CatalogViewModel.kt:86 | 6 canonical layer | medium | The `when (res) { null -> UiState.Loading; is DataResult.Failure -> UiState.Error(res.error); is DataResult.Success -> UiState.Success(...) }` ladder is copy-pasted verbatim across ViewModels: here (lines 86-117), BoardsViewModel.kt:41-70, and ThreadViewModel.kt:127. `LoadableFlow` and `UiState` alr | Confirmed | Fixed |
| F139 | app/src/main/java/dev/stan/yotsuba/feature/catalog/CatalogViewModel.kt:97 | 6 canonical layer | medium | Commits 43eb208 (catalog) and 7a7723a (thread) landed the same feature — apply filters, hide HIDE matches, stub STUB matches, count what was caught — in two different shapes one after the other. The catalog version (lines 97-106) mutates a `mutableMapOf` and a `var hiddenByFilter` from inside a `fil | Confirmed | Fixed |
| F140 | app/src/main/java/dev/stan/yotsuba/feature/history/HistoryList.kt:140 | 6 canonical layer | medium | The design system already owns both halves of this: `SearchField` (StateViews.kt:109-123, an OutlinedTextField with a hint res and singleLine) and `NoSearchResults` (StateViews.kt:99-106, EmptyState with `search_no_results_title` = "No matches" / `search_no_results_explanation` = "Nothing matches \" | Confirmed | Fixed |
| F141 | app/src/main/java/dev/stan/yotsuba/feature/history/HistoryViewModel.kt:62 | bug | medium | `board.contains(q.removePrefix("/").removeSuffix("/"), ignoreCase = true)` — for the query `"/"` (or `"//"`) both strips fire and the argument becomes `""`. `String.contains("")` is always true, so typing a single slash in the history search box matches every entry instead of none. The `query.isBlan | Confirmed | Fixed |
| F142 | app/src/main/java/dev/stan/yotsuba/feature/home/HomeScreen.kt:84 | bug | medium | Commit 3604285 documents at lines 66-68 that "The current page survives tab switches via rememberSaveable". It does not survive process death. `pageCount` is `boards?.size ?: 0` (line 82) and `boards` is a StateFlow whose initial value is deliberately `null` (HomeViewModel.kt:220-222, "Null until se | Confirmed | Fixed |
| F143 | app/src/main/java/dev/stan/yotsuba/feature/home/HomeScreen.kt:98 | 4 magic/wrappers | medium | `val boardInfo by (currentViewModel?.boardInfo ?: remember { kotlinx.coroutines.flow.MutableStateFlow(null) }).collectAsStateWithLifecycle()` — a throwaway `MutableStateFlow` is allocated purely so the elvis chain type-checks, written with a fully-qualified name because the file deliberately avoids  | Confirmed | Fixed |
| F144 | app/src/main/java/dev/stan/yotsuba/feature/home/HomeScreen.kt:116 | ui/ux | medium | `list == null -> Unit`: while the settings store is loading, Home renders an empty body under the app bar — no skeleton, no spinner. `LoadingSkeleton` (StateViews.kt:187) exists precisely for this and every other screen in the app uses it via `UiStateContent`. On a cold start the user sees a blank p | Confirmed | Fixed |
| F145 | app/src/main/java/dev/stan/yotsuba/feature/home/HomeScreen.kt:136 | 2 spaghetti growth | medium | Commit 131a5c2 splits one gesture across two components with no shared model. `ReorderableTabRow` owns the drag and decides the drop target with a hard-coded `val removeThreshold = with(density) { 32.dp.toPx() }` and `val overRemove = from != null && dragY > removeThreshold` (ReorderableTabRow.kt:80 | Confirmed | Fixed |
| F146 | app/src/main/java/dev/stan/yotsuba/feature/home/HomeViewModel.kt:45 | bug | medium | `removeFavourite` returns an undo closure over `var before: Set<String> = emptySet()`, but `before` is only assigned inside the `viewModelScope.launch` at line 46. The returned lambda is handed straight to the snackbar action (HomeScreen.kt:133-134) and can run before that coroutine's `settingsRepos | Confirmed | Fixed |
| F147 | app/src/main/java/dev/stan/yotsuba/feature/home/HomeViewModel.kt:45 | 7 orchestration/atomicity | medium | In `removeFavourite` (added by f11d0db), `var before: Set<String>` is assigned inside `viewModelScope.launch { settingsRepository.update { ... } }` but read from the returned undo lambda, which runs in a different coroutine whenever the user taps the snackbar's Undo. There is no ordering guarantee o | Confirmed | Fixed |
| F148 | app/src/main/java/dev/stan/yotsuba/feature/home/ReorderableTabRow.kt:135 | bug | medium | Line 87 guards the drag index (`if (from != null && from < widths.size)`) but line 135 does not: `val shift = if (from == null) 0 else shiftFor(index, from, target) * widths[from]`. `widths` is truncated to `boards.size` at line 74, so if `boards` shrinks while `dragging != null`, `widths[from]` thr | Confirmed | Fixed |
| F149 | app/src/main/java/dev/stan/yotsuba/feature/home/ReorderableTabRow.kt:238 | ui/ux | medium | `BoardTab` is a `Box(...).clickable(onClick = onClick)` with no `role` and no selection semantics; selection is conveyed purely by text colour (line 244) and the drawn indicator at line 213-221. TalkBack announces it as an unlabelled button and never says "selected", so a screen-reader user cannot t | Confirmed | Fixed |
| F150 | app/src/main/java/dev/stan/yotsuba/feature/media/DownloadAction.kt:110 | 4 magic/wrappers | medium | `SaveStatusIcon` hardcodes `Color(0xFF81C784)` for saved (:110), `Color(0xFFE57373)` for failed (:125) and `Color(0xFF81C784).copy(alpha = 0.7f)` for already-saved (:130) — raw Material-2 palette hex, in a codebase whose theme defines a full tonal scheme plus semantic extras (theme/Color.kt YotsubaC | Confirmed | Fixed |
| F151 | app/src/main/java/dev/stan/yotsuba/feature/media/MediaFeedViewer.kt:243 | ui/ux | medium | `MediaFeedViewer` fires the long-press haptic unconditionally before delegating: `onLongPress = { haptics.longPress(); onLongPressPage(page) }` at line 243 (video) and 258 (image). But `onLongPressPage` defaults to `{}` and VaultScreen.kt:646-658 passes nothing — so in the vault every long-press buz | Confirmed | Fixed |
| F152 | app/src/main/java/dev/stan/yotsuba/feature/media/MediaScreen.kt:115 | 2 spaghetti growth | medium | `pages = state.items.map { it.toViewerPage(context, state) }` runs on every recomposition of MediaScreen — and MediaScreen recomposes on every `saveStatuses` tick, every storage-access refresh and every settings change. `toViewerPage` (:222-256) allocates a fresh `ImageRequest.Builder(context)...bui | Confirmed | Fixed |
| F153 | app/src/main/java/dev/stan/yotsuba/feature/media/MediaScreen.kt:145 | ui/ux | medium | The live viewer's trailing lambda is `{ page, _ ->` — it discards the `openReplies` callback that `ThreadMediaViewer` passes (ThreadMediaViewer.kt:49, :113) and renders only DownloadAction and Share. The vault viewer uses it and renders a labelled, badge-counted Comment button (VaultScreen.kt:659-67 | Confirmed | Fixed |
| F154 | app/src/main/java/dev/stan/yotsuba/feature/media/MediaShare.kt:26 | 6 canonical layer | medium | `mimeOf(ext)` — the extension→MIME table — is a public function in `feature/media`. `feature/vault/VaultShare.kt:7` already reaches across features to import it, and `data/repository/MediaVaultRepositoryImpl.kt:237-246` carries a byte-for-byte copy of the same `when` block (`.jpg/.jpeg`, `.png`, `.g | Confirmed | Fixed |
| F155 | app/src/main/java/dev/stan/yotsuba/feature/media/MediaShare.kt:44 | ui/ux | medium | `runCatching { context.startActivity(Intent.createChooser(intent, null)) }` swallows the failure with no `onFailure`; same in `shareVaultEntries` (VaultShare.kt:33). If the chooser cannot start (no handler, FileProvider path mismatch, a transient ActivityNotFound), the user taps Share on the viewer  | Confirmed | Fixed |
| F156 | app/src/main/java/dev/stan/yotsuba/feature/media/MediaViewModel.kt:79 | 4 magic/wrappers | medium | `MediaUiState` carries six pass-through members over `thread`/`saved`: `posts` (79), `backlinks` (80), `board` (81), `graph` (84), plus `loaded` (74) and `isSaved` (75). A grep across `app/src/main` finds zero production callers of `state.backlinks`, `state.board`, `state.isSaved(...)` or `state.loa | Confirmed | Fixed |
| F157 | app/src/main/java/dev/stan/yotsuba/feature/media/MediaViewModel.kt:111 | 5 type/boundary | medium | `private val details = MutableStateFlow<ThreadDetails?>(null)` (111) holds exactly what `Source.Loaded(val details: ThreadDetails)` (107) already holds. `load()` writes both by hand (lines 134 and 136) and nothing keeps them in step afterwards. `details` is not part of the `combine` at 148 — it exis | Confirmed | Fixed |
| F158 | app/src/main/java/dev/stan/yotsuba/feature/media/MediaViewModel.kt:128 | 7 orchestration/atomicity | medium | `load()` awaits `threadRepository.thread(board, threadNo)` (128), then — and only then — falls into `mediaVault.savedThread(...)` if it failed (131) and finally awaits `boardRepository.board(board)` (133). The board lookup is independent of the thread fetch and can itself hit the network: `BoardRepo | Confirmed | Fixed |
| F159 | app/src/main/java/dev/stan/yotsuba/feature/media/MediaViewModel.kt:173 | 6 canonical layer | medium | Settings -> ViewerBehaviour mapping is written twice: MediaViewModel L173-178 (`ViewerBehaviour(keepScreenOn = settings.keepScreenOnWhileWatching, doubleTapSeek = settings.doubleTapSeekEnabled, seekStepSeconds = settings.seekStep.seconds, holdToSave = settings.holdToSave)`) and VaultViewModel L252-2 | Rejected (Misreads the code: VaultViewModel's omission of holdToSave is deliberate and documented ('Saving does not apply here', VaultViewModel.kt:249), not silent drift.) | Deferred (rejected in verification: Misreads the code: VaultViewModel's omission of holdToSave is deliberate and documented ('Saving does not apply here', VaultViewModel.kt:249), not silent drift. What remains is a four-line data-class construction in two places; a companion factory with a defaulted flag is a thin wrapper, not a canonical-layer violation.) |
| F160 | app/src/main/java/dev/stan/yotsuba/feature/media/MediaViewModel.kt:210 | 6 canonical layer | medium | `conversationFor` decides whether to capture replies by reading `uiState.value.saveReplies` (210) — i.e. the setting is round-tripped through a `stateIn(WhileSubscribed(5_000))` projection instead of being read from `settingsRepository`. `MediaUiState()`'s default is `saveReplies = false`, so any `e | Confirmed | Fixed |
| F161 | app/src/main/java/dev/stan/yotsuba/feature/media/SoundTrack.kt:45 | 6 canonical layer | medium | SoundTrack.kt:45-57 and VideoPage.kt:153-165 are the same block twice: `val lifecycleOwner = LocalLifecycleOwner.current`, `val shouldPlay = rememberUpdatedState(...)`, `DisposableEffect(lifecycleOwner, player) { LifecycleEventObserver { _, event -> ON_STOP -> player.playWhenReady = false; ON_START  | Confirmed | Fixed |
| F162 | app/src/main/java/dev/stan/yotsuba/feature/media/ViewerChrome.kt:69 | 6 canonical layer | medium | MotionSpecs.kt states the contract: 'Everything here collapses to its end state under reduced motion, so a call site never checks that itself' (MotionSpecs.kt:24-27), and provides rememberMotionSpec/motionEnter/motionExit. Three animations in this slice bypass it and animate regardless of `LocalRedu | Confirmed | Fixed |
| F163 | app/src/main/java/dev/stan/yotsuba/feature/settings/sections/FiltersSection.kt:314 | 6 canonical layer | medium | `private val FilterField.labelRes` (314-323) and `private val FilterAction.labelRes` (325-329) are defined at the bottom of a UI file in package `feature.settings.sections`, while `SettingsLabels.kt` in `feature.settings` exists for exactly this and carries the doc comment "String resources for the  | Confirmed | Fixed |
| F164 | app/src/main/java/dev/stan/yotsuba/feature/settings/sections/StorageSection.kt:36 | 5 type/boundary | medium | Six of the twelve parameters carry defaults — `restoreAvailable: BackupInfo? = null, backupResult: BackupResult? = null, onExportBackup: () -> Unit = {}, onImportBackup: () -> Unit = {}, onDismissRestore: () -> Unit = {}, onBackupResultShown: () -> Unit = {}` (lines 36-41) — yet the single call site | Confirmed | Fixed |
| F165 | app/src/main/java/dev/stan/yotsuba/feature/settings/sections/StorageSection.kt:45 | ui/ux | medium | `fun clear(bodyRes: Int, action: () -> Unit) = confirmThen(bodyRes) { action(); showMessage(cleared) }`. Every `action` here is a fire-and-forget `viewModelScope.launch` (SettingsViewModel.kt:106-108: `onClearCache`, `onClearHistory`, `onClearBookmarks`). The "Cleared" snackbar is therefore shown th | Confirmed | Fixed |
| F166 | app/src/main/java/dev/stan/yotsuba/feature/settings/SettingsSectionScreen.kt:53 | 7 orchestration/atomicity | medium | `viewModel: SettingsViewModel = hiltViewModel()` inside a NavHost destination scopes the ViewModel to the NavBackStackEntry, so every settings subscreen constructs its own `SettingsViewModel` — and every construction runs `init { viewModelScope.launch { if (backupRepository.isFreshInstall()) _restor | Confirmed | Fixed |
| F167 | app/src/main/java/dev/stan/yotsuba/feature/settings/SettingsViewModel.kt:45 | 4 magic/wrappers | medium | `private val backupRepository: BackupRepository = BackupRepository.None` sits on an `@Inject constructor` of a `@HiltViewModel`. Dagger ignores Kotlin default arguments entirely, and `core/di/Modules.kt:159` binds `BackupRepositoryImpl`, so in the app this default is unreachable. Its only consumer i | Confirmed | Fixed |
| F168 | app/src/main/java/dev/stan/yotsuba/feature/settings/UpdatesSection.kt:38 | ui/ux | medium | `var needsPermission by remember { mutableStateOf(false) }` is set to true when Install is tapped without the install-packages permission (line 123) and only ever cleared by a second successful tap (line 126). The tap also fires `onRequestInstallPermission()`, which leaves the app for the system "in | Confirmed | Fixed |
| F169 | app/src/main/java/dev/stan/yotsuba/feature/thread/components/ExternalLinkDialog.kt:27 | ui/ux | medium | The `dismissButton` slot holds a `Row` with two visually identical `TextButton`s: "Always trust <domain>" and "Cancel". The trust action is persistent and security-relevant (it writes to `settings.trustedDomains`, ThreadViewModel.kt:511-515) and it *also* opens the link (ThreadScreen.kt:466-469), ye | Confirmed | Fixed |
| F170 | app/src/main/java/dev/stan/yotsuba/feature/thread/components/PostCard.kt:75 | 5 type/boundary | medium | `PostCardActions` encodes the card's three contexts (list, preview sheet, media-viewer reply panel) as ten independently-nullable callbacks plus a `forPreview()` method that nulls six of them (lines 94-99). The card then reads those nulls as mode switches rather than as "no handler": line 207 `val s | Confirmed | Fixed |
| F171 | app/src/main/java/dev/stan/yotsuba/feature/thread/components/PostCard.kt:86 | 2 spaghetti growth | medium | dab9640 replaced the backlink count chip with a "Quoted by" row but kept the old one alive behind a second nullable callback, encoding *which widget to draw* in which lambda happens to be non-null: PostCardActions.onBacklinksTap at line 86 is documented "Legacy count chip, shown only when [onBacklin | Confirmed | Fixed |
| F172 | app/src/main/java/dev/stan/yotsuba/feature/thread/components/PostCard.kt:207 | 4 magic/wrappers | medium | ``` // Only the list's own card shares with the viewer: a preview or the // viewer's reply panel would claim the same key twice on one screen. val shared = actions.onThumbnailLongPress != null val sharedWithViewer = if (shared) Modifier.sharedMedia(media.fullUrl) else Modifier val sharedWithCatalog  | Confirmed | Fixed |
| F173 | app/src/main/java/dev/stan/yotsuba/feature/thread/components/PostCard.kt:208 | 4 magic/wrappers | medium | Commit e3f3843 stacks two shared-element modifiers on one layout node: `.then(sharedWithViewer).then(sharedWithCatalog)` (lines 223-224), where `sharedWithViewer = Modifier.sharedMedia(media.fullUrl)` (line 208) and `sharedWithCatalog = Modifier.sharedMedia(media.thumbnailUrl)` (lines 209-211) for t | Rejected (The stacking is real (208-211 applied at 223-224), but the finding is speculation dressed as a defect: it asserts Compose 'is specified for one shared-content s) | Deferred (rejected in verification: The stacking is real (208-211 applied at 223-224), but the finding is speculation dressed as a defect: it asserts Compose 'is specified for one shared-content state per node' without evidence, and names no concrete misbehaviour — the two keys can never match at once, since the catalog is not composed during a thread->viewer transition and the viewer never publishes the thumbnail key. The proposed fix also fails the behaviour bar: `cameFromCatalog` does not exist anywhere in the tree, and having the catalog publish media.fullUrl means widening the catalog's data path (CatalogPane.kt:303 has only thumbnailUrl), which changes what animates on both screens.) |
| F174 | app/src/main/java/dev/stan/yotsuba/feature/thread/components/PostCard.kt:329 | 6 canonical layer | medium | PostCard.SaveStatusBadge L329-359 and DownloadAction.SaveStatusIcon (feature/media/DownloadAction.kt L105-138) are the same `when (status)` over MediaSaveStatus with the same icons (DownloadDone/Schedule/spinner/Download-or-ErrorOutline/DownloadDone@0.7) and the same hard-coded colours 0xFF81C784 an | Rejected (Misreads the code: the two mappings are not the same. PostCard tints Queued Color.White while DownloadAction uses White.copy(alpha = 0.7f); PostCard's Failed ic) | Deferred (rejected in verification: Misreads the code: the two mappings are not the same. PostCard tints Queued Color.White while DownloadAction uses White.copy(alpha = 0.7f); PostCard's Failed icon is ErrorOutline, DownloadAction's is Download; DownloadAction also has a null branch PostCard's non-null parameter lacks. A shared composable with the proposed signature would change the badge's Queued tint and Failed glyph, and parameterising those differences away leaves only two shared colour constants — not enough to earn the extra component.) |
| F175 | app/src/main/java/dev/stan/yotsuba/feature/thread/components/QuotePreviewSheet.kt:71 | 2 spaghetti growth | medium | `val scrollStates = remember { mutableMapOf<Long, LazyListState>() }` is then mutated during composition on the next two lines: `scrollStates.keys.retainAll(preview.path.toSet())` and `scrollStates.getOrPut(focus.no) { LazyListState() }`. Composition is meant to be side-effect free and can be re-run | Confirmed | Fixed |
| F176 | app/src/main/java/dev/stan/yotsuba/feature/thread/components/ThreadTopBar.kt:79 | ui/ux | medium | The subtitle slot is an if/else-if chain over three unrelated signals: the poster-ID filter chip, the "N replies to you" indicator, and the "N posts filtered" count (lines 79-102). Only one can ever show. So filtering by poster ID silently hides the fact that someone replied to you, and a single rep | Confirmed | Fixed |
| F177 | app/src/main/java/dev/stan/yotsuba/feature/thread/components/ThreadTopBar.kt:96 | ui/ux | medium | The top bar's subtitle slot is a single `if / else if / else if` chain over three unrelated indicators (lines 79-102): the poster-ID filter chip, "N replies to you" (added by the thread-reader merge 9f0fbd0), and "N filtered" (added by 7a7723a at line 96). Each commit appended one more `else if` to  | Rejected (Duplicate of F176 — same lines, same defect, same remedy; the only extra content is commit archaeology. Keep F176, which additionally catches the sub-48dp untyp) | Deferred (rejected in verification: Duplicate of F176 — same lines, same defect, same remedy; the only extra content is commit archaeology. Keep F176, which additionally catches the sub-48dp untyped tap target.) |
| F178 | app/src/main/java/dev/stan/yotsuba/feature/thread/ThreadScreen.kt:106 | ui/ux | medium | `var searchOpen by remember { mutableStateOf(false) }` is likewise not saveable, while the query lives in the VM's `Session` (ThreadUiState.kt:129) and survives rotation. After a rotation the search bar (and its match counter and next/prev buttons, ThreadScreen.kt:479-506) is gone but `s.searchQuery | Confirmed | Fixed |
| F179 | app/src/main/java/dev/stan/yotsuba/feature/thread/ThreadScreen.kt:191 | ui/ux | medium | All three jump buttons animate across the whole list: `onTop = { listState.animateScrollToItem(0) }` (:191), `onFirstNew = { listState.animateScrollToItem(index) }` (:192), `onBottom = { listState.animateScrollToItem(rows.lastIndex.coerceAtLeast(0)) }` (:193); the catalog's scroll-to-top FAB does th | Confirmed | Fixed |
| F180 | app/src/main/java/dev/stan/yotsuba/feature/thread/ThreadScreen.kt:348 | 3 design cleanliness | medium | The thread list uses `items(count = s.rows.size, key = { ... })` with no `contentType`, while the item body (:360-378) renders four structurally different composables depending on the row type: a `PostCard` inside a padded Box, a `NewPostsDivider` (which additionally starts an `AnimatedVisibility` t | Confirmed | Fixed |
| F181 | app/src/main/java/dev/stan/yotsuba/feature/thread/ThreadViewModel.kt:187 | 7 orchestration/atomicity | medium | The two visible-post consumers are set up side by side but debounce inconsistently: ``` topVisiblePostNo.filterNotNull().distinctUntilChanged().collectLatest { postNo ->     delay(500)     historyRepository.updateScrollPosition(board, threadNo, postNo) } ... bottomVisiblePostNo.filterNotNull().disti | Rejected (The code is at line 187 as cited, but the proposed fix breaks an existing test: ThreadViewModelTest.kt:516-523 drives onVisiblePostsChanged and asserts env.hist) | Deferred (rejected in verification: The code is at line 187 as cited, but the proposed fix breaks an existing test: ThreadViewModelTest.kt:516-523 drives onVisiblePostsChanged and asserts env.history.readMark/env.bookmarks.seen after only dispatcher.scheduler.runCurrent(), which never advances virtual time, so a delay(500) inside collectLatest would leave the mark unwritten. Beyond that it is a micro-optimization of write volume with identical final state — rule 7 explicitly says not to over-index on those — and it introduces a real behavioural edge (leaving the thread within 500ms of the last scroll now drops the read mark), so it is not the behaviour-preserving cleanup it claims.) |
| F182 | app/src/main/java/dev/stan/yotsuba/feature/thread/ThreadViewModel.kt:543 | 6 canonical layer | medium | Lines 543-684 are a 140-line `companion object` holding the whole presentation-model pipeline — `visiblePosts`, `filterVerdicts`, `filteredRow`, `rows`, `linearRows`, `treeRows` (a 26-line index-walking loop with its own `error("hidden posts were dropped above")` invariant at line 610), `previewShee | Confirmed | Fixed |
| F183 | app/src/main/java/dev/stan/yotsuba/feature/threads/ThreadsScreen.kt:73 | ui/ux | medium | `var searching by remember { mutableStateOf(false) }` sits one line below `var segment by rememberSaveable { ... }` (line 67) — the author saved the segment but not the search toggle. The query itself lives in `HistoryViewModel` (HistoryViewModel.kt:98 `private val query = MutableStateFlow("")`), an | Confirmed | Fixed |
| F184 | app/src/main/java/dev/stan/yotsuba/feature/threads/ThreadsScreen.kt:99 | 6 canonical layer | medium | The Threads shell owns another feature's app-bar state and wiring: it holds `searching` and `confirmClear` (lines 73-74), builds History's search and clear-all icon buttons inline (lines 99-117) including the `history.totalCount > 0` enablement rules, places `HistorySearchField` into the title slot  | Confirmed | Fixed |
| F185 | app/src/main/java/dev/stan/yotsuba/feature/vault/VaultDedupSheet.kt:311 | 4 magic/wrappers | medium | `DateFormat.getDateInstance(DateFormat.SHORT).format(Date(entry.savedAt))` is called inside `Thumb`, a composable rendered once per duplicate file inside a `LazyRow` inside a `LazyColumn` (250-251). `DateFormat.getDateInstance` builds a fresh SimpleDateFormat (pattern lookup + symbol tables) on ever | Confirmed | Fixed |
| F186 | app/src/main/java/dev/stan/yotsuba/feature/vault/VaultEntrySheet.kt:76 | 6 canonical layer | medium | Four private near-identical "ListItem(headlineContent = Text, leadingContent = Icon, modifier = clickable)" sheet rows: VaultEntrySheet.SheetAction L76-82, PostActionSheet.SheetRow L69-75, BookmarksList.SheetAction L220-234 (adds enabled/supporting), CatalogPane.ThreadActionsSheet L247-261 (three in | Confirmed | Fixed |
| F187 | app/src/main/java/dev/stan/yotsuba/feature/vault/VaultExplorer.kt:114 | 5 type/boundary | medium | `val view = Triple(state.sort, state.reversed, state.filter)` is allocated fresh on every recomposition of VaultExplorer and passed to three list composables declared as `view: Any` (:218 BoardList, :256 ThreadList, :483 MediaGrid). The compose compiler report names it explicitly — app-composables.t | Confirmed | Fixed |
| F188 | app/src/main/java/dev/stan/yotsuba/feature/vault/VaultExplorer.kt:117 | ui/ux | medium | `VaultUiState()` defaults `hasStorageAccess = false` (VaultViewModel.kt:191) and `uiState` is `stateIn(..., VaultUiState())` behind `.flowOn(compute)` (VaultViewModel.kt:468-469), so the first composed frame of the vault tab always takes the `!state.hasStorageAccess` branch and paints the 'The vault | Confirmed | Fixed |
| F189 | app/src/main/java/dev/stan/yotsuba/feature/vault/VaultExplorer.kt:131 | 6 canonical layer | medium | Three bespoke empty/error states here reimplement what core/designsystem/component/StateViews.kt already provides: the no-content state (:131-141) is a bare Column of two Texts where `EmptyState(title, explanation, icon, action)` exists (StateViews.kt:68); the search-empty state (:145-151) is a cent | Confirmed | Fixed |
| F190 | app/src/main/java/dev/stan/yotsuba/feature/vault/VaultExplorer.kt:340 | 6 canonical layer | medium | The same "SingleChoiceSegmentedButtonRow over an enum's entries, forEachIndexed, SegmentedButton(selected = x == option, shape = itemShape(i, size), label = stringResource(when(option) ...))" block appears four times: VaultExplorer.ModeSwitch L340-362, VaultExplorer.VaultChipRow L429-449 (icon varia | Confirmed | Fixed |
| F191 | app/src/main/java/dev/stan/yotsuba/feature/vault/VaultExplorer.kt:483 | 5 type/boundary | medium | `MediaGrid(view: Any, ...)`, `ThreadList(view: Any, ...)` (:256) and `BoardList(view: Any, ...)` (:218) take an untyped `Any` whose only use is `LaunchedEffect(view) { scrollToItem(0) }`. The caller builds it as `val view = Triple(state.sort, state.reversed, state.filter)` (:114). The type says noth | Rejected (Duplicate of F187 (same `view: Any` parameters, same fix, cited at MediaGrid rather than the origin).) | Deferred (rejected in verification: Duplicate of F187 (same `view: Any` parameters, same fix, cited at MediaGrid rather than the origin).) |
| F192 | app/src/main/java/dev/stan/yotsuba/feature/vault/VaultExplorer.kt:503 | ui/ux | medium | Each grid cell is a bare `Box(Modifier.aspectRatio(1f).combinedClickable(onClick = …, onLongClick = …))` with no `role` and no selection semantics; the tick that communicates selected/not-selected is an `Icon(..., contentDescription = null)` (:541-546) and the only other cue is a 6.dp inset on the t | Confirmed | Fixed |
| F193 | app/src/main/java/dev/stan/yotsuba/feature/vault/VaultExplorer.kt:604 | 6 canonical layer | medium | 656c59f added `internal fun formatDuration(ms: Long)` at VaultExplorer.kt:604-610 and a file-level `private val dateFormat = DateFormat.getDateInstance(DateFormat.MEDIUM)` + `internal fun savedDate(millis)` at :612-614, inside a 628-line Compose file. Both duplicate work that already exists elsewher | Confirmed | Fixed |
| F194 | app/src/main/java/dev/stan/yotsuba/feature/vault/VaultScreen.kt:113 | 6 canonical layer | medium | `statsOpen` (113) and `dedupOpen` (114) are plain `remember { mutableStateOf(false) }`, not `rememberSaveable` and not in the ViewModel — while this very screen deliberately hoists explorer position, viewer url, query, inspected entry and pending delete into the VM because, per VaultViewModel.kt:262 | Rejected (The claimed symptom does not hold. The activity declares android:configChanges="orientation\|screenSize\|screenLayout\|…" (AndroidManifest.xml:32), so rotation ) | Deferred (rejected in verification: The claimed symptom does not hold. The activity declares android:configChanges="orientation\|screenSize\|screenLayout\|…" (AndroidManifest.xml:32), so rotation does not recreate it, and the nav bar↔rail switch is an `if` block that is a *sibling* of the Scaffold inside one Row (AppNavHost.kt:112-127) — the NavHost/VaultScreen subtree keeps its composition identity, so the `remember`ed flags survive. With the stated user-visible bug gone, what remains is a preference for hoisting two transient sheet booleans into the ViewModel, which adds a sealed VaultSheet type, a state flow and a UiState field for no behaviour gain.) |
| F195 | app/src/main/java/dev/stan/yotsuba/feature/vault/VaultScreen.kt:193 | 2 spaghetti growth | medium | c96f983 added `BackHandler(enabled = state.viewer != null) { closeViewer() }` as the last handler and, in the same commit, bolted `&& state.viewer == null` onto the drill-down handler. Because BackHandler callbacks fire in reverse registration order (last-composed wins — the commit's own comment sta | Confirmed | Fixed |
| F196 | app/src/main/java/dev/stan/yotsuba/feature/vault/VaultScreen.kt:242 | 1 file size | medium | `VaultScreen` runs from line 95 to 461 — a single ~370-line composable holding notice/undo snackbar effects, two picker launchers, four BackHandlers, the whole top bar, the FAB, the explorer, the viewer overlay and five dialogs/sheets. The `actions = { ... }` lambda alone spans 242-350: four sibling | Rejected (Duplicate of F066: same file, same VaultScreen 95-461 span, same topBar/actions block, same proposed extraction into VaultTopBar with ImportMenu/SyncMenu. It ci) | Deferred (rejected in verification: Duplicate of F066: same file, same VaultScreen 95-461 span, same topBar/actions block, same proposed extraction into VaultTopBar with ImportMenu/SyncMenu. It cites line 242 (`actions = {`) instead of 204 (`topBar = {`), but that is a nested part of the very block F066 already covers; reporting it twice adds nothing actionable.) |
| F197 | app/src/main/java/dev/stan/yotsuba/feature/vault/VaultScreen.kt:297 | ui/ux | medium | The toolbar button at line 297-298 is a Refresh icon labelled `R.string.vault_sync` ("sync"), but its dropdown has become a junk drawer of four unrelated things: Rescan and Fetch new replies (sync), Statistics (added by bfd7d5d, line 324-333) and Duplicates (line 334-343). Two of the four have nothi | Confirmed | Fixed |
| F198 | app/src/main/java/dev/stan/yotsuba/feature/vault/VaultScreen.kt:406 | ui/ux | medium | `val stats by viewModel.stats.collectAsStateWithLifecycle()` sits inside `if (statsOpen)` (VaultScreen.kt:405-407), so nothing subscribes to the `WhileSubscribed(5_000)` flow until the sheet is already being composed. The first value the sheet sees is therefore the `stateIn` initial value, `VaultSta | Confirmed | Fixed |
| F199 | app/src/main/java/dev/stan/yotsuba/feature/vault/VaultStatsSheet.kt:49 | 4 magic/wrappers | medium | `token/Spacing.kt` defines xs/sm/md/lg/xl/xxl and most of the app reads `LocalSpacing.current`, but the vault sheets and grid ignore it wholesale: VaultStatsSheet uses literal 16/12/32/24/8/4/2/96/3.dp (:49, :52, :61, :74, :87, :114, :122, :131, :144, :179, :184, :194, :202, :208) and never touches  | Confirmed | Fixed |
| F200 | app/src/main/java/dev/stan/yotsuba/feature/vault/VaultStatsSheet.kt:217 | 6 canonical layer | medium | `private val dateFormat: DateFormat = DateFormat.getDateInstance(DateFormat.MEDIUM)` here is byte-for-byte the same declaration as VaultExplorer.kt:612, which already backs the shared `internal fun savedDate(millis: Long)` (VaultExplorer.kt:614) used by ThreadList (281) and `entryDetails` (VaultEntr | Confirmed | Fixed |
| F201 | app/src/main/java/dev/stan/yotsuba/feature/vault/VaultViewModel.kt:425 | 5 type/boundary | medium | `@Suppress("UNCHECKED_CAST") val uiState = combine(mediaVault.entries(), syncState, selection, viewing, activity, editing) { args -> val entries = args[0] as List<VaultEntry>; ... args[5] as Editing }` -- six unchecked casts from `Array<Any?>`. todo.md §1 records the same smell being fixed in Thread | Rejected (Duplicate of F068 (same 6-arity array combine and unchecked casts at VaultViewModel.kt:424-433); F068 carries the simpler fold that keeps the existing Editing/A) | Deferred (rejected in verification: Duplicate of F068 (same 6-arity array combine and unchecked casts at VaultViewModel.kt:424-433); F068 carries the simpler fold that keeps the existing Editing/Activity structure.) |
| F202 | app/src/main/java/dev/stan/yotsuba/feature/vault/VaultViewModel.kt:471 | 7 orchestration/atomicity | medium | `val stats: StateFlow<VaultStats> = mediaVault.entries().map { VaultStats.of(it, System.currentTimeMillis()) }.flowOn(compute).stateIn(...)` opens a second independent collection of the same Room-backed flow that `uiState` (VaultViewModel.kt:452, `.flowOn(compute)` at 467-468) is already collecting. | Confirmed | Fixed |
| F203 | app/src/main/java/dev/stan/yotsuba/feature/vault/VaultViewModel.kt:643 | 0 structural simplification | medium | Six commits in this slice each bolted one more field-plus-flow onto the same class (b7fa6cf delete dialog, d18372f delete outcome, 73afeae import/delete scoping, f8185ac playback, 2249904 storage access, 5107dca viewer thread). The result is 759 lines holding a 20-field `VaultUiState`, six combine i | Confirmed | Fixed |
| F204 | app/src/main/java/dev/stan/yotsuba/navigation/AppNavHost.kt:111 | bug | medium | `val openSettings = { navController.navigate(Route.Settings) }` — no `launchSingleTop`, no in-flight guard. The same lambda is handed to HomeScreen, BoardsScreen, ThreadsScreen and VaultScreen (lines 187, 193, 233, 239). Every other push in this file has the same shape (`navigate(Route.Thread(...))` | Confirmed | Fixed |
| F205 | app/src/main/java/dev/stan/yotsuba/navigation/AppNavHost.kt:164 | 6 canonical layer | medium | All four NavHost transition lambdas open with a reduced-motion branch: `if (reduced) EnterTransition.None else …` at 165-168, 170-171, 173-175, 177-180. `core/designsystem/MotionSpecs.kt` is the declared canonical home for this — its header says "Everything here collapses to its end state under redu | Rejected (Duplicate of F074: same file, same four lambdas (164-180), same rule and same proposed move into MotionSpecs.kt. Reporting it twice adds nothing.) | Deferred (rejected in verification: Duplicate of F074: same file, same four lambdas (164-180), same rule and same proposed move into MotionSpecs.kt. Reporting it twice adds nothing.) |
| F206 | app/src/main/java/dev/stan/yotsuba/navigation/AppNavHost.kt:305 | 4 magic/wrappers | medium | `private fun NavController.hasEntry(route: Route) = runCatching { getBackStackEntry(route) }.isSuccess` — the common case (following a quote to a board whose catalog is not open) throws `IllegalArgumentException` from inside Navigation and swallows it to produce a boolean. Exception-driven control f | Confirmed | Fixed |
| F207 | app/src/main/java/dev/stan/yotsuba/navigation/Route.kt:43 | 6 canonical layer | medium | `SettingsSectionId` lives in the navigation package and carries `titleRes: Int` and `icon: ImageVector` — pure Settings presentation data — which drags nine `androidx.compose.material.icons.outlined.*` imports and `dev.stan.yotsuba.R` into the routing file (Route.kt:4-15). The navigation layer only  | Confirmed | Fixed |
| F208 | app/src/main/res/values/strings.xml:6 | 3 design cleanliness | medium | 16 string resources have no reference from Kotlin or XML. Checked each with `rg -c "R\.(string\|plurals)\.<name>\b" app/src -g '*.kt'` = 0 and no `@string/` use, and `rg getIdentifier app/src` is empty so there is no reflective lookup: tab_bookmarks (6), tab_history (7), boards_category_toggle (43), | Confirmed | Fixed |
| F209 | app/src/main/res/values/strings.xml:122 | 6 canonical layer | medium | Two competing conventions for the same resources coexist: a 284-line `strings.xml` organised by feature comment banners (`<!-- Bookmarks -->` line 118, `<!-- History -->` line 131, `<!-- Settings -->` line 145) *and* 15 per-feature `strings_*.xml` files covering the same features. The result is supe | Confirmed | Fixed |
| F210 | app/src/test/java/dev/stan/yotsuba/data/BackupRepositoryImplTest.kt:97 | 7 orchestration/atomicity | medium | `Env` defaults `scope: CoroutineScope = CoroutineScope(Job())` — a real, never-cancelled scope on `Dispatchers.Default`. `BackupRepositoryImpl` uses that scope in its constructor for a live auto-export collector (`.debounce(AUTO_EXPORT_DEBOUNCE_MS).launchIn(scope)`, BackupRepositoryImpl.kt:54-56). F | Confirmed | Fixed |
| F211 | app/src/test/java/dev/stan/yotsuba/feature/boards/BoardsViewModelTest.kt:64 | 4 magic/wrappers | medium | The same three-line Turbine helper — `dispatcher.scheduler.advanceUntilIdle(); return expectMostRecentItem()` — is written out six times with only the type parameter changed: BoardsViewModelTest.kt:64, CatalogViewModelTest.kt:122, HistoryViewModelTest.kt:103, SettingsViewModelTest.kt:136, MediaViewM | Confirmed | Fixed |
| F212 | app/src/test/java/dev/stan/yotsuba/feature/catalog/CatalogViewModelTest.kt:49 | 6 canonical layer | medium | `private val dispatcher = StandardTestDispatcher()` + `@Before fun setUp() { Dispatchers.setMain(dispatcher) }` + `@After fun tearDown() { Dispatchers.resetMain() }` appears in 13 test classes (`grep -rln "Dispatchers.setMain" app/src/test` → 13 files: CatalogViewModelTest, BoardsViewModelTest, Hist | Confirmed | Fixed |
| F213 | app/src/test/java/dev/stan/yotsuba/feature/HistoryViewModelTest.kt:95 | 6 canonical layer | medium | `fake/FakeSettings.kt:9` carries the KDoc "Several ViewModel tests declare their own private copy of this; new tests should use this one." That instruction is ignored six more times, verbatim: HistoryViewModelTest.kt:95, BoardsViewModelTest.kt:51, CatalogViewModelTest.kt:75, MediaViewModelTest.kt:92 | Confirmed | Fixed |
| F214 | app/src/test/java/dev/stan/yotsuba/feature/MediaViewModelTest.kt:92 | 6 canonical layer | medium | app/src/test/java/dev/stan/yotsuba/fake/FakeSettings.kt exists and its own doc says "Several ViewModel tests declare their own private copy of this", yet six tests still do: MediaViewModelTest L92, HistoryViewModelTest L95, ThreadViewModelTest L130, SettingsViewModelTest L55, boards/BoardsViewModelT | Confirmed | Fixed |
| F215 | app/src/test/java/dev/stan/yotsuba/feature/ThreadViewModelTest.kt:526 | 2 spaghetti growth | medium | One 777-line file reads the same ui state three different ways. A member helper `private fun content(vm: ThreadViewModel): ThreadContent = (vm.uiState.value as UiState.Success).data` is declared at line 526 — buried between two `@Test` functions rather than with the other fixtures at the top. Five t | Confirmed | Fixed |
| F216 | app/src/test/java/dev/stan/yotsuba/feature/VaultViewModelTest.kt:168 | 0 structural simplification | medium | `VaultViewModel(vault, FakeSettings(), boards, compute = Dispatchers.Unconfined)` is written out ~29 times in this 711-line file (lines 168, 188, 198, 217, 232, 269, 293, 309, 322, 345, 367, 392, 411, 435, 447, 475, 503, 521, 534, 547, 564, 579, 590, 611, 624, 643, 666, 675, 686, 704). Two variants  | Confirmed | Fixed |
| F217 | app/src/test/java/dev/stan/yotsuba/util/NetworkErrorMappingTest.kt:16 | 6 canonical layer | medium | This whole 44-line class tests `toNetworkError`, which `ApiResultTest.kt` (same package `dev.stan.yotsuba.util`) already covers at lines 45-77 with the same cases: SocketTimeout→Timeout, UnknownHost/Connect→Offline, 404→NotFound, 429→RateLimited, 5xx→Server, 418→Unknown, arbitrary→Unknown. Both file | Confirmed | Fixed |
| F218 | app/src/test/java/dev/stan/yotsuba/vault/VaultPathsTest.kt:58 | 6 canonical layer | medium | Three tests in this file test `VaultMetaCodec`/`VaultThreadMeta`, not `VaultPaths`: `meta json round-trips and upserts by file name` (58-70), `meta remove drops the entry` (72-78) and `decode of garbage returns null` (80-83). `VaultMetaCodecTest.kt` already covers exactly these — `encode-decode roun | Confirmed | Fixed |
| F219 | .github/workflows/ci.yml:56 | 7 orchestration/atomicity | low | Four separate Gradle invocations run back to back — `:app:testDebugUnitTest` (56), `:app:lintDebug` (59), `:app:compileDebugAndroidTestKotlin` (65), `:app:assembleDebug` (68) — each paying full configuration and daemon warm-up, and each serialised even though lint and the androidTest compile share n | Rejected (Lines 56/59/65/68 are as cited, but this isn't a structural defect the rules flag. Named per-check steps are the direct, boring form: each failure is attributed) | Deferred (rejected in verification: Lines 56/59/65/68 are as cited, but this isn't a structural defect the rules flag. Named per-check steps are the direct, boring form: each failure is attributed to a step in the GitHub UI, and collapsing them into one `--continue` invocation trades that away for a modest daemon-warm-up saving. Rule 7 targets orchestration that makes an implementation brittle or leaves state half-applied; sequential CI checks on one runner do neither, and Gradle would still run these tasks in dependency order inside a single invocation anyway. Taste nit at best, a signal regression at worst.) |
| F220 | .gitignore:1 | 4 magic/wrappers | low | `.idea/` is partially tracked (git ls-files shows .idea/gradle.xml, misc.xml, runConfigurations.xml, etc.) while `.idea/modules.xml` and `.idea/yotsuba.iml` sit untracked in `git status` and the root .gitignore does not mention .idea at all; `.claude/` is also untracked and unignored. Every contribu | Rejected (Out of scope for this review: the skill flags structural code-quality problems (abstractions, spaghetti branching, file size, layer boundaries), not repo hygien) | Deferred (rejected in verification: Out of scope for this review: the skill flags structural code-quality problems (abstractions, spaghetti branching, file size, layer boundaries), not repo hygiene in .gitignore. It is also not a 'magic/wrappers' issue, and the cited line 1 ('.gradle/') has nothing to do with the claim. The factual core is thin too: .idea/ is deliberately partially tracked via its own .idea/.gitignore, so the untracked modules.xml/*.iml is a housekeeping preference, not a defect, and no code behaviour or maintainability of the app is affected.) |
| F221 | app/src/androidTest/java/dev/stan/yotsuba/SettingsFlowTest.kt:39 | 3 design cleanliness | low | The comment reads "Open Settings from the Boards gear", but the test never leaves the Home start destination; the gear it taps is HomeScreen's own (HomeScreen.kt:107-109, contentDescription from strings_home.xml:9 `home_settings` = "Settings"). BoardsScreen has a separate gear (AppNavHost.kt:190-194 | Confirmed | Fixed |
| F222 | app/src/main/java/dev/stan/yotsuba/core/backup/BackupModule.kt:14 | 6 canonical layer | low | `@ApplicationScope` and its provider have exactly one consumer, BackupRepositoryImpl.kt:42 (`rg -n -w 'ApplicationScope' app/src/main`). Meanwhile MediaDownloadQueue.kt:33 and Updater.kt:52 each build their own `CoroutineScope(SupervisorJob() + Dispatchers.IO)` for the same 'process-lifetime work no | Confirmed | Fixed |
| F223 | app/src/main/java/dev/stan/yotsuba/core/database/dao/DownloadedMediaDao.kt:3 | 4 magic/wrappers | low | An identical 11-line import block has been copy-pasted into five DAO files — BookmarkDao.kt:3-13, DownloadedMediaDao.kt:3-13, HiddenThreadDao.kt:3-13, HistoryDao.kt:3-13, SavedMediaDao.kt:3-13 — regardless of what each file uses. DownloadedMediaDao is the extreme case: it declares one query and impo | Confirmed | Fixed |
| F224 | app/src/main/java/dev/stan/yotsuba/core/database/dao/DownloadedMediaDao.kt:17 | 3 design cleanliness | low | The legacy `downloaded_media` table, `DownloadedMediaEntity`, `DownloadedMediaDao.allOnce()` and `YotsubaDatabase.downloadedMediaDao()` are kept alive solely by `VaultLegacyMigration` (VaultLegacyMigration.kt:37,52), a one-shot gated by the `vault_legacy_migrated_v1` preference (MediaVaultRepository | Rejected (The cited line (DownloadedMediaDao.kt:17) is the one place the entity IS used; the only actionable part — unused `DownloadedMediaEntity` imports — lives at line) | Deferred (rejected in verification: The cited line (DownloadedMediaDao.kt:17) is the one place the entity IS used; the only actionable part — unused `DownloadedMediaEntity` imports — lives at line 9 of four other files, and those files also carry three other unused entity imports each (a shared copy-pasted import block), so the finding both mislocates and under-describes it. Unused imports are a lint-level style nit, and the rest of the finding is 'schedule for deletion later', not a present defect.) |
| F225 | app/src/main/java/dev/stan/yotsuba/core/dedup/DHash.kt:64 | 4 magic/wrappers | low | The bit-emitting loop is `for (y in 0 until ROWS) { for (x in 0 until ROWS) { ... cell[y * COLS + x] vs cell[y * COLS + x + 1] ... } }`. The inner loop walks *columns* — it must run `COLS - 1` = 8 times — but is bounded by `ROWS`. It is correct only because ROWS (8) happens to equal COLS - 1 (8). Ch | Confirmed | Fixed |
| F226 | app/src/main/java/dev/stan/yotsuba/core/designsystem/component/MediaThumbnail.kt:5 | 3 design cleanliness | low | `import androidx.compose.foundation.layout.fillMaxSize` is unused — the file's three branches use `modifier.background(...)` (lines 31-32, 41-42, 55) and never call `fillMaxSize`. CLAUDE.md states lint findings are treated as real, and this is a standard `UnusedImports` hit. | Confirmed | Fixed |
| F227 | app/src/main/java/dev/stan/yotsuba/core/designsystem/component/StateViews.kt:57 | 5 type/boundary | low | `UiStateContent` accepts a `modifier` and forwards it to `LoadingSkeleton` (line 61) and `ErrorState` (line 62), but the `UiState.Success` branch calls `content(state.data)` (line 63) and drops it. A caller that passes `modifier = Modifier.padding(16.dp)` gets padding while loading and while errorin | Confirmed | Fixed |
| F228 | app/src/main/java/dev/stan/yotsuba/core/designsystem/component/StateViews.kt:113 | 5 type/boundary | low | `SearchField(..., hintRes: Int, ...)` types a string-resource id as a bare `Int` and passes it straight to `stringResource(hintRes)` (line 119). Nothing stops a caller passing a drawable id, a plurals id, or an arbitrary integer; the failure surfaces at runtime as a `NotFoundException` or a wrong st | Confirmed | Fixed |
| F229 | app/src/main/java/dev/stan/yotsuba/core/designsystem/MotionSpecs.kt:47 | 4 magic/wrappers | low | `LazyItemScope.animatedListItem()` (lines 36-44) and `LazyGridItemScope.animatedGridItem()` (lines 48-56) have byte-identical bodies — same reduced-motion guard, same `motion.medium`/`motion.medium`/`motion.short` triple — differing only in the receiver type. Retuning the list-item feel means rememb | Rejected (The duplication is real but trivial (three tween lines against two different scope receivers), and the proposed fix makes the code less legible, not more: a nul) | Deferred (rejected in verification: The duplication is real but trivial (three tween lines against two different scope receivers), and the proposed fix makes the code less legible, not more: a nullable Triple<FiniteAnimationSpec<Float>, FiniteAnimationSpec<IntOffset>, FiniteAnimationSpec<Float>> drops the named parameters (fadeInSpec/placementSpec/fadeOutSpec) that make the call sites readable and encodes the reduced-motion decision as a null. That trades boring, direct code for indirection, which rule 4 argues against. Taste nit at low severity.) |
| F230 | app/src/main/java/dev/stan/yotsuba/core/designsystem/theme/Color.kt:21 | 5 type/boundary | low | `LocalYotsubaColors`'s default block (lines 21-28) is a second, independently-maintained definition of the same four semantic colours that `yotsubaColors()` (lines 30-35) computes, and the two disagree on every field. The default sets `greentext = SeedColor` (#789922), a value the function never pro | Confirmed | Fixed |
| F231 | app/src/main/java/dev/stan/yotsuba/core/designsystem/theme/Theme.kt:104 | 3 design cleanliness | low | Lines 104-106 provide `LocalSpacing provides Spacing()`, `LocalElevation provides Elevation()`, `LocalMotion provides Motion()`. Each of those locals is declared as `staticCompositionLocalOf { Spacing() }` / `{ Elevation() }` / `{ Motion() }` (Spacing.kt:18, Elevation.kt:15, Motion.kt:13) — the prov | Confirmed | Fixed |
| F232 | app/src/main/java/dev/stan/yotsuba/core/di/DispatchersModule.kt:14 | 5 type/boundary | low | `@Provides fun computeDispatcher(): CoroutineDispatcher = Dispatchers.Default` binds the bare `CoroutineDispatcher` type in SingletonComponent with no qualifier. It has exactly one consumer, VaultViewModel.kt:241's `compute` parameter. Any future `CoroutineDispatcher` constructor parameter anywhere  | Confirmed | Fixed |
| F233 | app/src/main/java/dev/stan/yotsuba/core/media/SoundPost.kt:37 | 4 magic/wrappers | low | ``` repeat(MAX_DECODE_PASSES) {     val next = percentDecode(decoded)     if (next == decoded) return@repeat     decoded = next } ``` `return@repeat` returns from the lambda, i.e. it is `continue`, not `break` — the comment above it ("decode until the string stops changing") reads as an early exit t | Confirmed | Fixed |
| F234 | app/src/main/java/dev/stan/yotsuba/core/network/ArchiveHosts.kt:49 | 2 spaghetti growth | low | `ArchiveSource.WAROSU` is a domain enum value (ThreadPost.kt:47, doc: "has no JSON API, so nothing reads from it yet") that `sourceFor` (245) resolves for 13 boards including /g/ and /biz/, after which `apiUrl` returns null (261) and ThreadRepositoryImpl.archivedThread (line 36) turns it into `Netwo | Rejected (ArchiveHosts.kt is 51 lines long, so line 232 does not exist and the nearby real code (the WAROSU arm at line 49) does not support the claim. The file is a sing) | Deferred (rejected in verification: ArchiveHosts.kt is 51 lines long, so line 232 does not exist and the nearby real code (the WAROSU arm at line 49) does not support the claim. The file is a single small lookup table with one two-arm `when`, explicitly documented as the extension hook for a Fuuka scraper; that is not ad-hoc spaghetti growth the rules would flag. WAROSU is also live in `threadUrl` (ThreadViewModel.kt:148 builds the browser link from it), and the proposed split into a `webOnly` map would rewrite ArchiveHostsTest assertions and change `sourceFor`'s contract for 13 boards for no maintainability gain. Taste nit at a wrong line.) |
| F235 | app/src/main/java/dev/stan/yotsuba/core/network/NetworkMonitor.kt:49 | 4 magic/wrappers | low | `val metered: Flow<Boolean> = status.map { it.isMetered }.distinctUntilChanged()` has no callers: a grep over app/src finds only `networkMonitor.status` (CatalogViewModel.kt:53, MediaViewModel.kt:149) and `networkMonitor.current()` (Modules.kt:90, VaultSyncWorker.kt:55). It is a pass-through over a  | Confirmed | Fixed |
| F236 | app/src/main/java/dev/stan/yotsuba/core/update/GithubReleases.kt:70 | 5 type/boundary | low | okhttp is pinned at 5.5.0 (gradle/libs.versions.toml:11), where `Response.body` is non-null. Line 70 asserts it anyway (`body!!.string()`) while Updater.kt:127 treats the same property as nullable (`val body = resp.body ?: throw ReleaseException("empty response")`, a branch that can never be taken). | Confirmed | Fixed |
| F237 | app/src/main/java/dev/stan/yotsuba/core/util/Urls.kt:23 | 6 canonical layer | low | Protocol-relative URL normalisation `if (x.startsWith("//")) "https:$x" else x` appears in Urls.parseInternal L23, Urls.domainOf L43, PostHtmlParser.resolveUrl L136 and SoundPost.toUrl L45. | Rejected (Duplicate of F102 — same duplication, same four sites, same proposed fix, just cited at Urls.kt:23 instead of :43 and with a weaker write-up. Reporting the same) | Deferred (rejected in verification: Duplicate of F102 — same duplication, same four sites, same proposed fix, just cited at Urls.kt:23 instead of :43 and with a weaker write-up. Reporting the same extraction twice is exactly the nit-flooding the rules warn against; F102 carries the finding.) |
| F238 | app/src/main/java/dev/stan/yotsuba/core/vault/VaultMeta.kt:32 | 3 design cleanliness | low | The KDoc says "Replaces any entry with the same fileName, keeps order stable", but the body is `val kept = files.filterNot { it.fileName == entry.fileName }; return copy(files = kept + entry)` — an update moves the entry to the end of the list. Every re-save of an existing file (MediaVaultRepository | Confirmed | Fixed |
| F239 | app/src/main/java/dev/stan/yotsuba/core/vault/VaultPosts.kt:124 | 3 design cleanliness | low | VaultPostsCodec (VaultPosts.kt L124-136) and VaultMetaCodec (VaultMeta.kt L67-79) are the same object twice: identical `Json { ignoreUnknownKeys = true; prettyPrint = true; encodeDefaults = false }`, an encode, and a `runCatching { decodeFromString }.getOrNull()` decode. Same with the two transparen | Confirmed | Fixed |
| F240 | app/src/main/java/dev/stan/yotsuba/core/vault/VideoStills.kt:35 | bug | low | `val scaled = shrink(frame)` (line 35) is compressed to JPEG and then neither `frame` nor `scaled` is recycled; the `retriever` is released in `finally` but the bitmaps are not. `shrink` returns `frame` itself when the frame is already small (line 53), so a naive `frame.recycle()` would be wrong for | Confirmed | Fixed |
| F241 | app/src/main/java/dev/stan/yotsuba/core/widget/WatchedThreadsWidget.kt:125 | 6 canonical layer | low | `sizeBucket(size: DpSize): WidgetSizeBucket` is a pure function that lives in the Glance file, while its other half — `WidgetSizeBucket` and `rowLimitFor(bucket)` — lives in WidgetRows.kt with the comment "Pure data so the ordering can be tested on the JVM and the Glance composable stays a dumb rend | Confirmed | Fixed |
| F242 | app/src/main/java/dev/stan/yotsuba/core/widget/WatchedThreadsWidget.kt:174 | ui/ux | low | The header's Refresh control is a `Text` at 12.sp with `.padding(horizontal = 6.dp, vertical = 2.dp).clickable(...)` — roughly a 20dp-tall tap target on a home screen, well under the 48dp minimum, and it is styled as coloured text with no button affordance beyond colour. The header also renders unco | Confirmed | Fixed |
| F243 | app/src/main/java/dev/stan/yotsuba/core/work/BookmarkRefreshWorker.kt:73 | 2 spaghetti growth | low | notify() gates one thing — may we post — three times, at three points in the function: areNotificationsEnabled() at 51, the SDK/POST_NOTIFICATIONS check at 73-76, and the SecurityException catch at 79-81. The middle check runs *after* the channel is created (52-58), the launch PendingIntent is built | Confirmed | Fixed |
| F244 | app/src/main/java/dev/stan/yotsuba/core/work/VaultSyncWorker.kt:83 | 3 design cleanliness | low | WorkManagerVaultSyncScheduler L83-100 is a copy of WorkManagerBookmarkRefreshScheduler (core/work/BookmarkRefreshScheduler.kt L23-41): same isInitialized guard, same PeriodicWorkRequestBuilder + CONNECTED constraint, same KEEP enqueue; only the worker class, interval and unique name differ. Two inte | Confirmed | Fixed |
| F245 | app/src/main/java/dev/stan/yotsuba/data/repository/BookmarkRepositoryImpl.kt:29 | 5 type/boundary | low | `internal var clock: () -> Long = System::currentTimeMillis` is mutable state on a `@Singleton`, existing only so tests can swap it; `BackupRepositoryImpl.kt:59-60` does the same twice (`internal var rootOverride: File?`, `internal var ioDispatcher: CoroutineDispatcher`). The package already contain | Confirmed | Fixed |
| F246 | app/src/main/java/dev/stan/yotsuba/data/repository/Mappers.kt:71 | 4 magic/wrappers | low | The same three-line derivation is copy-pasted at lines 71-73 and 130-132: `body.segments.mapNotNull { (it.annotation as? PostAnnotation.QuotelinkSameThread)?.postNo }.distinct()`. It is a pure function of `PostText`, and `PostText` (PostAnnotations.kt:55-60) already hosts exactly this kind of derive | Confirmed | Fixed |
| F247 | app/src/main/java/dev/stan/yotsuba/data/repository/Mappers.kt:168 | 4 magic/wrappers | low | `archiveCommentToHtml` (added by 98a1729) compiles `Regex("&gt;&gt;(\\d+)")` inside the `joinToString` lambda, so a fresh Regex is built for every line of every post in an archived thread — and the file already demonstrates the right pattern seven lines above at line 161, where `ARCHIVE_QUOTELINK` i | Confirmed | Fixed |
| F248 | app/src/main/java/dev/stan/yotsuba/data/repository/Mappers.kt:240 | 5 type/boundary | low | `HistoryEntity.toDomain()` (240-248) and `HistoryEntry.toEntity()` (250-258) omit `maxReadPostNo`, so HistoryRepositoryImpl.kt:63-68 adds two private wrappers `toDomainWithReadMark`/`toFullEntity` that `.copy(maxReadPostNo = maxReadPostNo)` back on. The comment says the drop is "on purpose for calle | Confirmed | Fixed |
| F249 | app/src/main/java/dev/stan/yotsuba/data/repository/MediaVaultRepositoryImpl.kt:375 | 4 magic/wrappers | low | Two overrides exist where one would do: `syncSavedThreads(onProgress) = syncSavedThreads(onProgress, emptySet())` at lines 375-377 is a pure pass-through to the override on 379. The interface makes it worse — MediaVaultRepository.kt:88-91 declares the 2-arg form with a *default body* `= syncSavedThr | Rejected (The observation is right — MediaVaultRepository.kt:88-91 gives the 2-arg form a default body that silently drops `skip` — but the proposed fix does not compile ) | Deferred (rejected in verification: The observation is right — MediaVaultRepository.kt:88-91 gives the 2-arg form a default body that silently drops `skip` — but the proposed fix does not compile as claimed. Roughly ten fakes override only the 1-arg form (VaultViewModelTest.kt:113, MediaViewModelTest.kt:121, ThreadViewModelTest.kt:155, BookmarksViewModelTest.kt:97, ThreadQuoteTapTest.kt:71, ThreadRefreshFailureTest.kt:69, DedupQueueTest.kt:41, VaultDedupViewModelTest.kt:68, MediaDownloadQueueTest.kt:61, androidTest TestRepositoryModule.kt:313); collapsing to a single 2-param method makes every one of those a non-override and breaks the test and androidTest source sets. The evidence's own claim that existing call sites compile unchanged is false.) |
| F250 | app/src/main/java/dev/stan/yotsuba/data/repository/SavedMediaMappers.kt:99 | 5 type/boundary | low | `VaultLocation(board = board!!, threadNo = threadNo!!)` uses double-bang inside a branch that has already proven both non-null on the line above (`if (board != null && board != VaultPaths.UNSORTED_DIR_NAME && threadNo != null)`). `SavedMediaEntity` is a `data class` with `val board: String?` / `val  | Rejected (Duplicate of F118 at the same expression (SavedMediaMappers.kt:98-99) with the same fix; on its own, dropping two !! is a taste-level nit that the rules would n) | Deferred (rejected in verification: Duplicate of F118 at the same expression (SavedMediaMappers.kt:98-99) with the same fix; on its own, dropping two !! is a taste-level nit that the rules would not flag separately.) |
| F251 | app/src/main/java/dev/stan/yotsuba/domain/model/MediaItem.kt:29 | 3 design cleanliness | low | `hasSound` has no readers: `rg -n -w hasSound app/src` returns only the declaration; MediaFeedViewer.kt:272 does `current?.soundUrl != null` directly. `isAnimated` (line 32) is referenced only from MappersTest.kt:107-113, never from main. `MediaViewModel`'s `isSaved(url)` (MediaViewModel.kt:75) is l | Confirmed | Fixed |
| F252 | app/src/main/java/dev/stan/yotsuba/domain/model/PostGraph.kt:15 | 4 magic/wrappers | low | The secondary constructor at l.15-17, `constructor(byNo: Map<Long, ThreadPost>, backlinks: Map<Long, List<Long>>) : this(byNo.values.toList(), backlinks)`, is documented as "Map form kept for the media viewer; iteration order of the map is the thread order" — but the media viewer does not use it. Ev | Confirmed | Fixed |
| F253 | app/src/main/java/dev/stan/yotsuba/domain/repository/BookmarkRepository.kt:41 | 5 type/boundary | low | `refreshAll(...) = BookmarkRefreshSummary()`, `setPinned(...) {}` and `removeDead() {}` have no-op defaults in the interface (lines 41-42, 54, 57) even though BookmarkRepositoryImpl overrides all three and production calls them (BookmarkRefreshWorker, bookmarks screen). Same silent-fallback pattern  | Confirmed | Fixed |
| F254 | app/src/main/java/dev/stan/yotsuba/domain/repository/MediaVaultRepository.kt:114 | 5 type/boundary | low | ade3157 added two more repository methods with bodies on the interface that quietly fail: line 114 `suspend fun renameThread(...): VaultError? = VaultError.Io("not supported")` and line 122 the same for `mergeThreads`. There is exactly one production implementor (MediaVaultRepositoryImpl), so these  | Rejected (Real but duplicative: renameThread/mergeThreads are two of the eleven defaults already itemised at the same lines by F033, whose fix (abstract members plus a ty) | Deferred (rejected in verification: Real but duplicative: renameThread/mergeThreads are two of the eleven defaults already itemised at the same lines by F033, whose fix (abstract members plus a typed VaultError.Unsupported) is this finding's fix. The finding also concedes the English string never reaches a user, so nothing survives that F033 does not already carry.) |
| F255 | app/src/main/java/dev/stan/yotsuba/feature/bookmarks/BookmarksList.kt:101 | 6 canonical layer | low | VaultError -> R.string mapping (NoAccess->vault_error_no_access, NotFound->vault_error_not_found, Io->vault_error_io) exists in BookmarksList L100-105 (three pre-resolved strings) and DownloadAction.errorLabel (feature/media/DownloadAction.kt L140-144). | Confirmed | Fixed |
| F256 | app/src/main/java/dev/stan/yotsuba/feature/bookmarks/BookmarksList.kt:111 | 3 design cleanliness | low | The comment says "Auto-refresh whenever the tab comes back on screen (throttled in the ViewModel), so the pills update without a manual pull", and BookmarksViewModel.kt:73-75 says "Fired every time the screen comes on screen … at most once a minute so tab-hopping doesn't hammer the API". Neither is  | Rejected (Misreads the code. OnResumeEffect wraps LifecycleResumeEffect, whose block runs when the effect enters composition while the owner is already RESUMED (the lifec) | Deferred (rejected in verification: Misreads the code. OnResumeEffect wraps LifecycleResumeEffect, whose block runs when the effect enters composition while the owner is already RESUMED (the lifecycle observer is replayed to the current state), not only on a foreground return. The Crossfade in ThreadsScreen.kt:137-150 disposes/recreates BookmarksList when the segment changes, so switching back to WATCHED does re-fire onScreenVisible, and the 60 s throttle guards exactly that path. Both comments are accurate.) |
| F257 | app/src/main/java/dev/stan/yotsuba/feature/bookmarks/BookmarksList.kt:190 | ui/ux | low | `BookmarkActionSheet` hardcodes spacing that the tokens already name: `Column(Modifier.padding(bottom = 24.dp))` (line 190), `padding(horizontal = 24.dp, vertical = 8.dp)` (line 195) — `Spacing.xl` is 24.dp and `Spacing.sm` is 8.dp (token/Spacing.kt:13-14) — and `Arrangement.spacedBy(4.dp)` at line  | Confirmed | Fixed |
| F258 | app/src/main/java/dev/stan/yotsuba/feature/bookmarks/BookmarksViewModel.kt:33 | 5 type/boundary | low | Refresh progress crosses the ViewModel/UI boundary as `val checking: Pair<Int, Int>?` — "Non-null while a refresh pass is running: boards done / boards total". `onRefreshAll` seeds it with the meaningless `0 to 0` (line 87) before the repository reports a total, which forces the consumer to paper ov | Confirmed | Fixed |
| F259 | app/src/main/java/dev/stan/yotsuba/feature/media/MediaScreen.kt:184 | ui/ux | low | While a share is being prepared the IconButton is `enabled = !sharing` and its content swaps to a bare `CircularProgressIndicator(Modifier.size(24.dp), …)` with no contentDescription (:184-188). A disabled IconButton with unlabelled content leaves the control with no accessible name for the duration | Confirmed | Fixed |
| F260 | app/src/main/java/dev/stan/yotsuba/feature/media/VideoPage.kt:112 | 6 canonical layer | low | VideoPage builds an ExoPlayer L112-118 exactly as rememberSoundPlayer does in SoundTrack.kt L24-31 (`ExoPlayer.Builder(context).build().apply { setMediaItem(fromUri); repeatMode = REPEAT_MODE_ONE; prepare() }`), and both install the same LifecycleEventObserver (VideoPage L153-165, SoundTrack L45-57: | Rejected (The lifecycle-observer half is the same defect as F161 (already confirmed); what is left is a six-line ExoPlayer.Builder block, and the proposed rememberLooping) | Deferred (rejected in verification: The lifecycle-observer half is the same defect as F161 (already confirmed); what is left is a six-line ExoPlayer.Builder block, and the proposed rememberLoopingPlayer(uri: String?) returns a nullable player that VideoPage — which needs a non-null player for PlayerSurface, repeatMode mutation and prepare() on retry — would have to force-unwrap, making the type boundary worse rather than better.) |
| F261 | app/src/main/java/dev/stan/yotsuba/feature/settings/sections/FiltersSection.kt:80 | 3 design cleanliness | low | Two fully-qualified references sit in a file with 40 explicit imports: `androidx.compose.runtime.key(filter.id)` at line 80 and `horizontalArrangement = androidx.compose.foundation.layout.Arrangement.spacedBy(spacing.sm)` at line 236. No other file in the slice does this — the neighbouring `Row`, `C | Confirmed | Fixed |
| F262 | app/src/main/java/dev/stan/yotsuba/feature/settings/sections/FiltersSection.kt:158 | 3 design cleanliness | low | `filter.error?.let { Text(stringResource(R.string.filters_invalid_regex), …) }` discards `it` — the captured error message — and prints a fixed generic string, while `FilterDialog` at line 209 shows the real text via `supportingText = regexError?.let { { Text(it, …) } }`. The row therefore tells the | Confirmed | Fixed |
| F263 | app/src/main/java/dev/stan/yotsuba/feature/settings/UpdatesSection.kt:30 | 3 design cleanliness | low | `UpdatesSection` is the only section body that does not live in `feature/settings/sections/`. The other eight (AboutSection, AppearanceSection, BoardsSection, FiltersSection, LinksSection, MediaSection, ReadingSection, StorageSection) are all in that package and are imported one-per-line at Settings | Confirmed | Fixed |
| F264 | app/src/main/java/dev/stan/yotsuba/feature/settings/UpdatesSection.kt:33 | 3 design cleanliness | low | `onInstall: (dev.stan.yotsuba.core.update.Release) -> Unit` spells the type fully qualified in the signature even though `dev.stan.yotsuba.core.update.Updater` is imported two lines above (line 22). Separately, this file sits at feature/settings/UpdatesSection.kt while the other eight section bodies | Confirmed | Fixed |
| F265 | app/src/main/java/dev/stan/yotsuba/feature/thread/components/ThreadGallerySheet.kt:62 | ui/ux | low | `items(posts, key = { it.no }) { post -> val media = post.presentMedia ?: return@items ... }` emits an empty grid cell for any post without present media, leaving a hole in the grid. The caller passes `s.mediaPosts` (ThreadScreen.kt:441), which ThreadViewModel.kt:160 already filters to `presentMedia | Confirmed | Fixed |
| F266 | app/src/main/java/dev/stan/yotsuba/feature/thread/ThreadScreen.kt:230 | 6 canonical layer | low | `UiStateContent(state, onRetry = { viewModel.load() })` retries through the cache, while CatalogPane.kt:154 retries with `viewModel::retry`, which `CatalogViewModel.retry()` (CatalogViewModel.kt:75) defines as `load(forceRefresh = true, showLoading = true)` with the comment "bypass the cache like pu | Confirmed | Fixed |
| F267 | app/src/main/java/dev/stan/yotsuba/feature/thread/ThreadScreen.kt:309 | 3 design cleanliness | low | Lines 309-334 are two copies of the same banner — same `labelMedium` style, same `onSurfaceVariant` colour, same `fillMaxWidth().background(surfaceVariant).padding(spacing.sm)` modifier chain — differing only in which string they resolve. Twenty-four lines of the screen's top-level layout are consum | Confirmed | Fixed |
| F268 | app/src/main/java/dev/stan/yotsuba/feature/thread/ThreadScreen.kt:415 | 6 canonical layer | low | Plain-text share intent (`Intent(ACTION_SEND).apply { type = "text/plain"; putExtra(EXTRA_TEXT, ...) }; startActivity(createChooser(...))`) is duplicated in ThreadScreen L416-420 and ThreadTopBar L131-135 (the top-bar copy also lacks the runCatching the screen copy has). feature/media/MediaShare.kt  | Confirmed | Fixed |
| F269 | app/src/main/java/dev/stan/yotsuba/feature/thread/ThreadViewModel.kt:89 | 5 type/boundary | low | 2b7dd4e folded the thread's session flows into one state — a good move — but exposed the mutable holder: line 88-89 `/** Exposed for tests: every change to it is one atomic emission. */ val session = MutableStateFlow(Session())`. It is public and writable, so any composable holding the ViewModel can | Confirmed | Fixed |
| F270 | app/src/main/java/dev/stan/yotsuba/feature/thread/ThreadViewModel.kt:290 | 6 canonical layer | low | recordHistory L290-300 and onToggleBookmark L329-345 both derive the same row fields from the OP: `subject = op.subject, opExcerpt = op.body.plainText.take(200), thumbnailUrl = op.presentMedia?.thumbnailUrl`. Meanwhile domain has three copies of `displayTitle = subject ?: opExcerpt.take(60).ifBlank  | Rejected (Thin-abstraction nit, not a rule-6 violation. The ViewModel repetition is three field assignments at two sites in one file, constructing two different domain ty) | Deferred (rejected in verification: Thin-abstraction nit, not a rule-6 violation. The ViewModel repetition is three field assignments at two sites in one file, constructing two different domain types; a ThreadSummary value object would add indirection without deleting a concept (rule 4 explicitly warns against this). The three displayTitle getters are one-liners with genuinely different inputs and fallbacks (CatalogThread reads PostText.plainText and falls back to "#$no", the other two to "/$board/$threadNo"), and writeSnapshot's take(60) is unrelated code in the data layer.) |
| F271 | app/src/main/java/dev/stan/yotsuba/feature/thread/ThreadViewModel.kt:599 | ui/ux | low | ``` val visible = visiblePosts(details.posts, session).mapTo(HashSet()) { it.no } val nodes = PostGraph.of(details).tree().filter { it.post.no in visible && ... } ``` The tree is built over all posts and then filtered, but each surviving node keeps the `depth` it had in the unfiltered tree. With a p | Confirmed | Fixed |
| F272 | app/src/main/java/dev/stan/yotsuba/feature/vault/VaultExplorer.kt:203 | ui/ux | low | The search branch handles its empty case explicitly (145-151, "vault_search_empty"), but the thread grid (201-211) and the Recent grid (172-183) render `MediaGrid` unconditionally. Set the type filter to Videos inside an image-only thread and the screen is a chip row above a blank area, with the shu | Confirmed | Fixed |
| F273 | app/src/main/java/dev/stan/yotsuba/feature/vault/VaultExplorer.kt:233 | 6 canonical layer | low | "N items · <size>" is assembled three times: BoardList L233-234, ThreadList L279-281 (plus ' · date'), VaultScreen.vaultSubtitle L730-731; MergeDialog L502 does the count alone. VaultDedupSheet.Thumb L290-294 re-derives the thumbnail model (`!isVideo -> File(path); thumbnailPath != null -> File(thum | Confirmed | Fixed |
| F274 | app/src/main/java/dev/stan/yotsuba/feature/vault/VaultExplorer.kt:247 | 2 spaghetti growth | low | Commit 2619e61 ("Animate list changes") added `animatedListItem()` / `animatedGridItem()` to the vault's three lists. Three commits later, 06914f1 ("Scroll the vault lists back to the top when sort, direction or filter changes") silently reverted all three — `modifier = animatedListItem().clickable  | Confirmed | Fixed |
| F275 | app/src/main/java/dev/stan/yotsuba/feature/vault/VaultExplorer.kt:612 | 6 canonical layer | low | Five independent DateFormat spellings for the same job: VaultExplorer L612 and VaultStatsSheet L217 both declare `private val dateFormat = DateFormat.getDateInstance(DateFormat.MEDIUM)`; VaultDedupSheet L311 calls `DateFormat.getDateInstance(DateFormat.SHORT)` inside a composable per row; ThreadScre | Rejected (Misreads the code. The five call sites are not "the same job": DateFormat.SHORT (VaultDedupSheet:311), the default style (ThreadScreen:311) and getDateTimeInsta) | Deferred (rejected in verification: Misreads the code. The five call sites are not "the same job": DateFormat.SHORT (VaultDedupSheet:311), the default style (ThreadScreen:311) and getDateTimeInstance(MEDIUM, SHORT) (StorageSection:146) render deliberately different strings, so folding them into one savedDate/dateTime pair changes visible text — the finding claims behaviourChange:false. Likewise formatMs (VideoPage:413) intentionally never rolls over to hours ("75:03"), while formatDuration emits "1:15:03"; swapping them changes the scrubber label on long videos. The cited line 612 is itself the canonical helper (internal savedDate, already reused by VaultEntrySheet:90), not a defect; the only real duplicate is the private dateFormat in a different file, VaultStatsSheet.kt:217.) |
| F276 | app/src/main/java/dev/stan/yotsuba/feature/vault/VaultScreen.kt:412 | 7 orchestration/atomicity | low | Tapping a thread in the statistics sheet (ca18442) does `viewModel.openBoard(location.board)` then `viewModel.openThread(location)` on consecutive lines 412-413. `openBoard` sets `selection` to `VaultSelection(board = board)` (thread = null) and `openThread` then copies `thread` in — two separate `M | Confirmed | Fixed |
| F277 | app/src/main/java/dev/stan/yotsuba/feature/vault/VaultScreen.kt:735 | ui/ux | low | `private fun defaultImportName(count: Int): String = if (count > 0) "Imported ($count)" else "Imported"` — a hardcoded English string that becomes the persisted thread directory name and the title shown in the thread list. Every other user-visible string on this screen goes through `stringResource`  | Confirmed | Fixed |
| F278 | app/src/main/java/dev/stan/yotsuba/feature/vault/VaultStatsSheet.kt:67 | 3 design cleanliness | low | `items(stats.perBoard, key = { it.board }) { BoardBar(it, stats.perBoard.first().bytes) }` recomputes `stats.perBoard.first()` inside every row's composition to obtain the same scale denominator, and `BoardBar` then re-derives the fraction with a `if (largest > 0)` guard (VaultStatsSheet.kt:132). Th | Rejected (Taste nit resting on a misread. perBoard is sorted bytes-descending (VaultStats.kt:47), so first().bytes is the max and the call is a trivial O(1) list head rea) | Deferred (rejected in verification: Taste nit resting on a misread. perBoard is sorted bytes-descending (VaultStats.kt:47), so first().bytes is the max and the call is a trivial O(1) list head read per row, not a meaningful recomputation. The `largest > 0` guard is not dead code by construction: the finding itself names the case that reaches it (every row's sizeBytes null so every board totals 0 bytes), so removing it without a new invariant risks a divide-by-zero NaN fraction. Rules 3/4 do not flag hoisting a one-token expression out of a lambda.) |
| F279 | app/src/main/java/dev/stan/yotsuba/feature/vault/VaultViewModel.kt:475 | 4 magic/wrappers | low | `stateIn(viewModelScope, SharingStarted.WhileSubscribed(5_000), VaultStats.of(emptyList(), 0L))` builds the sentinel initial value by running the real aggregation over an empty list with a fabricated clock of `0L`. The `0L` is meaningless — `of` documents `now` as the end of the rolling week windows | Confirmed | Fixed |
| F280 | app/src/main/java/dev/stan/yotsuba/navigation/AppNavHost.kt:116 | 3 design cleanliness | low | The tab-item loop is written twice with only the composable name changed: NavigationRail L116-124 (NavigationRailItem) and NavigationBar L137-145 (NavigationBarItem), each computing `isSelected(dest)`, icon = selected ? selectedIcon : unselectedIcon, label = stringResource(labelRes), onClick = navig | Rejected (The duplication is real at L114-124 and L136-146, but it is ~7 lines of declarative Compose slot wiring across two Material3 composables (NavigationRailItem/Nav) | Deferred (rejected in verification: The duplication is real at L114-124 and L136-146, but it is ~7 lines of declarative Compose slot wiring across two Material3 composables (NavigationRailItem/NavigationBarItem) that share no supertype or common API. The proposed fix is a higher-order composable taking a `(selected, onClick, icon, label) -> Unit` item factory — exactly the thin, indirection-adding wrapper rule 4 tells reviewers to be sceptical of, and it would not delete a concept, only relocate it. This is a low-value nit the review is told not to flood the output with.) |
| F281 | app/src/main/java/dev/stan/yotsuba/navigation/ExternalLinks.kt:21 | 6 canonical layer | low | `text?.let { URL.findAll(it) }?.map { m -> Urls.parseInternal(...) }?.firstOrNull { it != null }` builds an intermediate `Sequence<InternalLink?>` and then filters nulls out of it by hand — three chained safe calls to express "first URL in the text that parses". Kotlin's canonical helper for exactly | Confirmed | Fixed |
| F282 | app/src/main/res/drawable/ic_launcher_monochrome.xml:1 | 4 magic/wrappers | low | `ic_launcher_monochrome.xml` and `ic_launcher_foreground.xml` are byte-identical: same `108dp`/`108` viewport, same `android:fillColor="#FFFFFF"`, same 3-subpath `pathData`. Two files, one drawable — a themed-icon tweak to the glyph now has to be made twice or the launcher's themed icon silently div | Confirmed | Fixed |
| F283 | app/src/test/java/dev/stan/yotsuba/network/NetworkLayerTest.kt:7 | 3 design cleanliness | low | `import dev.stan.yotsuba.core.util.Urls` — `grep -n "Urls" NetworkLayerTest.kt` returns only line 7, the import itself. Nothing in the file references `Urls`. CLAUDE.md states "lint errors are real crashes, not pedantry", so dead imports in the test sources are the kind of thing that should not be a | Rejected (The unused import at line 7 is real (grep finds `Urls` only on that line), but it is a cosmetic lint nit, not something this review's rules flag: no structural ) | Deferred (rejected in verification: The unused import at line 7 is real (grep finds `Urls` only on that line), but it is a cosmetic lint nit, not something this review's rules flag: no structural regression, no branching or abstraction problem, no file-size or layering issue. The skill explicitly says not to flood the review with low-value nits and not to settle for cosmetic notes.) |
| F284 | app/src/test/java/dev/stan/yotsuba/vault/VaultSnapshotTest.kt:23 | 6 canonical layer | low | `diff <(sed -n '22,49p' VaultPruneTest.kt) <(sed -n '23,42p' VaultSnapshotTest.kt)` shows the only difference is VaultPruneTest's extra `savedDir` helper: the `@get:Rule val tmp`, `private val store by lazy { VaultStore(tmp.root) }`, the `post(no, quotes, isOp)` builder, the `/** OP 1; 2 -> 1; 3 ->  | Confirmed | Fixed |
| F285 | gradle/libs.versions.toml:27 | 4 magic/wrappers | low | Stale catalog entries: `retrofitSerialization = "3.0.0"` (line 28) duplicates `retrofit = "3.0.0"` for a module from the same artifact family; `okhttp-logging` (line 55) is declared but `HttpLoggingInterceptor` appears nowhere in app/src and the library is not in build.gradle.kts; `kotlin-android` p | Confirmed | Fixed |
| F286 | gradle/libs.versions.toml:60 | 4 magic/wrappers | low | Two catalog entries are declared and referenced by nothing: `okhttp-logging` (line 60) — grepping `*.kts` for `okhttp.logging`/`okhttp-logging` outside the catalog returns no hits — and the `kotlin-android` plugin alias (line 81), which neither `build.gradle.kts` nor `app/build.gradle.kts` applies.  | Rejected (Duplicate of F285 — same okhttp-logging, kotlin-android and inline-test-version observations with no additional content; keeping both would double-report one is) | Deferred (rejected in verification: Duplicate of F285 — same okhttp-logging, kotlin-android and inline-test-version observations with no additional content; keeping both would double-report one issue.) |
