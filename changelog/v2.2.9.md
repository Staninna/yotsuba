## Added
- **uguu.se as a backup host.** Reverse searching a local file now falls back to uguu.se, which deletes it after three hours, when litterbox refuses the upload, and 0x0.st after that.

## Fixed
- **Save frame button clipped.** In the frame picker the button keeps its full label at large font sizes and drops under the time when it needs the room.
- **Bars over the picture-in-picture window.** The top and bottom bars stay hidden while a video plays in the small window.
