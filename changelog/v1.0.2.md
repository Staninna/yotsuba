## Fixed
- **Updates on phones that block silent installs.** When your phone refuses to update Yotsuba in the background (HyperOS does this), the Settings update now falls back to Android's normal install dialog instead of failing with "Permission denied".
