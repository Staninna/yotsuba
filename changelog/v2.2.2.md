## Changed
- **Every release is launched before it ships.** Each build now gets installed and started on an emulator before publishing, so an update that crashes on launch never reaches your phone.
- **Saved threads are checked before every release.** The release build is verified to still read the vault sidecars, so a saved thread cannot come back as missing after an update.
