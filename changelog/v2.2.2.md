## Changed
- **Release checks.** Every update is now installed and launched on a test device before it is published, so a build that crashes on launch (as 1.1.1 did) never reaches your phone.
- **Saved thread checks.** Every release is also checked to still read the files that describe your saved threads, so a thread saved in the Vault cannot show up as missing after an update.
